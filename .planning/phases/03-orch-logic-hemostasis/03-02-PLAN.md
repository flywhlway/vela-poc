---
phase: 03-orch-logic-hemostasis
plan: 02
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - src/vela/agent/graph.py
  - config/llm.yaml
  - tests/test_agent.py
  - tests/test_gateway.py
autonomous: true
requirements:
  - ORCH-03
  - ORCH-04

must_haves:
  truths:
    - "ORCH-03：_parse_json 禁止跨段花括号提取；合法围栏 JSON 可解析；失败经 _llm_json 显式重试 2 次并发 llm.parse_failure ALERT"
    - "ORCH-04：planner/verifier max_tokens==2048；finish_reason==length 时 emit llm.truncation ALERT 且 metrics.inc"
    - "llm_parse_failure_rate / llm_truncation_rate 具备真实事件观测点（供后续 process 聚合）"
  artifacts:
    - path: "src/vela/agent/graph.py"
      provides: "_parse_json 加固 + _llm_json + truncation 感知"
      exports: ["_parse_json", "_llm_json"]
    - path: "config/llm.yaml"
      provides: "planner/verifier max_tokens 2048"
      contains: "max_tokens: 2048"
  key_links:
    - from: "node_plan / node_verify"
      to: "_llm_json"
      via: "JSON 逻辑模型统一走 _llm_json，不再手写重试环"
      pattern: "_llm_json\\("
    - from: "LLMResponse.finish_reason"
      to: "EventBus llm.truncation"
      via: "graph._llm/_llm_json 包装层检测 length"
      pattern: "llm\\.truncation"
---

<objective>
落地 ORCH-03/ORCH-04：加固 LLM JSON 输出契约（解析、重试、截断感知）并上调 planner/verifier max_tokens 至 2048。

Purpose: 消除基线中 llm_parse_failure_rate≈0.63 / llm_truncation_rate≈0.25 的结构性根因（静默 `{}`、跨段假成功、token 过小）。
Output: `_parse_json` + `_llm_json`；`llm.yaml` 2048；truncation/parse ALERT；对应 Wave 0 测试摘除 xfail 并转绿。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md
@.planning/phases/03-orch-logic-hemostasis/03-PATTERNS.md
@AGENTS.md
@src/vela/agent/graph.py
@config/llm.yaml
@src/vela/gateway/base.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 加固 _parse_json 并实现 _llm_json（ORCH-03）</name>
  <files>src/vela/agent/graph.py, tests/test_agent.py</files>
  <read_first>
    - src/vela/agent/graph.py（`_parse_json` L51-65；`_llm` L124-128；`node_plan`/`node_verify` 中 `_parse_json(self._llm(...))` 调用点）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（Code Examples ORCH-03；Discretion：`_llm_json(..., retries=2)`）
    - .planning/phases/03-orch-logic-hemostasis/03-PATTERNS.md（Anti-pattern 跨段花括号；EventBus ALERT 模式）
    - tests/test_agent.py（Wave 0 `test_parse_json_*`）
  </read_first>
  <behavior>
    - 整段非法 JSON → `{}`，且不得通过第一个 `{` 与最后一个 `}` 切片「抢救」
    - ```json 围栏剥离后合法对象 → 非空 dict
    - `_llm_json`：最多 1 次初始调用 + 失败后重试 2 次（共 3 次尝试）；耗尽仍 `{}` 时 `bus.emit("llm.parse_failure", Severity.ALERT, ...)` + `metrics.inc("llm.parse_failure")`
    - planner/verifier/distiller 等 JSON 节点改用 `_llm_json`；reporter 仍用文本 `_llm`
  </behavior>
  <action>
按 ORCH-03 与 RESEARCH Discretion：修正模块级 `_parse_json`——保留 `_JSON_FENCE` 围栏剥离；`json.loads` 成功且为 dict 则返回，否则 `{}`；**删除** `find("{")/rfind("}")` 跨段分支。

新增 `AgentGraph._llm_json(self, logical, system, user, retries=2) -> dict`：内部调用完整 `LLMResponse`（可复用/改造 `_llm` 为返回 response 或并行保留 `_llm` 返回 text）；每次对 `resp.text` 跑 `_parse_json`；空 dict 视为失败并重试；耗尽 emit `llm.parse_failure`。若 `finish_reason=="length"`，同步 emit `llm.truncation`（与 ORCH-04 合流，避免双路径遗漏）。

将 `node_plan`/`node_verify`（及任何 `_parse_json(self._llm(...))`）改为 `_llm_json`。禁止在 `agent/nodes/` 建文件；禁止引入新第三方 JSON 修复库。

摘除对应 xfail，使 `pytest -k parse_json` 绿。
  </action>
  <acceptance_criteria>
    - `rg -n "find\\(\"\\{\"\\)|rfind\\(\"\\}\"\\)" src/vela/agent/graph.py` 在 `_parse_json` 函数体内命中 0（跨段提取已移除）
    - `rg -n "def _llm_json|llm\\.parse_failure" src/vela/agent/graph.py` 均命中
    - `rg -n "_llm_json\\(" src/vela/agent/graph.py` ≥2（plan/verify 至少）
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k parse_json -q` 退出码 0 且无 xfail
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k parse_json -q</automated>
  </verify>
  <done>JSON 解析契约加固完成，parse_failure 可观测。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: max_tokens=2048 与 truncation 告警（ORCH-04）</name>
  <files>config/llm.yaml, src/vela/agent/graph.py, tests/test_gateway.py, tests/test_agent.py</files>
  <read_first>
    - config/llm.yaml（logical_models.planner/verifier 当前 1024/768）
    - src/vela/gateway/base.py（LLMResponse.finish_reason；chat 成功返回路径——Gateway 无 EventBus）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（A5：截断告警优先在 graph 包装层；reporter 保持 2048）
    - tests/test_gateway.py（Wave 0 max_tokens / truncation 测）
  </read_first>
  <behavior>
    - planner.max_tokens == 2048；verifier.max_tokens == 2048；reporter.max_tokens 仍为 2048（不改为 4096）
    - finish_reason == "length" → metrics 键 `llm.truncation` 递增且存在 `llm.truncation` 事件
  </behavior>
  <action>
按 ORCH-04：仅将 `config/llm.yaml` 中 `logical_models.planner` 与 `logical_models.verifier` 的 `max_tokens` 改为 `2048`；distiller/reporter 不顺带扩容（reporter 已是 2048）。

在 `graph._llm` / `_llm_json` 成功路径检测 `resp.finish_reason == "length"` → `self.bus.emit("llm.truncation", Severity.ALERT, ...)` + `self.metrics.inc("llm.truncation")`。不要给 Gateway 强行挂 EventBus（RESEARCH A5）。注意 `load_yaml` 有 lru_cache——测试进程内需确保读到新配置（新进程 pytest 即可）。

摘除 max_tokens/truncation 相关 xfail 并转绿。
  </action>
  <acceptance_criteria>
    - `rg -n "planner:.*max_tokens: 2048|verifier:.*max_tokens: 2048" config/llm.yaml` 或等价 YAML 结构断言为 2048
    - reporter 块仍为 `max_tokens: 2048` 且无 4096
    - `rg -n "llm\\.truncation" src/vela/agent/graph.py` 命中
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_gateway.py tests/test_agent.py -k 'max_tokens or truncation' -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_gateway.py tests/test_agent.py -k 'max_tokens or truncation' -q</automated>
  </verify>
  <done>ORCH-04 token/截断契约生效。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 文本输出 → 控制流 JSON | 不可信模型输出经 `_parse_json`/`_llm_json` 进入编排 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | `_parse_json` / `_llm_json` | mitigate | 禁止跨段花括号假成功；失败重试 2 次 + `llm.parse_failure` ALERT；空 dict 不驱动假动作 |
| T-03-02b | Denial of Service | max_tokens / truncation | mitigate | max_tokens=2048；`finish_reason==length` → `llm.truncation` 可观测，避免静默截断 |
| T-03-SC | Tampering | pip installs | accept | 无新依赖 |
</threat_model>

<verification>
`pytest -k 'parse_json or max_tokens or truncation'` 绿；`make test-fast` 绿。
</verification>

<success_criteria>
ORCH-03/04 行为可单测证明；真实 parse/trunc 事件可被后续 eval 聚合消费。
</success_criteria>

<output>
Create `.planning/phases/03-orch-logic-hemostasis/03-02-SUMMARY.md` when done
</output>
