---
phase: 03-orch-logic-hemostasis
plan: 03
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - src/vela/agent/state.py
  - src/vela/agent/skills.py
  - src/vela/agent/graph.py
  - config/skills/builtin.yaml
  - tests/test_agent.py
autonomous: true
requirements:
  - ORCH-07
  - ORCH-10

must_haves:
  truths:
    - "ORCH-07：excluded_skills 仅含 unproductive；round1 选中且 productive 的技能 ∉ round2 excluded；同 (skill_id,args_hash) 探针去重"
    - "ORCH-10（RESEARCH A1）：SK-GENERIC-EVIDENCE-FIRST 存在且 fallback_only=true；常规 retrieve 排除 fallback_only；注入谓词=全零分 AND 存在 ERROR 级信号（非字面「仅全零分」）；健康包无 ERROR 不注入"
  artifacts:
    - path: "src/vela/agent/state.py"
      provides: "unproductive-only excluded_skills + executed_probes"
      contains: "executed_probes"
    - path: "config/skills/builtin.yaml"
      provides: "SK-GENERIC-EVIDENCE-FIRST"
      contains: "fallback_only"
    - path: "src/vela/agent/skills.py"
      provides: "retrieve 排除 fallback_only + 注入辅助"
      contains: "fallback_only"
  key_links:
    - from: "node_plan actions"
      to: "executed_probes"
      via: "(skill_id, args_hash) 过滤重复探针"
      pattern: "executed_probes|args_hash"
    - from: "retrieve 零分"
      to: "SK-GENERIC-EVIDENCE-FIRST"
      via: "注入条件 A1：全零分 AND 存在 ERROR 信号"
      pattern: "SK-GENERIC-EVIDENCE-FIRST|fallback_only"
---

<objective>
落地 ORCH-07/ORCH-10：技能剔除回归 unproductive-only + 探针级去重，并引入全零分兜底技能 SK-GENERIC-EVIDENCE-FIRST。

Purpose: 打破「正确技能被 used∪unproductive 锁死」与「候选全零分手停」两类逻辑必然失效。
Output: state/skills/builtin/graph 改动；相关 Wave 0 测试转绿；技能数断言 12→13。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md
@.planning/phases/03-orch-logic-hemostasis/03-PATTERNS.md
@AGENTS.md
@src/vela/agent/state.py
@src/vela/agent/skills.py
@config/skills/builtin.yaml
@src/vela/util/jsonl.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: unproductive-only 剔除 + 探针去重（ORCH-07）</name>
  <files>src/vela/agent/state.py, src/vela/agent/graph.py, tests/test_agent.py</files>
  <read_first>
    - src/vela/agent/state.py（`excluded_skills` L65-73；status 注释；dataclass 字段）
    - src/vela/agent/graph.py（`node_plan` 中 excluded/illegal_skill；`node_retrieve` 执行 actions）
    - src/vela/util/jsonl.py（`canonical_json`）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（ORCH-07 Code Example；args_hash Discretion）
    - tests/test_agent.py（旧 `test_excluded_skills_includes_used_and_unproductive`；Wave 0 probe_dedup）
  </read_first>
  <behavior>
    - used=["SK-A"], unproductive=["SK-B"] → excluded_skills() == ["SK-B"]（不含 SK-A）
    - 同 skill_id 不同 args → 允许再次执行；相同 args → 过滤且不重复计入预算浪费路径
    - SessionState 新增 `executed_probes: list[str]`，键格式 `{skill_id}:{args_hash}`，`args_hash = blake2b(canonical_json(args), digest_size=8).hexdigest()`
  </behavior>
  <action>
按 ORCH-07：将 `SessionState.excluded_skills` 改为 `return sorted(set(self.unproductive_skills))`。新增字段 `executed_probes: list[str] = field(default_factory=list)`。

在 graph 侧（`node_plan` 产出 actions 后或 `node_retrieve` 执行前）过滤已在 `executed_probes` 中的 `(skill_id, args)`；执行成功后追加键。辅助函数可用模块级 `_args_hash(args: dict) -> str`，复用 `hashlib.blake2b` + `canonical_json`，禁止手写另一套 canonical。

删除或改写旧测 `test_excluded_skills_includes_used_and_unproductive`；摘除 Wave 0 xfail，使 `-k 'excluded_skills or probe_dedup'` 绿。更新任何依赖「used 即剔除」的 mock/注释（skills.py 模块 docstring 若仍写 used∪unproductive 须同步）。

禁止改 verifier/report 语义（属后续 plan）。
  </action>
  <acceptance_criteria>
    - `SessionState(...); used=["SK-A"]; unproductive=["SK-B"]; excluded_skills() == ["SK-B"]`
    - `rg -n "used_skills\\)\\s*\\|" src/vela/agent/state.py` 在 excluded_skills 中命中 0
    - `rg -n "executed_probes|blake2b|canonical_json" src/vela/agent/` 命中探针去重路径
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k 'excluded_skills or probe_dedup' -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k 'excluded_skills or probe_dedup' -q</automated>
  </verify>
  <done>ORCH-07 剔除与探针去重单测通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SK-GENERIC-EVIDENCE-FIRST 与零分注入（ORCH-10）</name>
  <files>config/skills/builtin.yaml, src/vela/agent/skills.py, src/vela/agent/graph.py, tests/test_agent.py</files>
  <read_first>
    - config/skills/builtin.yaml（SK-PHASE-OVERVIEW 条目形状；load_skills 保留未知键）
    - src/vela/agent/skills.py（`retrieve` pool 过滤 L72-75；`probes_of`）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（Pitfall 5 健康包注入谓词 A1；ORCH-10）
    - tests/test_agent.py（`test_skill_registry_loads_all_12` → 改为 13；Wave 0 generic_fallback）
  </read_first>
  <behavior>
    - builtin 含 id=SK-GENERIC-EVIDENCE-FIRST，`fallback_only: true`，`root_cause_label: null`，keywords 可空/宽，probes 含通用 search_logs/top_templates
    - retrieve 默认 pool 排除 `fallback_only` 为 true 的技能
    - **A1 注入谓词（锁定）：** 候选全零分（或等价「无有效候选分」）**AND** 会话/鸟瞰存在 ERROR 级信号 → 注入 GENERIC probes；全零分但无 ERROR（健康包）→ **不**注入；有 ERROR 但非全零分 → 不因 A1 注入
  </behavior>
  <action>
按 ORCH-10 + RESEARCH A1（相对 REQUIREMENTS「全零分即注入」字面收窄为健康特异性保护）：在 `builtin.yaml` 追加 GENERIC 技能。在 `SkillRegistry.retrieve` 于 exclude 过滤后增加 `if s.get("fallback_only"): continue`（或等价）。提供 `fallback_skill_id()` 常量/`probes_of("SK-GENERIC-EVIDENCE-FIRST")` 供 graph 注入。

在 `node_plan`（retrieve 候选之后）检测全零分/空有效候选：仅当同时存在 ERROR 信号（鸟瞰 aggregate/level 分布或已有 evidence 含 ERROR）才注入 GENERIC actions；健康无 ERROR 不注入。技能计数测试改为 13。

摘除 generic_fallback xfail 并转绿；单测须覆盖 A1 正例（零分+ERROR→注入）与反例（零分无 ERROR→不注入）。
  </action>
  <acceptance_criteria>
    - `rg -n "SK-GENERIC-EVIDENCE-FIRST|fallback_only" config/skills/builtin.yaml` 命中
    - `rg -n "fallback_only" src/vela/agent/skills.py` 命中
    - 常规 retrieve 结果 id 集合不含 SK-GENERIC-EVIDENCE-FIRST
    - `test_generic_fallback_zero_score_inject`（或同 -k）断言 A1：全零分+ERROR→注入；全零分无 ERROR→不注入
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k 'generic_fallback or loads_all' -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k 'generic_fallback or loads_all or skill_registry' -q</automated>
  </verify>
  <done>ORCH-10 兜底技能可注入且不污染常规召回。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 技能 YAML → 检索/执行 | 配置驱动技能进入探针调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01a | Elevation of Privilege | skill exclusion | mitigate | 程序化 unproductive-only；非法已剔除技能仍走 `plan.illegal_skill` |
| T-03-10 | Denial of Service | GENERIC 注入 | mitigate | 仅全零分且存在 ERROR 信号时注入，避免健康包无意义下钻（A1） |
| T-03-SC | Tampering | pip installs | accept | 无新依赖 |
</threat_model>

<verification>
`pytest -k 'excluded_skills or probe_dedup or generic_fallback'` 绿；`make test-fast` 绿。
</verification>

<success_criteria>
正确技能不会仅因「已用」被剔除；零分故障包可经 GENERIC 继续取证。
</success_criteria>

<output>
Create `.planning/phases/03-orch-logic-hemostasis/03-03-SUMMARY.md` when done
</output>
