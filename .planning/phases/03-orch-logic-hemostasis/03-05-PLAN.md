---
phase: 03-orch-logic-hemostasis
plan: 05
type: execute
wave: 4
depends_on:
  - "03-04"
files_modified:
  - src/vela/agent/graph.py
  - src/vela/gateway/prompts.py
  - src/vela/gateway/mock.py
  - tests/test_agent.py
autonomous: true
requirements:
  - ORCH-05
  - ORCH-06

must_haves:
  truths:
    - "ORCH-05：verdict status 经 _norm_status 枚举匹配；supported 或 ≥2 partial 可 decisive；大小写/连字符变体不再落空"
    - "ORCH-06：claims 为根因假设（技能 label/title 语义）而非 raw_line 自证循环；单假设可挂多条 citations"
    - "verdict_supported_ratio 在归一化后可提升（目标 ≥0.6，验收侧）"
  artifacts:
    - path: "src/vela/agent/graph.py"
      provides: "_norm_status + 假设 claims + decisive 放宽"
      contains: "_norm_status"
    - path: "src/vela/gateway/prompts.py"
      provides: "VERIFIER_SYSTEM 含 partial 枚举与假设语义"
      contains: "partial"
  key_links:
    - from: "node_verify claims"
      to: "verifier_user / mock._verify"
      via: "claim 字段语义对齐根因假设"
      pattern: "claim"
    - from: "verdicts.status"
      to: "decisive"
      via: "_norm_status + partial 计数"
      pattern: "partial|_norm_status|decisive"
---

<objective>
落地 ORCH-05/ORCH-06：verifier 判据归一化与 partial 可推进，并将 claim 重构为根因假设以消除自证循环。

Purpose: 修复基线 verdict_supported_ratio≈0.37 的脆弱精确匹配与「判断日志是否支撑它自己」的循环论证。
Output: `_norm_status`、新 claims 构造、VERIFIER_SYSTEM/mock 兼容、相关测试转绿。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md
@.planning/phases/03-orch-logic-hemostasis/03-PATTERNS.md
@src/vela/agent/graph.py
@src/vela/gateway/prompts.py
@src/vela/gateway/mock.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: claim 改为根因假设（ORCH-06）</name>
  <files>src/vela/agent/graph.py, src/vela/gateway/prompts.py, tests/test_agent.py</files>
  <read_first>
    - src/vela/agent/graph.py（`node_verify` claims 构造 L218-220；skills.label_of）
    - src/vela/gateway/prompts.py（VERIFIER_SYSTEM）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（ORCH-06；claims 上限 5 Discretion）
    - tests/test_agent.py（Wave 0 verify_claim_hypothesis）
  </read_first>
  <behavior>
    - 默认构造 1 条（或少量）根因假设 claim：文本来自技能 `root_cause_label`/`title`/`summary` 组合，而非单行 raw_line
    - citations 为支撑该假设的多个 evidence row_hash（上限配合 max_tokens：claims≤5）
    - claims[0].claim != 任一证据 raw_line 全文（消除自证）
  </behavior>
  <action>
按 ORCH-06：重写 `node_verify` 的 claims 列表构造。更新 `VERIFIER_SYSTEM` 说明：判断「根因假设是否被引用证据支撑」，枚举预留 partial（Task 2 完成归一化）。保持 verifier_user 入参形状兼容。

摘除 verify_claim_hypothesis xfail 并转绿。
  </action>
  <acceptance_criteria>
    - `rg -n "raw_line" src/vela/agent/graph.py` 在 claims 构造处不再把 raw_line 直接赋给 claim 字段（允许 citations 来自证据）
    - 单测：`claims[0]["claim"]` 含假设语义且不等于证据 raw_line
    - claims 长度 ≤5
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k verify_claim_hypothesis -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k verify_claim_hypothesis -q</automated>
  </verify>
  <done>ORCH-06 自证循环消除。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verdict 归一化与 partial 推进（ORCH-05）</name>
  <files>src/vela/agent/graph.py, src/vela/gateway/prompts.py, src/vela/gateway/mock.py, tests/test_agent.py</files>
  <read_first>
    - src/vela/agent/graph.py（decisive 计算 L237-239）
    - src/vela/gateway/mock.py（`_verify` status 赋值 L121-135）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（ORCH-05 Code Example；decisive = supported 或 ≥2 partial）
    - tests/test_agent.py（Wave 0 verdict_norm）
  </read_first>
  <behavior>
    - `_norm_status("Supported") == "supported"`；`"partially-supported"` / `"partially_supported"` / `"supported with caveats"` 归一后落入 partial 集合
    - decisive = (bool(supported) or len(partial) ≥ 2) and has_error_evidence and skill_id and label_of(skill_id)
    - mock 弱支撑可输出 `partial`（或归一化可识别的变体），避免 mock 评测与真实路径分叉
  </behavior>
  <action>
按 ORCH-05：在 graph 增加 `_norm_status` 与 `_OK`/`partial` 集合（见 RESEARCH）。替换 `status == "supported"` 精确匹配。更新 `VERIFIER_SYSTEM` JSON 枚举为 `supported|partial|weak|unsupported`（及文档说明变体会被归一）。调整 `mock._verify`：原 `weak` 路径改为 `partial`（或同时映射）。verify.done 事件 payload 的 supported 计数按归一化后的 supported 计（partial 不计入 supported 分子，但参与 decisive）。

摘除 verdict_norm xfail 并转绿。
  </action>
  <acceptance_criteria>
    - `rg -n "def _norm_status|partially_supported|decisive" src/vela/agent/graph.py` 命中
    - `rg -n 'status == "supported"' src/vela/agent/graph.py` 在 decisive 路径命中 0（改用归一化）
    - mock 可产生 partial 或测试直接注入变体 status 验证 decisive
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k verdict_norm -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k 'verdict_norm or verify_claim' -q</automated>
  </verify>
  <done>ORCH-05 判据归一化与 partial 推进生效。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verifier JSON → decisive 控制流 | 模型 status 字符串不可信，必须归一化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | verdict status | mitigate | `_norm_status` 枚举匹配；未知 status 不视为 supported |
| T-03-03b | Spoofing | claim 自证 | mitigate | claim=根因假设 + 多 citations；禁止 raw_line 自指循环 |
| T-03-SC | Tampering | pip installs | accept | 无新依赖 |
</threat_model>

<verification>
`pytest -k 'verdict_norm or verify_claim_hypothesis'` 绿；`make test-fast` 绿。
</verification>

<success_criteria>
脆弱精确匹配与自证循环消除；partial 可推进流程。
</success_criteria>

<output>
Create `.planning/phases/03-orch-logic-hemostasis/03-05-SUMMARY.md` when done
</output>
