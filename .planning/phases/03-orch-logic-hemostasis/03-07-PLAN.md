---
phase: 03-orch-logic-hemostasis
plan: 07
type: execute
wave: 6
depends_on:
  - "03-06"
files_modified:
  - .planning/phases/03-orch-logic-hemostasis/03-GATE-RESULTS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
  - ORCH-05
  - ORCH-06
  - ORCH-07
  - ORCH-08
  - ORCH-09
  - ORCH-10

must_haves:
  truths:
    - "make test 全量通过（含既有套件与 ORCH 单测）"
    - "mock make eval 仿真已通过用例回归数 = 0（一票否决）"
    - "ORCH-01..ORCH-10 在 REQUIREMENTS.md 勾选为完成（本 plan 实测后统一勾选）"
    - "不引用 44.4% 作为本阶段效果对比；过程指标对照 Phase 2 基线方法论"
  artifacts:
    - path: ".planning/phases/03-orch-logic-hemostasis/03-GATE-RESULTS.md"
      provides: "回归门实测记录"
      contains: "regression"
  key_links:
    - from: "make test / make eval"
      to: "REQUIREMENTS ORCH 勾选"
      via: "GATE-RESULTS 先落盘再勾选"
      pattern: "ORCH-0"
---

<objective>
执行 Phase 3 回归门：全量测试 + mock 仿真评测回归数 0，并在通过后统一勾选 ORCH-01..10。

Purpose: 编排层止血不得破坏既有 mock 正确性与仿真回归门；一票否决防假绿。
Output: `03-GATE-RESULTS.md` + REQUIREMENTS 勾选更新。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md
@.planning/phases/02-metrics-baseline/baseline/eval_report.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全量测试与 mock eval 回归门</name>
  <files>.planning/phases/03-orch-logic-hemostasis/03-GATE-RESULTS.md</files>
  <read_first>
    - .planning/ROADMAP.md（Phase 3 回归门）
    - .planning/phases/03-orch-logic-hemostasis/03-VALIDATION.md（Phase gate）
    - Makefile（`test` / `eval` 目标）
    - .planning/phases/02-metrics-baseline/02-06-PLAN.md 或既有 GATE-RESULTS 格式（若存在）作为落盘模板参考
  </read_first>
  <action>
运行 `make test`（PYTHON=.venv/bin/python3 约定）。记录 passed/failed 数量。

运行 mock 供应商下的仿真评测（`make eval` 或等价 `VELA_LLM_PROVIDER=mock` 的 eval run），对比既有已通过用例集合，确认回归数 = 0。将命令、退出码、关键数字写入 `03-GATE-RESULTS.md`（脱敏、无密钥）。

若失败：不得勾选 REQUIREMENTS；修复属回退到对应 ORCH plan 缺陷，本任务重跑直至绿。

可选（非阻断）：有凭证时用 `--no-cache` 抽检真实过程指标对照 ROADMAP 阈值，结果另节记录，失败不否决 mock 合入（RESEARCH Open Q3）。
  </action>
  <acceptance_criteria>
    - `03-GATE-RESULTS.md` 存在且含 `make test` 退出码 0 与用例计数
    - 文件显式记录 mock eval「regression = 0」或等价「已通过用例回归数 = 0」
    - 正文不出现将 44.4% 作为本阶段对比锚的表述
  </acceptance_criteria>
  <verify>
    <automated>make test</automated>
  </verify>
  <done>回归门实测绿并落盘 GATE-RESULTS。</done>
</task>

<task type="auto">
  <name>Task 2: 勾选 ORCH-01..10 并核对过程指标观测点</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/03-orch-logic-hemostasis/03-GATE-RESULTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md（ORCH-01..10 复选框）
    - .planning/phases/03-orch-logic-hemostasis/03-GATE-RESULTS.md（Task 1 结果）
    - tests/ 中 ORCH -k 覆盖（确认无残留 strict xfail）
  </read_first>
  <action>
仅当 Task 1 回归门通过：将 REQUIREMENTS.md 中 ORCH-01..ORCH-10 全部勾选为 `[x]`。在 GATE-RESULTS 追加核对表：各 ORCH 对应 `-k` 单测无 xfail、关键事件名（plan.stop_rejected / llm.parse_failure / llm.truncation / coverage.unexplained_errors）存在于代码路径。

禁止在未通过回归门时提前勾选。
  </action>
  <acceptance_criteria>
    - `rg -n "\\[ \\] \\*\\*ORCH-" .planning/REQUIREMENTS.md` 命中 0
    - `rg -n "\\[x\\] \\*\\*ORCH-" .planning/REQUIREMENTS.md` 命中 10
    - `rg -n "xfail.*ORCH|ORCH pending" tests/test_agent.py tests/test_gateway.py tests/test_eval.py` 命中 0（无残留 pending xfail）
  </acceptance_criteria>
  <verify>
    <automated>rg -n '\[ \] \*\*ORCH-' .planning/REQUIREMENTS.md; test $? -eq 1</automated>
  </verify>
  <done>ORCH-01..10 需求追踪闭环。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/本地回归 → 需求勾选 | 勾选必须以实测为准，禁止预勾 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Spoofing | REQUIREMENTS 勾选 | mitigate | GATE-RESULTS 先落盘；回归失败禁止勾选 |
| T-03-SC | Tampering | pip installs | accept | 无新依赖 |
</threat_model>

<verification>
`make test` 绿；mock eval 回归 0；ORCH 全勾选。
</verification>

<success_criteria>
Phase 3 回归门与需求追踪闭环；可进入 verify-work / 下一阶段。
</success_criteria>

<output>
Create `.planning/phases/03-orch-logic-hemostasis/03-07-SUMMARY.md` when done
</output>
