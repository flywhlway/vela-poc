---
phase: 03-orch-logic-hemostasis
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_agent.py
  - tests/test_gateway.py
  - tests/test_eval.py
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
  - ORCH-05
  - ORCH-06
  - ORCH-07
  - ORCH-08
  - ORCH-09
  - ORCH-10

must_haves:
  truths:
    - "ORCH-01..ORCH-10 均有可收集的命名单测骨架（xfail 或 skip 标注 pending plan）"
    - "Wave 0 骨架可被 pytest 收集，且 make test-fast 不因未实现功能而红"
  artifacts:
    - path: "tests/test_agent.py"
      provides: "ORCH agent 侧单测骨架"
      contains: "stop_rejected"
    - path: "tests/test_gateway.py"
      provides: "ORCH gateway/prompt/max_tokens 骨架"
      contains: "planner_system"
    - path: "tests/test_eval.py"
      provides: "过程指标真实事件优先骨架"
      contains: "parse_failure"
  key_links:
    - from: "Wave 0 测试名"
      to: "后续 03-02..03-06 实现计划"
      via: "xfail reason 指向目标 plan；实现时摘除 xfail"
      pattern: "xfail|ORCH-"
---

<objective>
为 ORCH-01..ORCH-10 建立 Wave 0 可收集单测骨架，锁定行为契约与自动化命令，使后续实现计划可按 RED→GREEN 摘除 xfail。

Purpose: Nyquist 采样连续性——每个 ORCH 需求在实现前就有 `-k` 可定位的测试名。
Output: `tests/test_agent.py` / `test_gateway.py` / `test_eval.py` 中带 `@pytest.mark.xfail(strict=True)` 的完整断言骨架。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md
@.planning/phases/03-orch-logic-hemostasis/03-VALIDATION.md
@.planning/phases/03-orch-logic-hemostasis/03-PATTERNS.md
@AGENTS.md
@tests/test_agent.py
@tests/test_gateway.py
@tests/test_eval.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写入 ORCH agent/gateway 单测骨架（xfail）</name>
  <files>tests/test_agent.py, tests/test_gateway.py</files>
  <read_first>
    - .planning/phases/03-orch-logic-hemostasis/03-VALIDATION.md（Per-Task Verification Map 命令与 -k 关键字）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（Validation Architecture 行为表）
    - tests/test_agent.py（现有 `test_excluded_skills_*` / e2e 风格；禁止 unittest.mock）
    - tests/test_gateway.py（mock gateway / finish_reason 断言风格）
    - .planning/phases/03-orch-logic-hemostasis/03-PATTERNS.md（tests 段）
  </read_first>
  <action>
在 `tests/test_agent.py` 新增下列测试函数（完整断言体，勿留 `pass`），全部加 `@pytest.mark.xfail(strict=True, reason="ORCH pending plan 03-NN")`，reason 映射：stop_rejected→03-04；parse_json→03-02；verdict_norm/verify_claim_hypothesis→03-05；excluded_skills 改写+probe_dedup→03-03；insufficient_citation/unexplained_sweep→03-06；generic_fallback→03-03。

必须覆盖的行为（按 ORCH，断言写进测试体）：
- ORCH-01 `test_plan_stop_rejected_round1`：构造 round_no==1 且模型输出 stop=True；期望 `plan.stop_rejected` 事件与 metrics 递增，且最终 stop=False（可直接调 `node_plan` 或注入解析结果后的守卫路径）。
- ORCH-03 `test_parse_json_no_cross_span_and_retry_alert`：跨段花括号文本不得假成功为非空 dict；合法 ```json 围栏成功；解析耗尽重试后存在 `llm.parse_failure` ALERT。
- ORCH-05 `test_verdict_norm_partial_decisive`：`Supported`/`partial`/`partially_supported` 归一化后可进入 decisive 路径；仅 unsupported 不 decisive。
- ORCH-06 `test_verify_claim_hypothesis_not_raw_line_loop`：claims[0].claim 不得等于证据 raw_line 自身循环；应含技能根因假设语义。
- ORCH-07：改写 `test_excluded_skills_includes_used_and_unproductive` 为仅 unproductive（本 Wave 先 xfail 新名 `test_excluded_skills_unproductive_only`，旧测暂保留或一并标 xfail 待 03-03 删除旧断言）；新增 `test_probe_dedup_same_args_skipped`。
- ORCH-08 `test_insufficient_citation_retry_then_status`：引用数 &lt; 0.5*chain_len → 重试一次 → 仍不足则 status==insufficient_citation。
- ORCH-09 `test_unexplained_sweep_blocks_no_fault_found`：库有 ERROR、evidence_pool 无对应 row_hash → 禁止 no_fault_found，status/label 为 insufficient_coverage。
- ORCH-10 `test_generic_fallback_zero_score_inject`：fallback_only 技能不进常规 retrieve；全零分且存在 ERROR 信号时注入 SK-GENERIC-EVIDENCE-FIRST。

在 `tests/test_gateway.py`：
- ORCH-02 `test_planner_system_distinguishes_stop_vs_inconclusive`：`PLANNER_SYSTEM` 含子串约束（禁止因尚无证据而 stop / 区分停止调查与无法定论）。
- ORCH-04 `test_llm_yaml_planner_verifier_max_tokens_2048`：`load_yaml("llm.yaml")` 中 planner/verifier max_tokens==2048。
- ORCH-04 `test_truncation_finish_reason_length_counted`：finish_reason==length 路径递增 truncation 指标或事件（实现位置以 graph 包装层为准时，可先放 test_agent 同名 -k truncation）。

禁止：在 `agent/nodes/` 建文件；引入 unittest.mock；本计划不改生产代码。
  </action>
  <acceptance_criteria>
    - `rg -n "def test_.*stop_rejected|def test_.*parse_json|def test_.*verdict_norm|def test_.*verify_claim_hypothesis|def test_.*probe_dedup|def test_.*insufficient_citation|def test_.*unexplained_sweep|def test_.*generic_fallback" tests/test_agent.py` 命中 ≥8
    - `rg -n "def test_.*planner_system|def test_.*max_tokens|truncation" tests/test_gateway.py` 命中 ≥2
    - 上述新测均含 `@pytest.mark.xfail`
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py tests/test_gateway.py -q --collect-only` 退出码 0
    - `make test-fast` 退出码 0（xfail 不计失败）
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py tests/test_gateway.py -q --collect-only && make test-fast</automated>
  </verify>
  <done>ORCH agent/gateway 单测骨架可收集且 test-fast 绿（xfail）。</done>
</task>

<task type="auto">
  <name>Task 2: 写入过程指标真实事件优先骨架（xfail）</name>
  <files>tests/test_eval.py</files>
  <read_first>
    - tests/test_eval.py（`test_process_metrics_keys_and_trace` 夹具形状）
    - src/vela/eval/process.py（aggregate_process_metrics 代理口径与 PROXY_FOOTNOTE）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（Pitfall 3；过程指标真实事件优先）
  </read_first>
  <action>
新增 `test_process_metrics_prefer_real_parse_and_truncation_events`（xfail reason→03-06）：cases 同时含代理可触发的空 plan.done 与真实 `llm.parse_failure` / `llm.truncation` 事件时，聚合优先计真实事件；脚注不得再含「Phase 3 前允许偏高」。

新增 `test_ablation_excludes_insufficient_statuses_from_misdiagnosis`（xfail→03-06）：status 为 `insufficient_citation` 或 `insufficient_coverage` 的用例不计入 misdiagnosis 分子。

禁止改 process.py 生产逻辑（本 Wave 仅测骨架）。
  </action>
  <acceptance_criteria>
    - `rg -n "def test_process_metrics_prefer_real|def test_ablation_excludes_insufficient" tests/test_eval.py` 命中 2
    - 两测均 `@pytest.mark.xfail`
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_eval.py -k 'process_metrics_prefer or ablation_excludes_insufficient' -q` 退出码 0（xfail 通过）
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_eval.py -k 'process_metrics_prefer or ablation_excludes_insufficient' -q</automated>
  </verify>
  <done>eval 过程指标升级骨架就位，指向 plan 03-06。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试夹具 → 生产模块导入 | 骨架只断言公开行为，不注入恶意路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00 | Tampering | Wave 0 测试 | accept | 测试代码不进生产控制流；xfail 防止假绿掩盖缺陷 |
| T-03-SC | Tampering | pip/npm installs | accept | 本计划无包安装 |
</threat_model>

<verification>
Wave 0：所有 ORCH -k 关键字可收集；`make test-fast` 绿。
</verification>

<success_criteria>
ORCH-01..10 均有命名测试骨架；后续计划可摘除对应 xfail 并转绿。
</success_criteria>

<output>
Create `.planning/phases/03-orch-logic-hemostasis/03-01-SUMMARY.md` when done
</output>
