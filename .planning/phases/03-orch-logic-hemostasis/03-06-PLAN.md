---
phase: 03-orch-logic-hemostasis
plan: 06
type: execute
wave: 5
depends_on:
  - "03-05"
files_modified:
  - src/vela/agent/graph.py
  - src/vela/agent/state.py
  - src/vela/agent/citations.py
  - config/budget.yaml
  - src/vela/eval/process.py
  - tests/test_agent.py
  - tests/test_eval.py
autonomous: true
requirements:
  - ORCH-08
  - ORCH-09

must_haves:
  truths:
    - "ORCH-08：报告引用数 ≥ ceil(min_citation_ratio * chain_len)；不足则带修复提示重试 1 次；仍不足 status=insufficient_citation（阈值来自 budget.yaml，默认 0.5）"
    - "ORCH-09：report/unanswerable 前经 LogQueryAPI.call(search_logs) 扫描未解释 ERROR；存在未入池错误时禁止 no_fault_found，降级 insufficient_coverage，并附 sweep.samples[:10]（emit + root_cause/报告可见）"
    - "过程指标优先真实事件 llm.parse_failure / llm.truncation / plan.stop_rejected；PROXY_FOOTNOTE 去掉「Phase 3 前允许偏高」；insufficient_* 不进 misdiagnosis 分子"
  artifacts:
    - path: "src/vela/agent/graph.py"
      provides: "引用比例闸门 + _unexplained_error_sweep"
      contains: "_unexplained_error_sweep"
    - path: "config/budget.yaml"
      provides: "report.min_citation_ratio"
      contains: "min_citation_ratio"
    - path: "src/vela/eval/process.py"
      provides: "真实事件优先聚合"
      contains: "llm.parse_failure"
  key_links:
    - from: "node_report"
      to: "extract_citations / budget report.min_citation_ratio"
      via: "比例校验→重试→insufficient_citation"
      pattern: "insufficient_citation|min_citation_ratio"
    - from: "_unexplained_error_sweep"
      to: "LogQueryAPI.call"
      via: "search_logs query=\"\" min_level=ERROR；禁止 api._q"
      pattern: "api\\.call\\(\"search_logs\""
    - from: "_unexplained_error_sweep samples[:10]"
      to: "coverage.unexplained_errors emit + root_cause/报告"
      via: "降级 insufficient_coverage 时附着未解释错误行"
      pattern: "samples|coverage\\.unexplained_errors"
---

<objective>
落地 ORCH-08/ORCH-09：报告引用比例闸门与全局未解释错误哨兵，并升级过程指标为真实事件优先聚合。

Purpose: 阻止零引用伪装完成与「库中有 ERROR 却报 no_fault_found」两类诚实性失效；尺子口径与 Phase 3 事件对齐。
Output: budget 阈值、citations helper（可选）、sweep、status 新值、process.py 升级、相关测试转绿。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md
@.planning/phases/03-orch-logic-hemostasis/03-PATTERNS.md
@src/vela/agent/graph.py
@src/vela/agent/citations.py
@src/vela/eval/process.py
@src/vela/eval/runner.py
@config/budget.yaml
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 引用比例校验与 insufficient_citation（ORCH-08）</name>
  <files>config/budget.yaml, src/vela/agent/citations.py, src/vela/agent/graph.py, src/vela/agent/state.py, tests/test_agent.py</files>
  <read_first>
    - src/vela/agent/graph.py（`node_report` L251-266 dangling 处理后）
    - src/vela/agent/citations.py（`extract_citations` / `verify_citations`）
    - config/budget.yaml（`cost:` 块风格）
    - src/vela/agent/state.py（status 注释枚举）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（ORCH-08；NR-4；A3 计数口径=引用次数 vs 链长）
  </read_first>
  <behavior>
    - `budget.yaml` 含 `report.min_citation_ratio: 0.5`
    - 有效引用数（去悬空后 extract_citations 长度或 verify 通过数）&lt; ceil(ratio * len(chain)) 且 chain 非空 → 带修复提示再调 reporter 1 次
    - 仍不足 → `st.status = "insufficient_citation"`（诚实终态，非 answered）
    - 禁止硬编码 0.5 字面量于 graph（必须读配置）
  </behavior>
  <action>
按 ORCH-08：在 budget.yaml 增加 `report.min_citation_ratio`。可选在 citations.py 增加 helper（如 `citation_ratio_ok(text, chain_len, min_ratio) -> bool`），复用 `extract_citations`，勿新建解析器。扩展 `SessionState.status` 注释含 `insufficient_citation`。

在 `node_report` dangling 处理之后实施比例闸门与单次重试；失败设 status 并 emit ALERT（如 `report.insufficient_citation`）。阈值经 `load_budget`/`load_yaml("budget.yaml")` 读取。

摘除 insufficient_citation xfail 并转绿。
  </action>
  <acceptance_criteria>
    - `rg -n "min_citation_ratio" config/budget.yaml src/vela/` 命中配置与读取路径
    - `rg -n "insufficient_citation" src/vela/agent/` 命中 status 赋值
    - graph 中无裸 `0.5` 作为引用阈值（允许测试夹具）
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k insufficient_citation -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k insufficient_citation -q</automated>
  </verify>
  <done>ORCH-08 引用比例闸门生效。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 未解释错误哨兵 + 过程指标真实事件口径（ORCH-09）</name>
  <files>src/vela/agent/graph.py, src/vela/agent/state.py, src/vela/eval/process.py, tests/test_agent.py, tests/test_eval.py</files>
  <read_first>
    - src/vela/agent/graph.py（`_root_cause` no_fault_found L487-498；`node_report`/`node_unanswerable`；主循环终态）
    - src/vela/eval/runner.py（L248-255 search_logs 代理——必须抄门面，禁 `_q`）
    - src/vela/eval/process.py（aggregate_process_metrics 代理；PROXY_FOOTNOTE；ablation mis 判定）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（ORCH-09 sweep；Pitfall 3/4；Open Q1 status 语义）
    - AGENTS.md（查询唯一收口）
  </read_first>
  <behavior>
    - `_unexplained_error_sweep(st)`：`api.call("search_logs", query="", mode="substring", min_level="ERROR", limit=200)`；与 evidence_pool row_hash 差集；返回 dict 含 `samples: unexplained[:10]`（RESEARCH Code Example）；禁止 `api._q`
    - 未解释非空且将输出 no_fault_found → 改为 insufficient_coverage（status 与/或 root_cause.label）；emit `coverage.unexplained_errors`，payload **必须含非空 samples**（截断至 ≤10）；samples 同时写入 `root_cause` 附件字段与/或报告可见结构（ORCH-09「附未解释错误行」——禁止只改 status 不挂 samples）
    - process：若 cases 含 `llm.parse_failure`/`llm.truncation`/`plan.stop_rejected` 事件则优先于代理口径；脚注删除「Phase 3 前允许偏高」
    - ablation：`insufficient_citation`/`insufficient_coverage` 不计 misdiagnosis 分子
  </behavior>
  <action>
按 ORCH-09：实现 `AgentGraph._unexplained_error_sweep`（返回含 `samples[:10]`）；在 `node_report` 与通向 unanswerable/no_fault_found 的路径前调用。扩展 status 注释含 `insufficient_coverage`。降级时接线 RESEARCH samples：`bus.emit("coverage.unexplained_errors", …)` 携带 samples；`_root_cause` / 报告路径附着同一 samples（字段名可用 `unexplained_samples` 或等价，须可被测试读取）。

升级 `aggregate_process_metrics`：真实事件优先，代理降为 fallback；更新 PROXY_FOOTNOTE。升级 `aggregate_ablation_metrics` 排除 insufficient_*（RESEARCH A4）。

摘除 unexplained_sweep 与 eval 两测 xfail 并转绿；Wave 0 测须断言 samples 非空。不削弱 `gateway/redact.py`。
  </action>
  <acceptance_criteria>
    - `rg -n "_unexplained_error_sweep|api\\.call\\(\"search_logs\"" src/vela/agent/graph.py` 命中
    - `rg -n "api\\._q" src/vela/agent/graph.py` 在 sweep 路径命中 0
    - `rg -n "insufficient_coverage|coverage\\.unexplained_errors" src/vela/agent/` 命中
    - `rg -n "samples" src/vela/agent/graph.py` 在 sweep/emit/root_cause 路径命中（未解释行附着，非注释）
    - `test_unexplained_sweep_blocks_no_fault_found` 断言：status/label=insufficient_coverage **且** samples 非空（len≥1）
    - `rg -n "Phase 3 前允许偏高" src/vela/eval/process.py` 命中 0
    - `rg -n "llm\\.parse_failure|plan\\.stop_rejected" src/vela/eval/process.py` 命中
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py tests/test_eval.py -k 'unexplained_sweep or process_metrics_prefer or ablation_excludes_insufficient' -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py tests/test_eval.py -k 'unexplained_sweep or process_metrics_prefer or ablation_excludes_insufficient' -q</automated>
  </verify>
  <done>ORCH-09 哨兵与过程指标真实事件口径落地。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gold DB ERROR 行 → 会话结论 | SQL 事实经 Query Plane 约束结论诚实性 |
| Reporter 文本 → 对外报告 | 引用比例程序化校验优先于模型自述 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Spoofing | node_report 引用 | mitigate | min_citation_ratio 闸门 + 单次修复重试 + insufficient_citation |
| T-03-05 | Repudiation | no_fault_found | mitigate | `_unexplained_error_sweep` via LogQueryAPI；未解释 ERROR → insufficient_coverage + samples[:10] 附着 emit/root_cause/报告 |
| T-03-05b | Information Disclosure | outbound LLM | accept | 本阶段不削弱 redact.py；无新出站面 |
| T-03-SC | Tampering | pip installs | accept | 无新依赖 |
</threat_model>

<verification>
ORCH-08/09 与 process 真实事件测绿；`make test-fast` 绿。
</verification>

<success_criteria>
无法在存在未解释 ERROR 时输出 no_fault_found；降级时附 samples[:10]；引用不足诚实降级；过程指标口径与事件对齐。
</success_criteria>

<output>
Create `.planning/phases/03-orch-logic-hemostasis/03-06-SUMMARY.md` when done
</output>
