---
phase: 03-orch-logic-hemostasis
plan: 04
type: execute
wave: 3
depends_on:
  - "03-03"
files_modified:
  - src/vela/gateway/prompts.py
  - src/vela/agent/graph.py
  - tests/test_agent.py
  - tests/test_gateway.py
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02

must_haves:
  truths:
    - "ORCH-01：round_no==1 时模型 stop=true 被程序驳回；emit plan.stop_rejected ALERT；metrics.inc；必要时补 actions（优先候选最高分非 fallback，否则 SK-GENERIC-EVIDENCE-FIRST）"
    - "ORCH-02：PLANNER_SYSTEM 规则 5 区分「停止调查」与「无法定论」，禁止因尚无证据而首轮 stop"
    - "premature_stop_rate 具备 plan.stop_rejected 可观测性，目标 ≤0.05（验收侧）"
  artifacts:
    - path: "src/vela/agent/graph.py"
      provides: "首轮 stop 守卫"
      contains: "plan.stop_rejected"
    - path: "src/vela/gateway/prompts.py"
      provides: "重写后的 PLANNER_SYSTEM 规则 5"
      contains: "stop"
  key_links:
    - from: "node_plan 解析后 out.stop"
      to: "plan.stop_rejected"
      via: "round_no==1 程序化驳回后再进入主循环 stop 分支"
      pattern: "stop_rejected"
    - from: "PLANNER_SYSTEM"
      to: "真实 LLM 行为引导"
      via: "提示词双层防御；mock 不读正文故单测必须打守卫"
      pattern: "PLANNER_SYSTEM"
---

<objective>
落地 ORCH-01/ORCH-02：首轮禁止 stop 的程序化守卫 + 重写 planner 提示词第 5 条，消除首轮误停诱导。

Purpose: mock 测不出的 D1 缺陷必须用代码硬拦；提示词与守卫双层防御。
Output: `node_plan` 守卫、`PLANNER_SYSTEM` 新规则 5、stop_rejected / planner_system 测试转绿。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md
@.planning/phases/03-orch-logic-hemostasis/03-PATTERNS.md
@src/vela/agent/graph.py
@src/vela/gateway/prompts.py
@config/skills/builtin.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重写 PLANNER_SYSTEM 规则 5（ORCH-02）</name>
  <files>src/vela/gateway/prompts.py, tests/test_gateway.py</files>
  <read_first>
    - src/vela/gateway/prompts.py（PLANNER_SYSTEM 现行规则 5：「证据不足时输出 stop=true…」）
    - .planning/REQUIREMENTS.md（ORCH-02 原文）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（ORCH-02）
    - tests/test_gateway.py（Wave 0 planner_system 测）
  </read_first>
  <behavior>
    - PLANNER_SYSTEM 不再包含诱导首轮空证据池必然 stop 的旧规则 5 原文
    - 新规则明确：首轮/尚无证据时必须继续调查（选技能并给 actions）；stop=true 仅表示「停止调查」且需已有充分探索；「无法定论」不得用 stop 偷换
  </behavior>
  <action>
按 ORCH-02 重写 `PLANNER_SYSTEM` 第 5 条（及必要的相邻措辞），使契约测试子串可稳定匹配。保持 JSON 输出 schema 字段不变（thought/selected_skill/actions/stop/reason），避免破坏 mock。提示词变更会使 LLM 磁盘缓存因 prompt_sha 失效——预期行为，勿回退。

摘除 planner_system xfail 并转绿。
  </action>
  <acceptance_criteria>
    - `rg -n "证据不足时输出 stop=true" src/vela/gateway/prompts.py` 命中 0
    - `PLANNER_SYSTEM` 同时体现「禁止因尚无证据而 stop」与「停止调查 ≠ 无法定论」语义（中文或等价关键词由测试子串钉死）
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_gateway.py -k planner_system -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_gateway.py -k planner_system -q</automated>
  </verify>
  <done>ORCH-02 提示词契约已重写并通过快照/子串测。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 首轮 stop 程序化守卫（ORCH-01）</name>
  <files>src/vela/agent/graph.py, tests/test_agent.py</files>
  <read_first>
    - src/vela/agent/graph.py（`node_plan` 解析后非法技能守卫 L154-160 模式；主循环 stop 分支 L329-338）
    - .planning/phases/03-orch-logic-hemostasis/03-RESEARCH.md（ORCH-01 Code Example；Open Q2 驳回后选技能优先级）
    - .planning/phases/03-orch-logic-hemostasis/03-PATTERNS.md（Guard pattern plan.illegal_skill 镜像结构）
    - tests/test_agent.py（Wave 0 stop_rejected）
  </read_first>
  <behavior>
    - st.round_no == 1 且 out["stop"] is True → emit `plan.stop_rejected`（Severity.ALERT）+ metrics.inc；out["stop"]=False
    - 若 actions 为空：selected_skill 若有效则填其 probes；否则取候选最高分非 fallback；再否则 `probes_of("SK-GENERIC-EVIDENCE-FIRST")`
    - round_no &gt; 1 时允许 stop=true 通过（本守卫不拦截）
  </behavior>
  <action>
按 ORCH-01：在 `node_plan` 内、写出 plan.done 之前，镜像 `plan.illegal_skill` 结构实现首轮 stop 驳回。强制 actions 非空策略遵循 RESEARCH Open Q2（与 ORCH-10 GENERIC 合流）。守卫必须在主循环消费 `plan["stop"]` 之前完成，避免假 stop 直接进 report/unanswerable。

单测直接驱动守卫逻辑（mock 不读提示词）。摘除 stop_rejected xfail。
  </action>
  <acceptance_criteria>
    - `rg -n "plan\\.stop_rejected" src/vela/agent/graph.py` 命中 emit 与 inc
    - round1 stop 被驳回后 `out["stop"] is False` 且 actions 非空（有可用技能/GENERIC 时）
    - round2+ stop=true 不被本守卫强制改 False（有单测或断言分支）
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k stop_rejected -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k stop_rejected -q</automated>
  </verify>
  <done>ORCH-01 首轮误停硬拦生效并可计量。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模型 stop 标志 → 会话终态 | 不可信控制流标志必须经程序守卫 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | node_plan stop | mitigate | round_no==1 强制驳回 stop + 补 actions；计数 `plan.stop_rejected` |
| T-03-01b | Tampering | PLANNER_SYSTEM | mitigate | ORCH-02 消除「证据不足→stop」诱导；与硬拦双层防御 |
| T-03-SC | Tampering | pip installs | accept | 无新依赖 |
</threat_model>

<verification>
`pytest -k 'stop_rejected or planner_system'` 绿；`make test-fast` 绿。
</verification>

<success_criteria>
首轮无法通过模型输出直接 stop 结束会话；提示词不再诱导该行为。
</success_criteria>

<output>
Create `.planning/phases/03-orch-logic-hemostasis/03-04-SUMMARY.md` when done
</output>
