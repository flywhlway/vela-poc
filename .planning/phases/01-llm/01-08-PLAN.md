---
phase: 01-llm
plan: 08
type: execute
wave: 5
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05", "01-06", "01-07"]
files_modified: []
autonomous: false
requirements: [ENV-01, ENV-02, ENV-03, ENV-04]

must_haves:
  truths:
    - "现有 177 个测试全部通过（make test 退出码 0，failed = 0，passed 不低于基线）"
    - "仿真基准已通过用例回归数 = 0：make eval 的正确场景集合是 01-BASELINE.md 基线集合的超集"
    - "run_all.sh 第 1 步的 doctor 在 set -euo pipefail 下不中断整条链路"
    - "make demo 在 mock provider 下仍可离线跑通"
    - "真实凭证下 vela doctor 四项连通性给出结论，且输出中 API key 为掩码形态"
    - "真实凭证下 pytest -m realllm 通过，报告含 [[EV:row_hash]] 引用"
    - "仓库中没有任何被追踪的凭证文件"
  artifacts: []
  key_links:
    - from: ".planning/phases/01-llm/01-BASELINE.md"
      to: "make eval 的本次结果"
      via: "正确场景 ID 集合比对，回归数必须为 0"
      pattern: "baseline_eval_correct_case_ids"
    - from: "run_all.sh 第 1 步 doctor"
      to: "set -euo pipefail"
      via: "D-14 退出码分层保证不中断"
      pattern: "doctor"
---

<objective>
执行 Phase 1 的一票否决回归门与 ENV-02 的真实实测验收：确认现有测试全部通过、仿真基准已通过用例回归数为 0、`run_all.sh` 与 `make demo` 两条演示链路不因 doctor 联网策略而中断；然后在真实火山引擎凭证下由人工确认 `vela doctor` 与 `pytest -m realllm` 的实际表现。

Purpose: 回归门是 ROADMAP 明确的一票否决项，必须作为独立收尾任务执行，不能作为某个功能任务的附带断言。ENV-02 的最终验收需要真实凭证与网络，且会产生付费调用，因此必须由人工介入确认——这也是 D-12「切到 volcengine 后 doctor 开始联网」这一已知后果的最后一道人工核验。

Output: 回归门结论 + 真实实测确认，无源码改动。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-llm/01-CONTEXT.md
@.planning/phases/01-llm/01-BASELINE.md
@.planning/phases/01-llm/01-01-SUMMARY.md
@.planning/phases/01-llm/01-03-SUMMARY.md
@.planning/phases/01-llm/01-04-SUMMARY.md
@.planning/phases/01-llm/01-06-SUMMARY.md
@.planning/phases/01-llm/01-07-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 执行一票否决回归门（测试全过 + 仿真基准回归数 = 0 + 演示链路不中断）</name>
  <files>（无文件改动，纯验证任务）</files>
  <read_first>
    - .planning/phases/01-llm/01-BASELINE.md（Plan 01 采集的三项基线：`baseline_interpreter` / `baseline_pytest_passed` / `baseline_eval_exit_code` / `baseline_eval_correct_case_ids`）
    - .planning/ROADMAP.md 第 33-35 行（回归门原文：现有 177 个测试全部通过 + 仿真基准已通过用例回归数 = 0，一票否决）
    - AGENTS.md §完成判据（`make test-fast` 通过；涉及建库/查询/推理链路的改动须 `make test` 全量通过；行为变化须同步 `config/*.yaml` 与相关文档）
    - Makefile 第 41-42 行 `doctor`、第 63-64 行 `demo`、第 66-68 行 `eval`、第 70-75 行 `test` / `test-fast`、第 85-88 行 `lint`
    - run_all.sh 第 1-40 行（`set -euo pipefail` + 六步链路，第 1 步是 doctor）
    - src/vela/cli.py 第 121-140 行 `cmd_eval`（四项门与 `eval_result.json` 结构）
  </read_first>
  <action>
在**未设置真实凭证的默认 mock 环境**下逐项执行回归门（若本机 `.env` 已切到 volcengine，用 shell 前置赋值 `VELA_LLM_PROVIDER=mock` 覆盖，勿修改 `.env` 文件本身）。全部命令用 Plan 01 确立的解释器约定：make 目标传 `PYTHON=.venv/bin/python3`，直接调 pytest 用 `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest`。

1. **语法门**：`PYTHON=.venv/bin/python3 make lint`，退出码必须为 0。
2. **快速门**：`PYTHON=.venv/bin/python3 make test-fast`，退出码必须为 0。
3. **全量测试门**：`PYTHON=.venv/bin/python3 make test`。判定标准：退出码 0；`failed` 为 0；`passed` **不低于** `01-BASELINE.md` 的 `baseline_pytest_passed`（本阶段新增了 dotenv / envcheck / doctor / gateway 若干用例，passed 应严格大于基线）；输出中含 `deselected`（`realllm` 被默认排除，证明零付费调用）。
4. **仿真基准回归门**：`VELA_LLM_PROVIDER=mock PYTHON=.venv/bin/python3 make eval`。从产出的 `eval_result.json` 取本次「判定为正确的场景 ID 集合」，与 `01-BASELINE.md` 的 `baseline_eval_correct_case_ids` 比对：
   - **回归数 = |基线集合 − 本次集合|，必须为 0**（基线中通过的场景本次不得有任何一个变为不通过）。本次集合可以是基线的真超集（新通过的场景不算回归）。
   - `make eval` 的退出码应与 `baseline_eval_exit_code` 一致或更好（基线为 0 则本次必须为 0）。
   - 若回归数 > 0，**立即停止并报告具体劣化的场景 ID**，不要尝试通过调整阈值或配置「修好」它——这是一票否决项，需要回到对应 Plan 定位根因。
5. **演示链路门（D-14 消费方验证）**：
   - `VELA_LLM_PROVIDER=mock ./run_all.sh --skip-eval --skip-tests`，退出码必须为 0；确认第 1 步 doctor 在 `set -euo pipefail` 下未中断链路。
   - `PYTHON=.venv/bin/python3 make demo`，退出码必须为 0（`make demo` 在 mock 下必须仍可离线跑通，D-02 本地优先）。
6. **凭证卫生门**：`git status --porcelain` 中不得出现 `.env`；`git ls-files | grep -c '^\.env$'` 必须为 0；对本阶段全部改动文件跑一次凭证形态扫描 `git diff --name-only HEAD~N`（N = 本阶段提交数）后逐文件 `grep -nE 'sk-[A-Za-z0-9]{8}|ep-2[0-9]{7}'`，必须无命中。

把六项结果（命令、退出码、关键数值、基线对比）逐项写进 SUMMARY。任一项不达标即判本阶段未完成。
  </action>
  <acceptance_criteria>
    - `PYTHON=.venv/bin/python3 make lint` 退出码 0
    - `PYTHON=.venv/bin/python3 make test-fast` 退出码 0
    - `PYTHON=.venv/bin/python3 make test` 退出码 0，`failed` 为 0，`passed` ≥ `baseline_pytest_passed`，输出含 `deselected`
    - `VELA_LLM_PROVIDER=mock PYTHON=.venv/bin/python3 make eval` 退出码 0，且基线正确场景集合 − 本次正确场景集合 = 空集（回归数 0）
    - `VELA_LLM_PROVIDER=mock ./run_all.sh --skip-eval --skip-tests` 退出码 0
    - `PYTHON=.venv/bin/python3 make demo` 退出码 0
    - `git ls-files | grep -c '^\.env$'` 为 0；`git status --porcelain .env` 无输出
    - 本阶段改动的全部文件中 `grep -nE 'sk-[A-Za-z0-9]{8}|ep-2[0-9]{7}'` 无命中
  </acceptance_criteria>
  <verify>
    <automated>PYTHON=.venv/bin/python3 make lint && PYTHON=.venv/bin/python3 make test-fast && VELA_LLM_PROVIDER=mock PYTHON=.venv/bin/python3 make test && test $(git ls-files | grep -c '^\.env$') -eq 0 && test -z "$(git status --porcelain .env)" && VELA_LLM_PROVIDER=mock PYTHON=.venv/bin/python3 make demo && VELA_LLM_PROVIDER=mock ./run_all.sh --skip-eval --skip-tests</automated>
  </verify>
  <done>六项回归门全部通过：语法 / 快速 / 全量测试 / 仿真基准回归数 0 / 两条演示链路 / 凭证卫生；结果已逐项记入 SUMMARY。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: ENV-02 / ENV-03 真实火山引擎环境实测验收</name>
  <files>（无文件改动，人工验收）</files>
  <read_first>
    - .planning/phases/01-llm/01-07-SUMMARY.md（`realllm` 用例的完整运行命令）
    - .planning/phases/01-llm/01-06-SUMMARY.md（`vela doctor --json` 的顶层键清单）
    - .planning/ROADMAP.md 第 29-32 行（四条成功判据原文）
    - docs/LLM_PRODUCTION.md（Plan 07 对齐后的接入文档，含 doctor 与 realllm 两节）
  </read_first>
  <action>
暂停执行并等待人工核验：本任务不产生任何文件改动。执行方需先确认 Task 1 的六项回归门全部通过，然后把下方 `<how-to-verify>` 的五步实测清单交给人工在真实火山引擎凭证与网络下执行。这些步骤会产生真实付费 API 调用（D-12 已知并被接受的后果），不可由 agent 代跑、不可自动批准。收到 "approved" 前不得把本阶段标记为完成。
  </action>
  <what-built>
Plan 01~07 已完成：`.env` 在 `config.py` 导入时自动加载（`override=False`）、provider 换成 openai 官方 SDK 并新增 `probe()` 探测原语、`vela doctor` 具备四项连通性自检与 `--offline`/`--online`/`--json` 三参数与退出码分层、`config/env_checks.yaml` 驱动的 `.env` 形态检查、`realllm` 标记的端到端验收用例。Task 1 的六项回归门已在 mock 环境下全部通过。

以下四项**必须**在真实火山引擎方舟凭证与可用网络下由人工确认——它们会产生真实付费 API 调用，无法自动化替代。
  </what-built>
  <how-to-verify>
前置：确认项目根 `.env` 中已填 `VELA_ARK_API_KEY`、`VELA_ARK_MODEL`（接入点 ID `ep-xxxx`）、`VELA_ARK_BASE_URL`，并把 `VELA_LLM_PROVIDER` 设为 `volcengine`。**全程不要手工 `export` 任何变量**——这正是 ENV-01 的验收点。

1. **ENV-01 / ENV-03（doctor 人读通道）**：运行
   `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m vela.cli doctor`
   确认：(a) 输出显示 `.env` 命中路径与已加载键数量；(b) 四项连通性（端点可达 / 鉴权有效 / 模型可用 / 四个逻辑模型映射完整）各有 ✅ 或 ❌ 结论；(c) `planner` / `verifier` / `reporter` / `distiller` 四条物理模型链均非空；(d) **API key 显示为前 4 后 4 掩码形态，不是明文**；(e) `base_url` 与 `ep-xxxx` 明文可见（便于对照 `.env` 排查）。
2. **ENV-03（`--json` 通道与退出码）**：运行
   `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m vela.cli doctor --json | .venv/bin/python3 -m json.tool`
   确认：(a) 输出是可解析的纯 JSON；(b) JSON 中的 API key 同样是掩码形态，且掩码结果与人读通道**完全一致**；(c) `echo $?` 的退出码：本地项全通过时为 `0`。再刻意断网或临时把 `VELA_ARK_API_KEY` 改错一位后重跑，确认连通性项标 ❌ 但**退出码仍为 0**（D-14）。
3. **ENV-04（形态错误可读出）**：把 `.env` 的 `VELA_ARK_BASE_URL` 临时改成 `https://ark.cn-beijing.volces.com/api/plan/v3` 后重跑 doctor，确认输出直接指出路径异常并给出正确形式 `/api/v3`，且退出码为 `1`。**确认后改回正确值**。
4. **ENV-02（端到端实测）**：运行
   `VELA_LLM_PROVIDER=volcengine PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest -m realllm -q`
   确认：(a) 用例被收集并实际执行（不是 skipped）；(b) 全部通过；(c) 诊断链路未因环境问题中途报错；(d) 产出的报告非空且含至少一个 `[[EV:row_hash]]` 引用。**不需要诊断结论正确**。
5. **凭证卫生终检**：`git status` 中不出现 `.env`；本次实测产生的 workspace 产物与审计日志未被加入版本库。

若任一项不符合预期，请描述具体现象（命令、输出片段——**输出片段中请自行抹去任何未被掩码的敏感值**）。
  </how-to-verify>
  <resume-signal>回复 "approved" 确认四项实测均符合预期；或描述不符合的具体项与现象</resume-signal>
  <acceptance_criteria>
    - 人工确认 doctor 人读通道四项连通性结论齐全，API key 为掩码形态
    - 人工确认 `--json` 为纯 JSON，掩码与人读通道一致
    - 人工确认连通性失败时退出码仍为 0；本地形态错误时退出码为 1
    - 人工确认 `pytest -m realllm` 实际执行（非 skipped）且通过，报告含 `[[EV:row_hash]]`
    - 人工确认 `git status` 中无 `.env`
    - 若任一项不达标，本阶段判为未完成，需回到对应 Plan 修复
  </acceptance_criteria>
  <verify>
    <automated>echo "checkpoint:human-verify gate=blocking — 需真实付费凭证与网络，由人工按 how-to-verify 五步确认后回复 approved"</automated>
  </verify>
  <done>四条 ROADMAP 成功判据在真实火山引擎环境下由人工逐条确认通过；凭证卫生终检无异常。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 真实凭证 + 网络 → 火山方舟端点 | Task 2 是本阶段唯一会真实外发数据并产生费用的执行路径 |
| 人工验收过程 → 反馈文本 / 工单 | 人工粘贴 doctor 输出时可能连带未掩码的敏感值 |
| 本阶段全部改动 → 版本库 | 收尾阶段是凭证误提交的最后拦截点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | 真实 `.env` 与实测产物被提交 | mitigate | Task 1 断言 `git ls-files \| grep -c '^\.env$'` 为 0 且 `git status --porcelain .env` 为空；Task 2 第 5 步人工终检 workspace 产物与审计日志未入库 |
| T-08-02 | Information Disclosure | 本阶段改动文件中的凭证形态残留 | mitigate | Task 1 第 6 项对本阶段全部改动文件跑 `grep -nE 'sk-[A-Za-z0-9]{8}\|ep-2[0-9]{7}'`，必须无命中 |
| T-08-03 | Information Disclosure | 人工反馈中粘贴的 doctor 输出 | mitigate | `how-to-verify` 明确要求「输出片段中请自行抹去任何未被掩码的敏感值」；掩码本身由 Plan 05 的 `mask_secret` 在数据层保证，人读与 `--json` 同源 |
| T-08-04 | Tampering | 为通过回归门而调整阈值/配置 | mitigate | Task 1 明确禁止用调阈值方式「修好」回归，要求停止并报告劣化场景 ID，回到对应 Plan 定位根因 |
| T-08-05 | Denial of Service | 实测产生非预期费用 | mitigate | `realllm` 用例只跑一个场景一次诊断；doctor 的探测量级为去重后物理模型集合各一次 `max_tokens=1`；Task 2 由人工主动触发，不进任何自动链路 |
| T-08-06 | Repudiation | 回归门结论无记录 | mitigate | Task 1 要求把六项结果（命令、退出码、关键数值、基线对比）逐项写进 SUMMARY，作为一票否决项的可追溯凭据 |
</threat_model>

<verification>
1. `PYTHON=.venv/bin/python3 make lint` / `make test-fast` / `make test` 三项退出码均为 0，`make test` 输出含 `deselected` 且 `failed` 为 0
2. `make eval` 的正确场景集合 ⊇ `01-BASELINE.md` 基线集合（回归数 0）
3. `./run_all.sh --skip-eval --skip-tests` 与 `make demo` 退出码均为 0
4. `git ls-files` 中无 `.env`，改动文件无凭证形态残留
5. 人工回复 "approved" 确认真实环境四项实测
</verification>

<success_criteria>
- ROADMAP 回归门两项（177 测试全过、仿真基准回归数 = 0）均达成且有记录
- ROADMAP 四条成功判据在真实环境下逐条被人工确认
- `run_all.sh` 与 `make demo` 两条演示链路未因 D-12 的联网策略而中断
- 版本库中不存在任何被追踪的凭证
</success_criteria>

<output>
Create `.planning/phases/01-llm/01-08-SUMMARY.md` when done（须记录：六项回归门的实测数值与基线对比、四条 ROADMAP 成功判据的逐条确认结论、以及真实环境实测中发现的任何遗留问题）
</output>
