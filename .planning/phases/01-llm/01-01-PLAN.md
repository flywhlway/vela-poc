---
phase: 01-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-llm/01-BASELINE.md
  - requirements.txt
  - pyproject.toml
autonomous: false
requirements: [ENV-01, ENV-02]

must_haves:
  truths:
    - "改动前的 mock 回归基线（测试通过数 + 黄金评测正确场景集合）已落盘，Plan 08 的『回归数 = 0』可被程序化判定"
    - "python-dotenv 与 openai 是必需依赖，缺失时安装即失败，运行期无 try-import 降级分支"
    - "pytest 默认不收集 realllm 用例，make test 不可能触发付费 API 调用"
    - "显式 pytest -m realllm 能覆盖默认排除并收集到 realllm 用例"
  artifacts:
    - path: ".planning/phases/01-llm/01-BASELINE.md"
      provides: "改动前 mock 基线：解释器 / pytest passed 数 / 黄金评测正确场景 ID 集合"
      contains: "baseline_eval_correct_case_ids"
    - path: "requirements.txt"
      provides: "必需依赖声明（含 python-dotenv 与 openai）"
      contains: "python-dotenv"
    - path: "pyproject.toml"
      provides: "PEP 621 dependencies + realllm 标记注册 + 默认排除"
      contains: "realllm"
  key_links:
    - from: "pyproject.toml"
      to: "pytest 收集行为"
      via: "[tool.pytest.ini_options] addopts"
      pattern: "not realllm"
    - from: "requirements.txt"
      to: "pyproject.toml dependencies"
      via: "两处必需依赖清单保持一致"
      pattern: "openai>="
---

<objective>
为整个 Phase 1 建立两个基座：(1) 采集改动前的 mock 回归基线，让 Plan 08 的回归门「仿真基准已通过用例回归数 = 0」有可比对的事实依据；(2) 按 D-03 把 `python-dotenv` 与 `openai` 落为必需依赖，并按 D-19 建立 `realllm` pytest 标记与默认排除机制。

Purpose: 后续所有计划都要新增 import 与新增用例，若不先钉住基线，「有没有回归」无法判定；若不先声明依赖，Plan 03（`.env` 加载）与 Plan 04（openai SDK 重写）会 import 失败；若不先建立 `realllm` 默认排除，任何一次 `make test` 都可能打真实付费 API。

Output: `01-BASELINE.md` 基线记录、`requirements.txt` 与 `pyproject.toml` 的依赖与标记声明。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-llm/01-CONTEXT.md
@.planning/phases/01-llm/01-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 采集改动前的 mock 回归基线</name>
  <files>.planning/phases/01-llm/01-BASELINE.md</files>
  <read_first>
    - Makefile（`install-dev` / `test` / `eval` / `lint` 目标。注意 `PYTHON ?= python3`：`make install-dev` 装进 `.venv`，但 `make test` 默认用系统 python3，必须显式传 `PYTHON=.venv/bin/python3`）
    - src/vela/cli.py 第 121-140 行（`cmd_eval` 的四项门与产物 `eval_report.md` / `eval_result.json`）
    - src/vela/eval/report.py（`_TARGETS` 与指标口径；确认 `eval_result.json` 中逐场景正确与否的字段名）
    - .planning/ROADMAP.md 第 33-35 行（回归门原文：177 个测试全过 + 仿真基准已通过用例回归数 = 0）
  </read_first>
  <action>
本仓库当前没有 `.venv`，系统 python3 缺 duckdb/pyarrow。先执行 `make install-dev` 建立 `.venv` 并可编辑安装。此后所有 make 目标一律用 `PYTHON=.venv/bin/python3 make <target>` 调用；直接调 pytest 时用 `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest ...`。这条约定要写进 SUMMARY，后续 Plan 全部沿用。

在**尚未做任何源码改动**的状态下采集三项基线，写入 `.planning/phases/01-llm/01-BASELINE.md`：

1. `baseline_interpreter`：`.venv/bin/python3` 的绝对路径与 `--version`，以及 duckdb / pyarrow / PyYAML / pytz / pytest 的实际版本。
2. `baseline_pytest`：运行 `PYTHON=.venv/bin/python3 make test`，记录 passed / failed / skipped 数量与退出码。ROADMAP 记载的期望值是 177 全过。
3. `baseline_eval`：运行 `PYTHON=.venv/bin/python3 make eval`（provider 保持 mock），记录退出码、四项门的实际指标数值，以及 `eval_result.json` 中**判定为正确的场景 ID 集合**（逐行列出）。这个集合即「仿真基准已通过用例」，Plan 08 用它判定回归数是否为 0。

`make eval` 会先跑 `make sim` 生成数据集，耗时较长，属预期。若 `make eval` 退出码非 0，不要改任何源码去「修好它」——如实记录当前退出码与未过的门，并在文件里注明「基线即当前事实，Plan 08 只判断是否劣化」。

`01-BASELINE.md` 用 YAML front-matter + Markdown 正文，front-matter 至少含 `baseline_interpreter`、`baseline_pytest_passed`、`baseline_eval_exit_code`、`baseline_eval_correct_case_ids` 四个键，便于 Plan 08 程序化读取。

⚠️ 禁止把任何环境变量值（尤其 `VELA_ARK_API_KEY`）写进基线文件，只记录命令、数量与场景 ID。
  </action>
  <acceptance_criteria>
    - `.venv/bin/python3 -c "import duckdb, pyarrow, yaml, pytz, pytest"` 退出码 0
    - `.planning/phases/01-llm/01-BASELINE.md` 存在，且同时含 `baseline_pytest_passed`、`baseline_eval_exit_code`、`baseline_eval_correct_case_ids` 三个键，值均为具体数字/列表而非占位符
    - `baseline_eval_correct_case_ids` 下逐行列出场景 ID（形如 `S3_UDS_NRC72`），条目数 ≥ 1
    - `grep -qiE 'sk-[A-Za-z0-9]{8}|VELA_ARK_API_KEY=[^[:space:]]' .planning/phases/01-llm/01-BASELINE.md` 无匹配（无凭证泄漏）
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python3 -c "import duckdb, pyarrow, yaml, pytz, pytest" && grep -q 'baseline_pytest_passed' .planning/phases/01-llm/01-BASELINE.md && grep -q 'baseline_eval_exit_code' .planning/phases/01-llm/01-BASELINE.md && grep -q 'baseline_eval_correct_case_ids' .planning/phases/01-llm/01-BASELINE.md && ! grep -qiE 'sk-[A-Za-z0-9]{8}|VELA_ARK_API_KEY=[^[:space:]]' .planning/phases/01-llm/01-BASELINE.md</automated>
  </verify>
  <done>基线文件落盘，含解释器、pytest 通过数、黄金评测退出码与正确场景 ID 集合，且不含任何凭证。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: 新增 pip 包合法性人工核验（python-dotenv / openai）</name>
  <files>（无文件改动，纯核验闸门）</files>
  <read_first>
    - .planning/phases/01-llm/01-CONTEXT.md（D-03 两个必需依赖的取舍理由；D-02 本地优先铁律保留）
    - AGENTS.md §安全边界
  </read_first>
  <action>
暂停执行并等待人工核验：本任务不产生任何文件改动，唯一动作是把下方 `<how-to-verify>` 的两个 PyPI 页面核验项交给人工确认。收到 "approved" 之前，**禁止**执行 Task 3 的任何依赖写入或 `pip install`。这是 D-03 引入 `python-dotenv` 与 `openai` 两个新包时的供应链闸门，不可自动批准。
  </action>
  <what-built>
Task 1 已完成基线采集。按 D-03 本阶段要引入两个此前不存在的 pip 依赖：`python-dotenv` 与 `openai`。本阶段无 RESEARCH.md（用户显式 `--skip-research`），因此不存在 Package Legitimacy Audit 表，按兜底策略两个包一律视为 `[ASSUMED]`，必须由人工核验合法性后才允许写入依赖清单。该闸门不可自动批准，不受 auto_advance 影响。
  </what-built>
  <how-to-verify>
1. 打开 https://pypi.org/project/python-dotenv/ ，确认：Homepage 指向 `github.com/theskumar/python-dotenv`；最近发布日期合理；下载量为百万量级；无相似名 typosquat 迹象。
2. 打开 https://pypi.org/project/openai/ ，确认：Homepage 指向 `github.com/openai/openai-python`；维护方为 OpenAI 官方；版本线与官方 SDK 一致（本机现存 2.41.0 可作对照）。
3. 确认二者均为纯本地可安装包，不引入必须联网才能跑通主链路的运行时服务（D-02「本地优先」）——`openai` 仅在 provider 切到 volcengine / openai_compat 时才发网络请求。
4. 若任一包判定为可疑（SUS）或伪造（SLOP），立即停止本阶段，反馈包名与理由。
  </how-to-verify>
  <resume-signal>回复 "approved" 放行写入依赖清单；或描述发现的问题</resume-signal>
  <acceptance_criteria>
    - 人工回复 "approved" 后方可执行 Task 3
    - 若回复非 approved，Task 3 不得执行，结论必须写入 SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>echo "checkpoint:human-verify gate=blocking-human — 包合法性闸门由人工放行，无自动化替代命令"</automated>
  </verify>
  <done>两个新增 pip 包的合法性已由人工在 pypi.org 页面核验并放行。</done>
</task>

<task type="auto">
  <name>Task 3: 写入必需依赖并建立 realllm 标记默认排除</name>
  <files>requirements.txt, pyproject.toml</files>
  <read_first>
    - requirements.txt（第 1-5 行：必需依赖惯例，行尾注释写「为什么必需」）
    - requirements-optional.txt（第 1-6 行：可选依赖惯例，注释写「缺失 → 如何降级」；D-03 明确两个新依赖不进这里）
    - pyproject.toml（第 11-16 行 `dependencies`；第 18-22 行 `optional-dependencies`；第 33-39 行 `[tool.pytest.ini_options]`，当前 `addopts = "-q --strict-markers"` 无任何 `-m` 排除）
    - .planning/phases/01-llm/01-PATTERNS.md §9（依赖与标记的既有形状）与 §No Analog Found（realllm 默认排除机制无先例）
    - src/vela/util/hashing.py 第 23-30 行（`try: import blake3 / except ImportError` 降级模式——本任务禁止复制，D-03 定为必需依赖）
  </read_first>
  <action>
按 D-03 把 `python-dotenv` 与 `openai` 写为必需依赖，两处清单保持一致：

1. `requirements.txt`：在核心依赖段追加 `python-dotenv>=1.0` 与 `openai>=1.40`，各带一行中文行尾注释说明「为什么必需」（python-dotenv：`.env` 加载失败即 ENV-01 失败，不设静默降级路径；openai：volcengine / openai_compat provider 的唯一实现基座，缺失时 provider 本就无法工作）。禁止写进 `requirements-optional.txt`。
2. `pyproject.toml` 的 `[project] dependencies` 追加 `"python-dotenv>=1.0"` 与 `"openai>=1.40"`。禁止追加到 `[project.optional-dependencies]` 的任何组（`fast` / `serve` / `dev` / `all` 均不动）。
3. `pyproject.toml` 的 `[tool.pytest.ini_options]`（D-19）：
   - `markers` 追加 `"realllm: 需要真实 LLM 端点的付费用例（默认排除，显式 -m realllm 才跑）"`，与既有 `slow` / `determinism` 同一书写风格。
   - `addopts` 由 `"-q --strict-markers"` 改为 `"-q --strict-markers -m 'not realllm'"`。addopts 经 shlex 解析，单引号形式可正确传出 `not realllm` 一个值；pytest 的 `-m` 是单值选项，命令行显式 `-m realllm` 会覆盖 addopts 中的值——这正是 D-19 要求的「默认排除、显式可跑」语义。

⚠️ 不要改 `Makefile` 的 `test` / `test-fast` 目标（`Makefile` 归 Plan 02 所有；且 `test-fast` 按文件名跑，不受 `-m` 影响）。
⚠️ 不要为这两个包写任何 try-import 降级分支（D-03）。
  </action>
  <acceptance_criteria>
    - `grep -c 'python-dotenv' requirements.txt` 为 1；`grep -cE '^openai' requirements.txt` 为 1
    - `grep -c 'dotenv\|openai' requirements-optional.txt` 为 0
    - `pyproject.toml` 的 `project.dependencies` 同时含 `python-dotenv` 与 `openai` 条目；`project.optional-dependencies` 的所有组均不含这两个名字
    - `tool.pytest.ini_options.addopts` 字符串含子串 `not realllm`；`markers` 列表存在以 `realllm:` 开头的条目
    - `.venv/bin/pip install -r requirements.txt` 退出码 0，且 `.venv/bin/python3 -c "import dotenv, openai"` 退出码 0
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q` 退出码 0，passed 数与 `01-BASELINE.md` 的 `baseline_pytest_passed` 相同（此时尚无 realllm 用例，排除机制不改变收集结果）
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pip install -q -r requirements.txt && .venv/bin/python3 -c "import dotenv, openai, tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=d['project']['dependencies']; assert any('python-dotenv' in x for x in deps), deps; assert any(x.startswith('openai') for x in deps), deps; opt=d['project']['optional-dependencies']; assert not any(('openai' in x or 'dotenv' in x) for g in opt.values() for x in g), opt; ini=d['tool']['pytest']['ini_options']; assert 'not realllm' in ini['addopts'], ini['addopts']; assert any(m.startswith('realllm:') for m in ini['markers']), ini['markers']; print('OK')" && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q</automated>
  </verify>
  <done>两个必需依赖在 requirements.txt 与 pyproject.toml 中一致声明；realllm 标记已注册且默认被 addopts 排除；既有测试全部仍通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → 本地 site-packages | 第三方包内容进入本进程执行上下文，是供应链攻击面 |
| `.env` / 进程环境 → 基线文档 | 采集基线时可能把凭证误写进 `.planning/` 下的可提交文件 |
| pytest 默认收集集合 → 外部付费 API | 若默认收集含 realllm 用例，一次 `make test` 即产生真实调用与数据外发 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `pip install python-dotenv` / `openai` | mitigate | Task 2 的 `checkpoint:human-verify gate="blocking-human"`：两个 `[ASSUMED]` 包必须人工在 pypi.org 核验 Homepage / 维护方 / typosquat 后放行；不可自动批准 |
| T-01-01 | Information Disclosure | `01-BASELINE.md` | mitigate | Task 1 明确禁止写入任何环境变量值；acceptance 用 `grep -qiE 'sk-[A-Za-z0-9]{8}\|VELA_ARK_API_KEY=[^[:space:]]'` 反向断言 |
| T-01-02 | Tampering | `[tool.pytest.ini_options] addopts` | mitigate | 追加 `-m 'not realllm'`，使付费用例默认不被收集；这是「测试误打付费 API」威胁的第一重缓解（第二重是 Plan 03 的 conftest 无条件锁 mock） |
| T-01-03 | Denial of Service | 新增运行期依赖 | accept | `openai` 仅在 provider 非 mock 时构造客户端并发请求；mock 下零网络，`make test` / `make demo` 主链路仍可离线跑通（D-02） |
| T-01-04 | Repudiation | 依赖版本无 lockfile | accept | 仓库既有状态即无 lockfile（`>=` 下限约束）；CONTEXT.md `<deferred>` 已把补 lockfile 显式推迟，本阶段不扩大范围 |
</threat_model>

<verification>
1. `.venv` 可用且 `import dotenv, openai` 成功
2. `01-BASELINE.md` 三项基线齐备且不含凭证
3. 两处依赖清单一致、均在必需组
4. `addopts` 含 `-m 'not realllm'`，`markers` 已注册 `realllm`
5. `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q` 退出码 0，passed 数不低于基线
</verification>

<success_criteria>
- 存在 `.planning/phases/01-llm/01-BASELINE.md`，front-matter 含 `baseline_pytest_passed` / `baseline_eval_exit_code` / `baseline_eval_correct_case_ids` 且值为实测数据
- `requirements.txt` 与 `pyproject.toml [project] dependencies` 均含 `python-dotenv` 与 `openai`，且二者不出现在任何可选依赖组
- `pyproject.toml` 的 `addopts` 含 `not realllm`，`markers` 含 `realllm:` 条目
- 既有测试通过数不低于基线，退出码 0
</success_criteria>

<output>
Create `.planning/phases/01-llm/01-01-SUMMARY.md` when done（须记录：`.venv` 解释器调用约定 `PYTHON=.venv/bin/python3`、基线三项数值、包合法性核验结论）
</output>
