---
phase: 01-llm
plan: 06
type: execute
wave: 3
depends_on: ["01-03", "01-04", "01-05"]
files_modified:
  - src/vela/cli.py
  - tests/test_doctor.py
autonomous: true
requirements: [ENV-03, ENV-04]

must_haves:
  truths:
    - "vela doctor 一次性输出端点可达性 / 鉴权有效性 / 模型可用性 / 四个逻辑模型映射完整性四项自检结果"
    - "无需先跑一次失败诊断即可定位配置错误"
    - "doctor 能报出 .env 中的行尾注释污染值与 base_url 路径异常"
    - "VELA_LLM_PROVIDER=mock 时不发起任何网络请求；volcengine/openai_compat 时自动探测"
    - "--offline 与 --online 可双向强制覆盖自动判定"
    - "连通性四项任一失败时输出显著标 ❌ 但退出码仍为 0，run_all.sh 的 set -euo pipefail 不会被中断"
    - "本地硬错误（配置缺失 / 必需依赖缺失 / .env 形态错误）退出码为 1"
    - "--json 输出是纯 JSON 且与人读输出来自同一套检查结果，掩码规则一致"
  artifacts:
    - path: "src/vela/cli.py"
      provides: "重构后的 cmd_doctor（list[dict] 收集 + 双通道渲染 + 退出码分层）与三个新参数"
      contains: "def cmd_doctor"
    - path: "tests/test_doctor.py"
      provides: "退出码分层 / --json 纯净性 / 掩码一致性 / 自动跳过 / 形态错误 的护栏"
      min_lines: 80
  key_links:
    - from: "src/vela/cli.py::cmd_doctor"
      to: "src/vela/envcheck.py::EnvChecker.run"
      via: "并入 checks 列表，kind=local"
      pattern: "EnvChecker"
    - from: "src/vela/cli.py::cmd_doctor"
      to: "src/vela/gateway/openai_compat.py::probe"
      via: "去重后的物理模型集合各发一次最小 chat"
      pattern: "probe\\("
    - from: "src/vela/cli.py::cmd_doctor"
      to: "src/vela/config.py::dotenv_report"
      via: ".env 命中路径与被遮蔽键名的展示"
      pattern: "dotenv_report"
    - from: "src/vela/cli.py::build_parser"
      to: "doctor 子命令"
      via: "--offline / --online / --json 三个参数"
      pattern: "--offline"
---

<objective>
按 D-12~D-15 与 D-18 实现 ENV-03 与 ENV-04 的呈现层：把 `cmd_doctor` 从「逐项 print、无中间结构」重构为「先收 `list[dict]` 再双通道渲染」，加入四项连通性自检与 `.env` 形态检查，并落地 D-14 的退出码分层与 D-13 的双向强制开关。

Purpose: 当前 `vela doctor` 零网络调用，配置错误只能靠跑一次失败诊断才暴露。ENV-03 要求把「端点可达 / 鉴权有效 / 模型可用 / 四个逻辑模型映射完整」四项在诊断之前一次性给出；ENV-04 要求形态错误可直接从 doctor 输出读出。同时 Phase 2 要把环境指纹写进评测报表，`--json` 是它的消费接口。

Output: 重构后的 `cmd_doctor` + 三个新 CLI 参数 + 六类护栏用例。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-llm/01-CONTEXT.md
@.planning/phases/01-llm/01-PATTERNS.md
@.planning/phases/01-llm/01-03-SUMMARY.md
@.planning/phases/01-llm/01-04-SUMMARY.md
@.planning/phases/01-llm/01-05-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 cmd_doctor 为 list[dict] 收集 + 双通道渲染，并注册三个新参数</name>
  <files>src/vela/cli.py</files>
  <read_first>
    - src/vela/cli.py 第 171-198 行（`cmd_doctor` 全文改造基线：逐项 print、无中间结构；必需 vs 可选依赖的三态图标惯例 `✅` / `❌ (必需，缺失)` / `⚠️ (可选，未安装，将降级)`；`config_dir` / `config_hash` 的左对齐 11 字符标签排版）
    - src/vela/cli.py 第 23-25 行 `_p()`（`--json` 通道必须调用它，不要另起 `json.dumps`）与第 80-85 行 `cmd_query`（stdout 纯 JSON 可被 `json.loads` 直接消费的先例）
    - src/vela/cli.py 第 144-162 行 `cmd_evidence`（CLI 侧「`list[dict]` → 图标化 print → 退出码」的现成形状）
    - src/vela/cli.py 第 225-231 行与第 233-243 行（`--list` 布尔开关与可选值参数的注册惯例；全项目无 `--no-xxx` 反向开关先例）、第 268-269 行（doctor 子命令改造基线）、第 273-276 行（`main()` 收口）
    - src/vela/evidence/qa.py 第 41-56 行与第 69-82 行、第 99 行（**唯一的双通道渲染先例**：checks 收成三元组 → JSON `{"name","ok","detail"}` + Markdown 表格 ✅/❌ 两次遍历）
    - src/vela/gateway/base.py 第 155-174 行 `build_gateway`（按 kind 分发 provider）与第 160 行（provider 判定优先级 `provider_name or os.environ["VELA_LLM_PROVIDER"] or cfg["active"]`——D-12 必须复用同一优先级，不要另写一套）
    - src/vela/gateway/openai_compat.py（Plan 04 产物：`probe()` 的五键返回与归因表、`ensure_credentials()`、`models_for()`）
    - src/vela/envcheck.py（Plan 05 产物：`EnvChecker.run(provider, environ)` 返回 `{name, ok, detail, kind}`）
    - src/vela/config.py（Plan 03 产物：`dotenv_report()` 返回 `{path, loaded, keys, shadowed}`）
    - config/llm.yaml 第 8-12 行（四个逻辑模型名 `planner` / `verifier` / `reporter` / `distiller` 的权威来源）
    - run_all.sh 第 7 行与第 30-31 行（`set -euo pipefail` + 第 1 步跑 doctor —— D-14 退出码策略的消费方）
    - Makefile 第 41-42 行（`doctor` 目标）
    - tests/test_cli_and_server.py 第 9-13 行（既有 doctor 用例，必须保持通过）与第 83-88 行（子命令注册回归护栏）
  </read_first>
  <action>
把 `cmd_doctor` 重构为「先收集 `list[dict]`，再双通道渲染」（D-18），并挂上三个新参数（D-13/D-18）。

**1. `build_parser()` 的 doctor 段**（第 268-269 行）追加三个参数，全部用 `action="store_true"`（项目无 `add_mutually_exclusive_group` 先例，采用两个独立开关 + 代码内校验，更贴近现状）：
   - `--offline`：强制跳过全部网络探测
   - `--online`：强制执行网络探测（即使 provider 是 mock）
   - `--json`：结构化输出，`dest="as_json"` 避免与模块名混淆
   两者同时给出时，向 stderr 打印中文错误并返回 `2`（沿用 `cli.py:74-76` 的「用法错 → 2」惯例）。

**2. 检查项数据结构**：统一为 `{"name": str, "ok": bool, "detail": str, "kind": "local"|"connectivity", "warn": bool}`。
   - `name` / `ok` / `detail` 三键对齐 `evidence/qa.py` 的既定契约（Plan 05 的 `EnvChecker` 已按此产出）。
   - `kind` 用于 D-14 的退出码分层。
   - `warn` 承载既有三态图标中的 ⚠️ 语义（可选依赖未安装、探测被跳过），默认 `False`。渲染规则：`❌` if not ok else (`⚠️ ` if warn else `✅`)。

**3. 本地检查项（`kind="local"`）**：
   - 五个 config YAML 存在性（`pipeline.yaml` / `parsers.yaml` / `ota_phases.yaml` / `budget.yaml` / `llm.yaml`），保持既有清单。
   - 必需模块可导入：把第 195 行的 `req = mod in ("duckdb", "pyarrow", "yaml")` 集合扩展为含 `dotenv` 与 `openai`（D-03 定为必需；注意导入名是 `dotenv` 而非 `python-dotenv`）。可选模块（`xxhash` / `blake3` / `fastapi` / `pytest`）缺失时 `ok=True, warn=True`，**不得影响退出码**（与原实现一致）。
   - `EnvChecker().run(provider)` 的全部结果并入（Plan 05 已把 `kind` 置为 `"local"`）——这就是 D-14 中「`.env` 形态错误返 1」的落点，也是 ENV-04 的输出来源。

**4. provider 判定与探测时机（D-12）**：
   - `provider = os.environ.get("VELA_LLM_PROVIDER") or load_yaml("llm.yaml").get("active", "mock")`，与 `base.py:160` 同一优先级，不要另写一套。
   - `do_probe = a.online or (not a.offline and provider != "mock")`。
   - 不探测时，四项连通性记为 `{"ok": True, "warn": True, "detail": "provider=<name>，已跳过网络探测（--online 可强制）"}`，保证 mock 下 `make demo` / `run_all.sh` 依然离线跑通（D-02 本地优先）。

**5. 连通性四项（`kind="connectivity"`，D-15）**：
   - 用 `build_gateway(provider)` 构造网关并取 `gw.provider`，不要自己拼客户端。
   - **第 4 项「四个逻辑模型映射完整性」先本地判定**：对 `("planner", "verifier", "reporter", "distiller")` 各调 `provider.models_for(name)`，任一链为空 → 该项 `ok=False`；`detail` 展示每个逻辑模型解析出的物理模型链（`ep-xxxx` 明文，D-17）。这一步零成本、零网络。
   - **前三项合并为最小 chat 探测**：把四条链的物理模型**去重**成集合，对每个物理模型调一次 `provider.probe(m)`（典型配置下四者回落到同一个 `VELA_ARK_MODEL`，只花 1 次调用）。把各次 probe 的 `reachable` / `authenticated` / `model_ok` 分别聚合（全部为真才为真），产出「端点可达」「鉴权有效」「模型可用」三项，`detail` 取各 probe 的 `error_kind` 与已 scrub 的 `detail` 拼接。
   - `provider` 不具备 `probe` 属性时（例如 `--online` 强制但 provider 是 mock）→ 四项记 `ok=True, warn=True`，detail 说明该 provider 不支持网络探测。用 `hasattr(provider, "probe")` 判定，不要 import `openai`——CLI 是业务代码，禁止写 provider 专属逻辑（AGENTS.md 铁律 3）。
   - `provider.ensure_credentials()` 抛 `LLMError` 的情形由 `probe()` 内部处理为 `error_kind="MissingCredentials"`，此时前三项 `ok=False`，但仍属 `kind="connectivity"`（凭证「未填」由 `EnvChecker` 以 local 项判定并决定退出码，避免同一问题两次影响退出码语义）。

**6. 双通道渲染（D-18）**：
   - `--json`：**只**调 `_p({...})`，stdout 必须是可被 `json.loads` 直接消费的纯 JSON，不得混入任何人读行。顶层键：`vela_version` / `python` / `config_dir` / `config_hash` / `provider` / `probed`（bool）/ `dotenv`（来自 `dotenv_report()`，仅 path/loaded/keys/shadowed）/ `checks`（上述 list[dict]）/ `checks_passed`（全部 `ok` 为真）/ `local_ok`（全部 `kind=="local"` 项为真）。
   - 人读：沿用既有左对齐 11 字符标签 + `: ` 排版与 ✅/❌/⚠️ 三态图标；新增一行展示 `.env` 加载状态——路径明文、`loaded`、已加载键**数量**、被进程变量遮蔽的键**名**列表（D-09 把加载信息收进 doctor）。**绝不打印任何变量值**：`dotenv_report()` 契约上只出键名，掩码由 `EnvChecker` 在 detail 中统一施加，两条通道共用同一份已掩码的 `checks`（D-17/D-18 掩码一致性由此结构性保证）。
   - 两条通道遍历的必须是**同一个** `checks` 变量（照 `qa.py:41-102` 的形状），禁止各自重新计算。

**7. 退出码（D-14）**：`local_bad = any(not c["ok"] for c in checks if c["kind"] == "local")`；`return 1 if local_bad else 0`。连通性失败**不影响**退出码。在 `return` 语句上方写一段注释，明确这是对本文件既有「失败即非零」惯例（第 63 / 85 / 118 / 140 / 162 行）的**有意偏离**，理由是 `run_all.sh:7` 的 `set -euo pipefail` + `:31` 第 1 步跑 doctor——一次限流或临时断网不应中断整条演示链路；并指向 `Makefile:41-42` 与 `tests/test_cli_and_server.py:9-13` 两个消费方。

**8. 纪律约束**：CLI 输出只用 `print()`，不引入 `logging`，不使用 `EventBus`（CLI 层现有子命令零处使用）。错误输出到 stderr 用 `print(..., file=sys.stderr)`（第 74 行先例）。
  </action>
  <acceptance_criteria>
    - `grep -c '\-\-offline' src/vela/cli.py` ≥ 1，`grep -c '\-\-online' src/vela/cli.py` ≥ 1，`grep -c 'as_json' src/vela/cli.py` ≥ 1
    - `grep -c 'EnvChecker' src/vela/cli.py` ≥ 1，`grep -c 'dotenv_report' src/vela/cli.py` ≥ 1，`grep -c 'hasattr(.*probe' src/vela/cli.py` ≥ 1
    - `grep -c 'import openai' src/vela/cli.py` 为 0（CLI 不写 provider 专属逻辑）
    - `grep -vE '^\s*#' src/vela/cli.py | grep -c 'import logging'` 为 0
    - `grep -c '"kind"' src/vela/cli.py` ≥ 1 且 `grep -c 'connectivity' src/vela/cli.py` ≥ 1
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m vela.cli doctor` 退出码 0，输出含 `config_hash`、四个逻辑模型名与「已跳过网络探测」（mock 下）
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m vela.cli doctor --json | .venv/bin/python3 -m json.tool` 退出码 0（stdout 是纯 JSON）
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m vela.cli doctor --offline --online` 退出码 2
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_cli_and_server.py -q` 退出码 0（既有 doctor 与子命令注册用例未被破坏）
    - `PYTHON=.venv/bin/python3 make doctor` 退出码 0，`PYTHON=.venv/bin/python3 make lint` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c '\-\-offline' src/vela/cli.py) -ge 1 && test $(grep -c '\-\-online' src/vela/cli.py) -ge 1 && test $(grep -c 'as_json' src/vela/cli.py) -ge 1 && test $(grep -c 'EnvChecker' src/vela/cli.py) -ge 1 && test $(grep -c 'dotenv_report' src/vela/cli.py) -ge 1 && test $(grep -c 'import openai' src/vela/cli.py) -eq 0 && test $(grep -vE '^\s*#' src/vela/cli.py | grep -c 'import logging') -eq 0 && test $(grep -c 'connectivity' src/vela/cli.py) -ge 1 && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m vela.cli doctor >/dev/null && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m vela.cli doctor --json | .venv/bin/python3 -m json.tool >/dev/null && ! PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m vela.cli doctor --offline --online >/dev/null 2>&1 && PYTHON=.venv/bin/python3 make lint && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_cli_and_server.py -q</automated>
  </verify>
  <done>`cmd_doctor` 已重构为单一 `checks` 列表 + 双通道渲染，四项连通性与 env 形态检查就位，`--offline`/`--online`/`--json` 三参数注册，退出码按 local/connectivity 分层。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新建 tests/test_doctor.py 六类护栏</name>
  <files>tests/test_doctor.py</files>
  <read_first>
    - src/vela/cli.py（Task 1 完成后的 `cmd_doctor` 与 `build_parser` 实际实现）
    - tests/test_cli_and_server.py 第 9-13 行（`main([...])` + `capsys` + 返回码的既有形状）、第 45-50 行（`json.loads(out)` 断言 stdout 纯净的形状）、第 83-88 行（子命令注册断言）
    - tests/test_envcheck.py（Plan 05 产物，避免重复覆盖同一断言）
    - .planning/phases/01-llm/01-CONTEXT.md D-12~D-18
    - .planning/ROADMAP.md 第 30-32 行（成功判据 3 与 4）
  </read_first>
  <behavior>
    - `test_doctor_default_mock_skips_network_and_exits_zero`：默认（conftest 锁 mock）→ rc 为 0，输出含「已跳过网络探测」，且不发起任何网络请求
    - `test_doctor_json_stdout_is_pure_json`：`doctor --json` 的 stdout 可被 `json.loads` 解析，顶层含 `checks` / `checks_passed` / `local_ok` / `provider` / `config_hash` / `dotenv`
    - `test_doctor_reports_four_logical_models`：输出中同时出现 `planner` / `verifier` / `reporter` / `distiller` 四个逻辑模型名
    - `test_doctor_offline_and_online_conflict_returns_two`：同时传 `--offline --online` → rc 为 2，stderr 有中文错误
    - `test_doctor_masks_api_key_in_both_channels`：设假密钥 `DUMMY-ARKKEY-0123456789ABCD` + provider=volcengine + `--offline` → 人读与 `--json` 两种输出**均不含完整假密钥串**，且均含 `DUMM****ABCD`
    - `test_doctor_bad_base_url_path_is_local_error_and_exits_one`：provider=volcengine + `VELA_ARK_BASE_URL` 末段为 `/api/plan/v3` + `--offline` → rc 为 1，输出含 `/api/v3` 修复提示
    - `test_doctor_connectivity_failure_does_not_change_exit_code`：monkeypatch 让 provider 的 `probe` 返回三项皆假 + 本地项全通过 → rc 仍为 0，但输出含 `❌`
    - `test_doctor_dotenv_report_shows_no_values`：`--json` 输出的 `dotenv` 对象键集合为 `{path, loaded, keys, shadowed}`，且其中不出现任何变量值
  </behavior>
  <action>
新建 `tests/test_doctor.py`，首行中文 docstring 说明用途（`vela doctor` 四项自检、双通道渲染、退出码分层与掩码一致性的回归护栏）。

实现要点：
- 一律走 `from vela.cli import main` + `main([...])` + `capsys`，与 `tests/test_cli_and_server.py` 同形。
- 环境变量全部用 `monkeypatch.setenv` / `delenv(raising=False)`，用完自动还原，禁止污染其他用例。
- **禁止真实网络调用**：所有需要连通性结果的用例一律 `monkeypatch` provider 的 `probe`（或 `vela.gateway.base.build_gateway`）返回桩结果；凡未 monkeypatch 的用例必须带 `--offline` 或让 provider 保持 mock。
- 掩码一致性用例（`test_doctor_masks_api_key_in_both_channels`）必须**两次调用** `main`（一次不带 `--json`、一次带），对两份输出施加同一组断言——这正是 D-18「同一套检查结果、掩码规则一致」的可执行验证。
- `test_doctor_connectivity_failure_does_not_change_exit_code` 的 docstring 要写明为什么这条用例不可省略：D-14 的退出码分层是对本项目「失败即非零」主流惯例的**有意偏离**，消费方是 `run_all.sh` 的 `set -euo pipefail`；没有这条断言，后续任何人「顺手改回非零」都不会被发现（照 `tests/test_cli_and_server.py:30-35` 的注释惯例书写）。
- 全部假值用 `DUMMY-` 前缀，禁止出现 `sk-` 或真实 `ep-` 接入点 ID 形态。
  </action>
  <acceptance_criteria>
    - `tests/test_doctor.py` 存在且行数 ≥ 80
    - `grep -c 'def test_' tests/test_doctor.py` ≥ 8
    - `grep -cE 'sk-[A-Za-z0-9]{8}|ep-2[0-9]{7}' tests/test_doctor.py` 为 0
    - `grep -c 'json.loads' tests/test_doctor.py` ≥ 1
    - `grep -c 'monkeypatch' tests/test_doctor.py` ≥ 4（连通性用例走桩而非真实网络）
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_doctor.py -q` 退出码 0
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q` 退出码 0，passed 数 ≥ `01-BASELINE.md` 的 `baseline_pytest_passed`
    - 用例执行期间无外发网络流量（人工核对：所有 connectivity 断言都在 monkeypatch 桩之后）
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'def test_' tests/test_doctor.py) -ge 8 && test $(grep -cE 'sk-[A-Za-z0-9]{8}|ep-2[0-9]{7}' tests/test_doctor.py) -eq 0 && test $(grep -c 'json.loads' tests/test_doctor.py) -ge 1 && test $(grep -c 'monkeypatch' tests/test_doctor.py) -ge 4 && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_doctor.py -q && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q</automated>
  </verify>
  <done>八条护栏覆盖退出码分层、`--json` 纯净性、掩码一致性、自动跳过、形态错误与 dotenv 报告不泄值；全量测试不低于基线。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `os.environ` / `EnvChecker` / `probe()` → doctor 的 stdout 与 `--json` | doctor 是本阶段凭证外泄面最大的组件：人读输出可能被贴进工单，`--json` 会被 Phase 2 写进评测报表 |
| CLI 进程 → 外部 LLM 端点 | doctor 是 CLI 层第一个主动联网的子命令，此前全无先例 |
| doctor 退出码 → `run_all.sh` / `Makefile` / CI | 退出码语义变化会改变整条演示链路的中断行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | doctor 人读输出与 `--json` | mitigate | 两条通道遍历同一个已掩码的 `checks` 列表（掩码由 Plan 05 的 `EnvChecker` 在 detail 中统一施加），结构上不可能只掩一边；`test_doctor_masks_api_key_in_both_channels` 对两份输出施加同一组断言 |
| T-06-02 | Information Disclosure | `.env` 加载状态行 | mitigate | 只展示 `dotenv_report()` 的 `path` / `loaded` / 键**名** / 被遮蔽键**名**；该函数契约上不返回任何值（Plan 03 已钉死），`test_doctor_dotenv_report_shows_no_values` 二次确认 |
| T-06-03 | Information Disclosure | SDK 异常消息经 probe 进入 detail | mitigate | `probe().detail` 在 Plan 04 已过 `_scrub()`（api_key → `***`）；doctor 直接透传，不再二次拼接原始异常对象 |
| T-06-04 | Denial of Service | doctor 自动联网（D-12） | accept | 已被用户明确接受；由 D-14 退出码策略兜底（连通性失败返 0）；探测量级为「去重后的物理模型集合各一次 `max_tokens=1`」，典型配置仅 1 次；mock 下零网络，`make test` / `make demo` 主链路仍离线 |
| T-06-05 | Tampering | 退出码语义被后续改动回退 | mitigate | `test_doctor_connectivity_failure_does_not_change_exit_code` 显式钉住「连通性失败 rc 仍为 0」；代码中 `return` 上方有注释说明这是有意偏离并指向消费方 |
| T-06-06 | Elevation of Privilege | CLI 层写入 provider 专属逻辑 | mitigate | 用 `hasattr(provider, "probe")` 判定可探测性，`grep -c 'import openai' src/vela/cli.py` 断言为 0；provider 差异仍收敛在 `gateway/` 内（AGENTS.md 铁律 3） |
| T-06-07 | Repudiation | `--json` 输出被非 JSON 内容污染 | mitigate | `--json` 分支只调 `_p()`，acceptance 用 `doctor --json \| python3 -m json.tool` 与 `json.loads` 双重断言 |
</threat_model>

<verification>
1. `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m vela.cli doctor` rc 为 0，含四个逻辑模型名与跳过说明
2. `... doctor --json | python3 -m json.tool` rc 为 0
3. `... doctor --offline --online` rc 为 2
4. `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_doctor.py tests/test_cli_and_server.py -q` rc 为 0
5. `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q` rc 为 0，passed 不低于基线
6. `PYTHON=.venv/bin/python3 make doctor` rc 为 0；`PYTHON=.venv/bin/python3 make lint` rc 为 0
</verification>

<success_criteria>
- `vela doctor` 一次性给出端点可达 / 鉴权有效 / 模型可用 / 四逻辑模型映射完整四项结果（ROADMAP 成功判据 3）
- `vela doctor` 报出 `base_url` 路径异常与值内注释残留（ROADMAP 成功判据 4）
- mock 下零网络、volcengine 下自动探测、`--offline` / `--online` 双向可覆盖
- 本地硬错误 rc=1；连通性失败 rc=0 且显著标 ❌
- `--json` 为纯 JSON，与人读同源同掩码
- 既有 `tests/test_cli_and_server.py` 用例零改动仍通过
</success_criteria>

<output>
Create `.planning/phases/01-llm/01-06-SUMMARY.md` when done（须记录：`--json` 输出的顶层键清单，供 Phase 2 消费环境指纹时对接）
</output>
