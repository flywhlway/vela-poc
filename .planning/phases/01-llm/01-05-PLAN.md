---
phase: 01-llm
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/vela/util/textutil.py
  - tests/test_util.py
  - config/env_checks.yaml
  - src/vela/envcheck.py
  - tests/test_envcheck.py
  - .env.example
autonomous: true
requirements: [ENV-04]

must_haves:
  truths:
    - "API key 在任何输出形态下只显示前 4 后 4，长度不足则全掩"
    - "base_url 路径异常（如 /api/plan/v3）能被规则表识别并给出正确形式提示"
    - "值内残留的行尾注释（加引号的值不会被 python-dotenv 剥离）能被识别"
    - "形态检查规则全部在 config/env_checks.yaml，业务代码不硬编码任何变量名或正则"
    - "env_checks.yaml 不参与 config_hash 计算，改它不影响推理结果与证据包指纹"
    - ".env.example 不再含会被朴素解析器吃进值的行尾注释"
  artifacts:
    - path: "src/vela/util/textutil.py"
      provides: "mask_secret 前 4 后 4 掩码"
      exports: ["mask_secret"]
    - path: "config/env_checks.yaml"
      provides: "ENV-04 形态检查规则表（纯诊断用途）"
      contains: "VELA_ARK_BASE_URL"
    - path: "src/vela/envcheck.py"
      provides: "规则驱动的形态检查器，cfg 与 environ 均可注入"
      exports: ["EnvChecker"]
    - path: "tests/test_envcheck.py"
      provides: "路径异常 / 注释残留 / 掩码 / provider 相关必填性 的护栏"
      min_lines: 60
    - path: ".env.example"
      provides: "无行尾注释的环境变量样例"
  key_links:
    - from: "src/vela/envcheck.py::EnvChecker"
      to: "config/env_checks.yaml"
      via: "load_yaml 默认加载 + cfg 可注入"
      pattern: "env_checks.yaml"
    - from: "src/vela/envcheck.py::EnvChecker.run"
      to: "src/vela/cli.py::cmd_doctor（Plan 06 消费）"
      via: "返回 {name, ok, detail, kind} 列表，字段名对齐 evidence/qa.py"
      pattern: "\"detail\""
    - from: "src/vela/envcheck.py"
      to: "src/vela/util/textutil.py::mask_secret"
      via: "display=masked 的变量值在 detail 中掩码"
      pattern: "mask_secret"
---

<objective>
按 D-16 / D-17 建立 ENV-04 的两块基建：配置驱动的环境变量形态检查规则表 `config/env_checks.yaml` 与其消费模块 `src/vela/envcheck.py`，以及统一的密钥掩码函数 `mask_secret`；同时清理 `.env.example` 的行尾注释。

Purpose: ENV-04 的重心在 python-dotenv 引入后已经转移——朴素解析器的注释污染问题被 SDK 化解，剩下的真实检查项是 `base_url` 路径异常（用户 `.env` 中疑似的 `/api/plan/v3` vs 代码文档口径的 `/api/v3`）、加引号值内的注释残留、接入点 ID 形态。这些必须在 doctor 跑连通性探测**之前**就能判定，且是 D-14 中「返 1 的本地硬错误」那一类。

Output: 掩码函数 + 规则表 + 检查器 + 护栏用例 + 清理后的 `.env.example`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-llm/01-CONTEXT.md
@.planning/phases/01-llm/01-PATTERNS.md
@.planning/phases/01-llm/01-01-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 mask_secret 前 4 后 4 掩码函数</name>
  <files>src/vela/util/textutil.py, tests/test_util.py</files>
  <read_first>
    - src/vela/util/textutil.py 第 63-72 行（`mask_vin()`：借它「长度不足则全掩」的短路结构与分隔注释排版，但**不借**哈希前缀——D-17 要求前 4 后 4 明文）
    - .planning/phases/01-llm/01-CONTEXT.md D-17（掩码规则原文：API key 只显示前 4 后 4，长度不足则全掩；`base_url` 与 `ep-xxxx` 接入点 ID 明文展示）
    - .planning/phases/01-llm/01-PATTERNS.md §Shared Patterns D（掩码惯例与「只借短路结构」的说明）
    - tests/test_util.py（既有用例风格与 `mask_vin` 相关断言的写法，新增用例并入同一文件）
  </read_first>
  <behavior>
    - `mask_secret("")` 返回 `""`（空值由调用方渲染成「未设置」，掩码函数不承担这层语义）
    - `mask_secret("short")` 返回 `"****"`（长度 < 12 全掩）
    - `mask_secret("DUMMY-KEY-0123456789ABCD")` 返回 `"DUMM****ABCD"`（前 4 + 固定 4 星 + 后 4）
    - 星号数量恒为 4，不随原值长度变化（不泄露长度信息）
    - 返回值中不含原值的中段任何字符
    - `mask_secret(v, keep=6)` 时前后各保留 6 位，阈值随 `keep` 变化为 `keep*2+4`
  </behavior>
  <action>
在 `src/vela/util/textutil.py` 中 `mask_vin()` 之后新增 `mask_secret(value: str, keep: int = 4) -> str`，沿用同文件的 `# ---...---` 分隔注释 + 中文用途说明排版风格（注释注明这是 D-17 的实现，用于 `vela doctor` 输出与 `--json` 两种形态）。

实现规则：
- `value` 为假值（空串 / None）→ 返回 `""`。
- `len(value) < keep * 2 + 4` → 返回 `"****"`（全掩）。阈值取 `keep*2+4` 而非 `keep*2`，保证掩码后中段至少还有 4 个字符被隐藏，避免「前 4 后 4」把一个 8 位密钥全部明文输出。
- 否则返回 `value[:keep] + "****" + value[-keep:]`。星号数固定为 4 而非按原长补齐——固定长度不泄露密钥长度信息。
- 函数不做任何 I/O、不 print、无副作用。

在 `tests/test_util.py` 追加一组 `mask_secret` 用例，覆盖 `<behavior>` 列出的六条断言。用例中的样本值一律用 `DUMMY-` 前缀假串，禁止出现 `sk-` 等真实密钥前缀形态。
  </action>
  <acceptance_criteria>
    - `grep -c 'def mask_secret' src/vela/util/textutil.py` 为 1
    - `PYTHONPATH=src .venv/bin/python3 -c "from vela.util.textutil import mask_secret as m; assert m('')=='' ; assert m('short')=='****'; assert m('DUMMY-KEY-0123456789ABCD')=='DUMM****ABCD'; assert m('DUMMY-KEY-0123456789ABCD').count('*')==4; assert '0123456789' not in m('DUMMY-KEY-0123456789ABCD'); print('OK')"` 输出 OK
    - `grep -c 'mask_secret' tests/test_util.py` ≥ 1
    - `grep -cE 'sk-[A-Za-z0-9]' tests/test_util.py` 为 0
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_util.py -q` 退出码 0
    - `git diff -U0 src/vela/util/textutil.py | grep '^[+-][^+-]' | grep -c 'def mask_vin\|def estimate_tokens\|def canon_rules_version'` 为 0（既有函数未被改动）
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'def mask_secret' src/vela/util/textutil.py) -eq 1 && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -c "from vela.util.textutil import mask_secret as m; assert m('')==''; assert m('short')=='****'; assert m('DUMMY-KEY-0123456789ABCD')=='DUMM****ABCD'; assert m('DUMMY-KEY-0123456789ABCD').count('*')==4; assert '0123456789' not in m('DUMMY-KEY-0123456789ABCD'); print('OK')" && test $(grep -cE 'sk-[A-Za-z0-9]' tests/test_util.py) -eq 0 && test $(git diff -U0 src/vela/util/textutil.py | grep '^[+-][^+-]' | grep -c 'def mask_vin\|def estimate_tokens\|def canon_rules_version') -eq 0 && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_util.py -q</automated>
  </verify>
  <done>`mask_secret` 就位并被单测钉住六条行为；`textutil.py` 既有函数零改动。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新建 env_checks.yaml 规则表与 EnvChecker 消费模块</name>
  <files>config/env_checks.yaml, src/vela/envcheck.py, tests/test_envcheck.py</files>
  <read_first>
    - src/vela/gateway/redact.py 全文 43 行（**照抄 `Redactor.__init__` 的构造器形状**：`cfg` 可注入 + 默认 `load_yaml` + 构造期预编译正则；以及第 28-43 行逐规则应用 + 累计命中的返回形状）
    - tests/test_gateway.py 第 15-21 行（靠 cfg 注入脱离真实 `config/` 目录测 `Redactor` 的写法——本任务的 `EnvChecker` 单测同理）
    - config/llm.yaml 第 56-66 行 `redaction.rules`（扁平 `{name, pattern, repl}` 规则表排版）
    - config/parsers.yaml 第 1-15 行（文件头分隔线 + 中文用途说明 + 「新增 = 追加一条 YAML，无需改代码」声明；以及多字段规则条目的排版）
    - src/vela/evidence/qa.py 第 41-56 行与第 69-71 行（`{name, ok, detail}` 三元组是既定字段名，本模块返回结构须对齐）
    - src/vela/config.py 第 28-34 行 `load_yaml`（有 `lru_cache`，测试内改 YAML 文件不生效，只能靠注入）与第 125-143 行 `config_hash()`（显式枚举三个 YAML，**本任务禁止改动**）
    - src/vela/gateway/volcengine.py 第 6-10 行（`base_url` 正确形式与 `ep-xxxxxxxx` 形态的事实基准）
    - docs/LLM_PRODUCTION.md 第 34-39 行（同一基准的第二处出处）
    - .planning/phases/01-llm/01-CONTEXT.md D-16 全文（含「重心已转移」的说明与「不进 config_hash」要求）
  </read_first>
  <behavior>
    - `VELA_ARK_BASE_URL` 为 `https://ark.cn-beijing.volces.com/api/plan/v3` 且 provider 为 volcengine → 该检查项 `ok` 为 False，`detail` 含正确形式 `/api/v3`
    - `VELA_ARK_BASE_URL` 为 `https://ark.cn-beijing.volces.com/api/v3` → 该项 `ok` 为 True
    - 任一被检查变量的值含 `   # 注释` 形态 → 触发 `inline_comment_residue` 规则，`ok` 为 False
    - `display: masked` 的变量，其 `detail` 中不含完整原值，且形如 `DUMM****ABCD`
    - provider 为 `mock` 时，仅 volcengine 必填的变量未设置**不**判为失败
    - provider 为 `volcengine` 时，`VELA_ARK_API_KEY` 未设置 → 该项 `ok` 为 False 且 `detail` 给出获取方式提示
    - `EnvChecker(cfg=...)` 与 `run(environ=...)` 均可注入，单测不依赖真实 `config/` 与真实进程环境
  </behavior>
  <action>
1. **新建 `config/env_checks.yaml`**（D-16）。文件头按 `config/parsers.yaml` 的惯例写分隔线 + 中文用途说明，并**必须显式声明**：本文件为纯诊断用途，**不进 `config_hash`**，改它不影响推理结果与证据包指纹；新增检查 = 追加一条 YAML，无需改代码。

   schema（字段命名属 Claude's Discretion，此处锁定为以下形状）：
   - 顶层 `version: "1.0"`
   - 顶层 `value_hygiene`: 通用值形态规则列表，对下方 `variables` 中每个**已设置**的值统一应用。至少三条：
     - `inline_comment_residue`（pattern 匹配「空白 + `#`」）：提示「值内含疑似行尾注释残留；python-dotenv 只会剥离未加引号值的行尾注释，加引号的值不会被剥离——请把注释移到变量上一行」
     - `edge_whitespace`（pattern 匹配首尾空白）
     - `wrapping_quotes`（pattern 匹配值被成对引号包裹）：提示引号会成为值的一部分
   - 顶层 `variables`: 每条含 `name` / `display`（`plain` | `masked`）/ `required_for_providers`（provider 名列表，空列表表示任何 provider 下都不必填）/ `pattern`（形态正则，可为空表示不校验形态）/ `hint`（不匹配时的中文修复指引，必须指出下一步动作）。
   - 至少覆盖八个变量：`VELA_LLM_PROVIDER`（枚举 `mock|volcengine|openai_compat`）、`VELA_ARK_BASE_URL`（`required_for_providers: [volcengine]`，pattern 锚定以 `/api/v3` 结尾，hint 写明正确形式 `https://ark.cn-beijing.volces.com/api/v3` 并点名 `/api/plan/v3` 是常见笔误）、`VELA_ARK_API_KEY`（`display: masked`，`required_for_providers: [volcengine]`，hint 指向方舟控制台 API Key 页）、`VELA_ARK_MODEL`（`required_for_providers: [volcengine]`，pattern 容纳 `ep-xxxxxxxx` 与普通模型名两种形态，hint 说明填推理接入点 ID）、`VELA_ARK_EMBED_MODEL`、`VELA_OPENAI_BASE_URL` / `VELA_OPENAI_API_KEY`（`display: masked`）/ `VELA_OPENAI_MODEL`（`required_for_providers: [openai_compat]`）、`VELA_PROFILE`（枚举 `poc|production`）。
   - ⚠️ 文件中禁止出现任何真实密钥或接入点 ID 的样例值。

2. **新建 `src/vela/envcheck.py`**（模块路径裁定：放 `src/vela/` 顶层而非 `gateway/`，因为它跨越模型网关与运行期变量，且直接被 CLI 消费）。首行中文 docstring 说明用途与「纯诊断、不进指纹」。
   - `class EnvChecker`，构造器**照抄 `Redactor.__init__` 形状**：`def __init__(self, cfg: dict | None = None)`，`cfg = cfg if cfg is not None else load_yaml("env_checks.yaml")`，构造期把 `value_hygiene` 与 `variables` 中的 pattern 用 `re.compile` 预编译。可注入是单测前提（`load_yaml` 有 `lru_cache`，测试内改 YAML 文件不生效）。
   - `def run(self, provider: str, environ: Mapping[str, str] | None = None) -> list[dict]`：`environ` 默认 `os.environ`，可注入。返回 `list[dict]`，每项字段为 `{"name": str, "ok": bool, "detail": str, "kind": "local"}`——`name` / `ok` / `detail` 三个键名对齐 `src/vela/evidence/qa.py` 的既定契约；`kind` 固定 `"local"`，供 Plan 06 的 doctor 做 D-14 退出码分层（形态错误属本地硬错误，返 1）。
   - 每个变量的判定顺序：未设置 → 若 `provider` 在 `required_for_providers` 中则 `ok=False` 并给 hint，否则 `ok=True` 且 detail 标「未设置（当前 provider 不需要）」；已设置 → 先跑 `value_hygiene` 全部规则，任一命中即 `ok=False`；再跑 `pattern`，不匹配即 `ok=False` 并附 hint。
   - `detail` 中呈现变量值时：`display == "masked"` 走 `mask_secret(value)`，`display == "plain"` 明文（D-17：`base_url` 与 `ep-xxxx` 不是凭证且看不到就无法排查）。**掩码在此处统一施加**，保证 Plan 06 的人读与 `--json` 两条通道拿到的是同一份已掩码数据（D-18）。
   - 不使用 `logging`，不 `print`，不做任何 I/O（除构造期的 `load_yaml`）。
   - ⚠️ **禁止改动 `src/vela/config.py::config_hash()`**：它显式枚举 `pipeline.yaml` / `parsers.yaml` / `ota_phases.yaml`，新增 `env_checks.yaml` 零改动即满足 D-16。

3. **新建 `tests/test_envcheck.py`**：首行中文 docstring。全部用例通过注入 `cfg` 与 `environ` 构造，**不读真实 `config/env_checks.yaml`、不读真实 `os.environ`**。覆盖 `<behavior>` 列出的七条断言，外加一条「全部合规时所有项 `ok` 均为 True」。样本值一律 `DUMMY-` 前缀，禁止真实凭证形态。另加一条断言 `set(item) == {"name","ok","detail","kind"}`，把返回契约钉死，防止 Plan 06 消费时字段漂移。
  </action>
  <acceptance_criteria>
    - `config/env_checks.yaml` 存在；`.venv/bin/python3 -c "import yaml;d=yaml.safe_load(open('config/env_checks.yaml'));assert set(d)>={'version','value_hygiene','variables'};assert len(d['variables'])>=8;assert any(v['name']=='VELA_ARK_BASE_URL' for v in d['variables']);print('OK')"` 输出 OK
    - `grep -c 'config_hash' config/env_checks.yaml` ≥ 1（文件头声明了不进指纹）
    - `grep -cE 'sk-[A-Za-z0-9]{8}|ep-2[0-9]{7}' config/env_checks.yaml` 为 0（无真实凭证/接入点样例）
    - `grep -c 'class EnvChecker' src/vela/envcheck.py` 为 1；`grep -c 'cfg if cfg is not None else load_yaml' src/vela/envcheck.py` 为 1（照抄 Redactor 构造器形状）
    - `grep -c 'mask_secret' src/vela/envcheck.py` ≥ 1
    - `grep -vE '^\s*#' src/vela/envcheck.py | grep -c 'print(\|import logging'` 为 0
    - `git diff src/vela/config.py` 无输出（`config_hash()` 与 `load_yaml` 均未被改动）
    - `grep -c 'def test_' tests/test_envcheck.py` ≥ 8
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_envcheck.py -q` 退出码 0
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -c "from vela.envcheck import EnvChecker; rs=EnvChecker().run('volcengine', {'VELA_ARK_BASE_URL':'https://ark.cn-beijing.volces.com/api/plan/v3'}); bad=[r for r in rs if r['name']=='VELA_ARK_BASE_URL']; assert bad and not bad[0]['ok'] and '/api/v3' in bad[0]['detail']; print('OK')"` 输出 OK
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python3 -c "import yaml;d=yaml.safe_load(open('config/env_checks.yaml'));assert set(d)>={'version','value_hygiene','variables'};assert len(d['variables'])>=8;assert any(v['name']=='VELA_ARK_BASE_URL' for v in d['variables']);print('OK')" && test $(grep -c 'config_hash' config/env_checks.yaml) -ge 1 && test $(grep -cE 'sk-[A-Za-z0-9]{8}|ep-2[0-9]{7}' config/env_checks.yaml) -eq 0 && test $(grep -c 'class EnvChecker' src/vela/envcheck.py) -eq 1 && test $(grep -c 'cfg if cfg is not None else load_yaml' src/vela/envcheck.py) -eq 1 && test $(grep -c 'mask_secret' src/vela/envcheck.py) -ge 1 && test $(grep -vE '^\s*#' src/vela/envcheck.py | grep -c 'print(\|import logging') -eq 0 && test -z "$(git diff src/vela/config.py)" && test $(grep -c 'def test_' tests/test_envcheck.py) -ge 8 && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_envcheck.py -q && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -c "from vela.envcheck import EnvChecker; rs=EnvChecker().run('volcengine', {'VELA_ARK_BASE_URL':'https://ark.cn-beijing.volces.com/api/plan/v3'}); bad=[r for r in rs if r['name']=='VELA_ARK_BASE_URL']; assert bad and not bad[0]['ok'] and '/api/v3' in bad[0]['detail']; print('OK')"</automated>
  </verify>
  <done>规则表与检查器就位，`cfg`/`environ` 双注入，返回契约为 `{name, ok, detail, kind}`；`/api/plan/v3` 能被判为异常并给出正确形式提示；`config_hash()` 零改动。</done>
</task>

<task type="auto">
  <name>Task 3: 清理 .env.example 行尾注释并复核 .gitignore 覆盖</name>
  <files>.env.example</files>
  <read_first>
    - .env.example 全文 27 行（第 6-15 行是**同一文件里已有的正确写法**：注释独占一行；第 22-27 行是待改造的五处行尾注释）
    - .gitignore 第 6-14 行（确认 `.env` / `.env.*` 被忽略且 `!.env.example` 例外仍在）
    - .planning/phases/01-llm/01-CONTEXT.md D-16（`.env.example` 仍需清理——ENV-04 字面要求，且避免误导）
    - .planning/ROADMAP.md 第 32 行（成功判据 4：`.env.example` 不再含会被朴素解析器吃进值的行尾注释）
  </read_first>
  <action>
1. 把 `.env.example` 第 22-27 行的五处行尾注释改为「注释独占一行、置于变量上一行」的形状，与本文件第 6-15 行既有写法一致：
   - `VELA_WORKSPACE=./workspace` 的「产物根目录」
   - `VELA_PROFILE=poc` 的「poc | production（预算档位）」
   - `VELA_TENANT=demo-tenant` 的「强制租户谓词的租户标识」
   - `PYTHONHASHSEED=0` 的「确定性要求：固定哈希种子」
   - `VELA_LOG_LEVEL=INFO` 若无注释则保持原样
   改造后，`=` 之后到行尾之间不得再出现 ` #`。
2. 在文件头部（第 1-4 行的说明块）补一句：`.env` 由 `src/vela/config.py` 在导入时自动加载（`override=False`，已存在的进程环境变量优先），无需 `source .env` 或手工 `export`——对应 D-06 / D-08，同时纠正 `docs/LLM_PRODUCTION.md` 里过时的 `source .env` 口径（该文档归 Plan 07 改）。
3. 追加两个此前缺失但 `models_for()` 会读取的可选变量并配上独占行注释：`VELA_ARK_MODEL_FALLBACK=`（主接入点失败时的降级接入点，对应 `config/llm.yaml` 的 `fallback_chain`）与 `VELA_ARK_MODEL_PLANNER=`（按逻辑模型分别指定接入点，`_VERIFIER` / `_REPORTER` / `_DISTILLER` 同理，在注释中说明）。
4. 复核 `.gitignore`：确认 `.env` 与 `.env.*` 被忽略、`!.env.example` 例外仍在。**若已正确覆盖则不改动 `.gitignore`**（本计划的 `files_modified` 未包含它）；若发现覆盖不全，停止并在 SUMMARY 中报告，不要自行扩大改动面。
5. ⚠️ 所有 key 类变量的值必须保持为**空**（`VELA_ARK_API_KEY=`、`VELA_OPENAI_API_KEY=`），不得填入任何真实或形似真实的凭证；`VELA_ARK_BASE_URL` 保持既有的正确默认值 `https://ark.cn-beijing.volces.com/api/v3`。
  </action>
  <acceptance_criteria>
    - `grep -cE '^[A-Z_]+=[^#]* #' .env.example` 为 0（`=` 之后到行尾无行尾注释）
    - `grep -c 'VELA_ARK_MODEL_FALLBACK' .env.example` 为 1，`grep -c 'VELA_ARK_MODEL_PLANNER' .env.example` 为 1
    - `grep -c 'override=False' .env.example` ≥ 1（头部已说明自动加载语义）
    - `grep -cE '^VELA_ARK_API_KEY=.+|^VELA_OPENAI_API_KEY=.+' .env.example` 为 0（密钥值为空）
    - `grep -c 'https://ark.cn-beijing.volces.com/api/v3' .env.example` 为 1
    - `git check-ignore -q .env` 退出码 0；`git check-ignore -q .env.example` 退出码非 0（样例仍被追踪）
    - `git status --porcelain .env` 无输出（真实 `.env` 未被意外加入版本库）
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -c "from dotenv import dotenv_values; v=dotenv_values('.env.example'); assert all('#' not in (x or '') for x in v.values()), v; print('OK', len(v))"` 输出 OK
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -cE '^[A-Z_]+=[^#]* #' .env.example) -eq 0 && test $(grep -c 'VELA_ARK_MODEL_FALLBACK' .env.example) -eq 1 && test $(grep -c 'VELA_ARK_MODEL_PLANNER' .env.example) -eq 1 && test $(grep -c 'override=False' .env.example) -ge 1 && test $(grep -cE '^VELA_ARK_API_KEY=.+|^VELA_OPENAI_API_KEY=.+' .env.example) -eq 0 && test $(grep -c 'https://ark.cn-beijing.volces.com/api/v3' .env.example) -eq 1 && git check-ignore -q .env && ! git check-ignore -q .env.example && test -z "$(git status --porcelain .env)" && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -c "from dotenv import dotenv_values; v=dotenv_values('.env.example'); assert all('#' not in (x or '') for x in v.values()), v; print('OK', len(v))"</automated>
  </verify>
  <done>`.env.example` 无行尾注释、密钥值为空、补齐降级与逐逻辑模型变量；`.gitignore` 对 `.env` 的覆盖已复核无误。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `os.environ` 中的凭证 → `EnvChecker.run()` 返回的 `detail` | 检查结果会同时流向 doctor 人读输出与 `--json`，是凭证外泄的主要新增路径 |
| `.env.example` → 版本库 | 样例文件是被追踪的，任何写入其中的值都会被提交 |
| `config/env_checks.yaml` → `config_hash` | 若被纳入指纹，诊断用配置的每次调整都会让历史证据包无法比对 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `EnvChecker.run()` 的 `detail` 字段 | mitigate | 掩码在 `envcheck.py` 内统一施加（`display: masked` → `mask_secret`），而非交给渲染层；这样 Plan 06 的人读与 `--json` 两条通道天然拿到同一份已掩码数据（D-18），不存在「只掩了一边」的可能 |
| T-05-02 | Information Disclosure | `.env.example` / `config/env_checks.yaml` 中的样例值 | mitigate | acceptance 断言两文件均无 `sk-` / `ep-2xxxxxxx` 形态串，且 `VELA_*_API_KEY=` 右侧为空 |
| T-05-03 | Information Disclosure | 真实 `.env` 被提交 | mitigate | acceptance 用 `git check-ignore -q .env` 与 `git status --porcelain .env` 双重断言；Task 3 明确禁止扩大改动面去动 `.gitignore` |
| T-05-04 | Tampering | `config_hash()` 被顺带纳入 `env_checks.yaml` | mitigate | D-16 要求零改动即满足；acceptance 用 `git diff src/vela/config.py` 为空断言，杜绝「顺手加进 payload」 |
| T-05-05 | Information Disclosure | 测试夹具 | mitigate | `tests/test_envcheck.py` 与 `tests/test_util.py` 的样本值一律 `DUMMY-` 前缀；不读真实 `os.environ`（`run(environ=...)` 注入） |
| T-05-06 | Spoofing | 形态规则被硬编码在业务代码 | mitigate | 全部变量名、正则与提示文案进 `config/env_checks.yaml`（AGENTS.md 铁律 2 配置驱动）；`envcheck.py` 中不出现任何具体变量名字面量 |
</threat_model>

<verification>
1. `mask_secret` 六条行为断言通过
2. `config/env_checks.yaml` 可被 `yaml.safe_load` 解析，含 `version` / `value_hygiene` / `variables`，变量数 ≥ 8
3. `EnvChecker` 对 `/api/plan/v3` 判为异常且 detail 含 `/api/v3`
4. `git diff src/vela/config.py` 为空（`config_hash` 未被触碰）
5. `.env.example` 无行尾注释，密钥值为空，`dotenv_values` 解析后无值含 `#`
6. `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q` 退出码 0，passed 不低于基线
</verification>

<success_criteria>
- `mask_secret` 在 `util/textutil.py` 中就位，前 4 后 4、固定 4 星、短值全掩
- `config/env_checks.yaml` 为唯一规则来源，业务代码零硬编码变量名
- `EnvChecker` 支持 `cfg` 与 `environ` 双注入，返回 `{name, ok, detail, kind}`
- `env_checks.yaml` 不在 `config_hash` payload 中
- `.env.example` 满足 ROADMAP 成功判据 4
</success_criteria>

<output>
Create `.planning/phases/01-llm/01-05-SUMMARY.md` when done（须记录：`EnvChecker.run()` 的返回字段契约与 `env_checks.yaml` 的最终 schema，供 Plan 06 的 `cmd_doctor` 直接消费）
</output>
