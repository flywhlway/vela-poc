---
phase: 01-llm
plan: 07
type: execute
wave: 4
depends_on: ["01-04", "01-06"]
files_modified:
  - tests/test_realllm.py
  - docs/LLM_PRODUCTION.md
autonomous: true
requirements: [ENV-02]

must_haves:
  truths:
    - "存在一条 realllm 标记的端到端用例，断言真实 LLM 下诊断跑完不中途报错、报告非空、含至少一个 [[EV:row_hash]] 引用"
    - "该用例默认不被 make test 收集，只有显式 -m realllm 才运行"
    - "该用例在缺少真实凭证时 skip 而非 fail"
    - "该用例不断言诊断结论正确（ROADMAP 成功判据 2 只要求链路通）"
    - "生产接入文档不再指导用户 source .env，不再提及已删除的 retry_backoff_s"
    - "生产接入文档记录了 vela doctor 的三个新参数与退出码语义"
  artifacts:
    - path: "tests/test_realllm.py"
      provides: "ENV-02 的真实端到端验收用例"
      contains: "pytest.mark.realllm"
      min_lines: 40
    - path: "docs/LLM_PRODUCTION.md"
      provides: "与 .env 自动加载 / SDK 化 / doctor 新能力对齐的接入文档"
      contains: "vela doctor --json"
  key_links:
    - from: "tests/test_realllm.py"
      to: "src/vela/agent/citations.py::CITE_RX"
      via: "复用权威引用正则断言报告含引用"
      pattern: "CITE_RX"
    - from: "tests/test_realllm.py"
      to: "pyproject.toml addopts -m 'not realllm'"
      via: "标记默认排除机制"
      pattern: "realllm"
---

<objective>
按 D-19 落地 ENV-02 的可复现验收手段：新增 `realllm` 标记的端到端用例，断言真实火山引擎方舟环境下 `vela agent diagnose` 跑完不中途报错、报告非空且含至少一个 `[[EV:row_hash]]` 引用；同时把 `docs/LLM_PRODUCTION.md` 与本阶段的三项行为变更（`.env` 自动加载、SDK 化、doctor 新能力）对齐。

Purpose: ENV-02 的验收若只靠人工跑一次，下次谁也无法复现。把它固化成一条标记用例，既是本阶段的验收凭据，也是后续 Phase 2~6 在真实环境上做度量前的冒烟入口。文档对齐则是 AGENTS.md「行为变化须同步相关文档」的完成判据要求——`source .env` 与 `retry_backoff_s` 两处口径在本阶段后已经是错的。

Output: `tests/test_realllm.py` + 对齐后的 `docs/LLM_PRODUCTION.md`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-llm/01-CONTEXT.md
@.planning/phases/01-llm/01-PATTERNS.md
@.planning/phases/01-llm/01-03-SUMMARY.md
@.planning/phases/01-llm/01-04-SUMMARY.md
@.planning/phases/01-llm/01-06-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 tests/test_realllm.py 端到端验收用例</name>
  <files>tests/test_realllm.py</files>
  <read_first>
    - tests/test_cli_and_server.py 第 58-64 行（`test_cli_agent_diagnose_end_to_end` 的形状：`built` fixture + `main(["agent","diagnose",...])` + `capsys` + rc 断言；本用例照此写但**不断言结论字符串**）
    - tests/conftest.py（`built` fixture 的产物结构：`db` / `ws` / `archive` 三个键；以及 Plan 03 已把 `VELA_LLM_PROVIDER` 无条件锁为 `mock`——本用例必须 `monkeypatch.setenv` 回 `volcengine`）
    - src/vela/agent/citations.py 第 15 行 `CITE_RX = re.compile(r"\[\[EV:([0-9a-fA-F]{8,32})\]\]")`（引用格式的权威来源，直接复用，不要在测试里另写正则）
    - src/vela/cli.py 第 88-118 行 `cmd_agent`（诊断产物的落盘位置与 rc 语义：`return 0 if st.status == "answered" else 3`）
    - pyproject.toml `[tool.pytest.ini_options]`（Plan 01 已注册 `realllm` 标记并加 `-m 'not realllm'`）
    - .planning/phases/01-llm/01-CONTEXT.md D-19（断言范围：跑完不报错 + 报告非空 + 至少一个引用；**不断言诊断结论正确**）
    - .planning/ROADMAP.md 第 30 行（成功判据 2 原文）
  </read_first>
  <behavior>
    - 用例被 `pytest.mark.realllm` 标记，`pytest tests/ -q` 默认不收集它（deselected）
    - `pytest -m realllm` 能收集到它
    - `VELA_ARK_API_KEY` 或 `VELA_ARK_MODEL` 未设置时 → `pytest.skip`（不是 fail）
    - 凭证齐备时：`main(["agent","diagnose", ...])` 返回码非异常终止；报告文本非空；`CITE_RX` 在报告中至少命中一次
    - 用例不断言任何具体根因标签或诊断结论文本
  </behavior>
  <action>
新建 `tests/test_realllm.py`，首行中文 docstring 说明：ENV-02 真实火山引擎方舟端到端验收；**会产生真实付费 API 调用**；默认被 `pyproject.toml` 的 `addopts = "... -m 'not realllm'"` 排除，只有显式 `VELA_LLM_PROVIDER=volcengine pytest -m realllm` 才运行。

实现要点：
- 模块级 `pytestmark = pytest.mark.realllm`，让文件内全部用例统一带标记。
- 一个模块级 fixture 做前置守卫：若 `os.environ.get("VELA_ARK_API_KEY")` 或 `os.environ.get("VELA_ARK_MODEL")` 为空 → `pytest.skip("未配置真实火山引擎凭证（VELA_ARK_API_KEY / VELA_ARK_MODEL），跳过 ENV-02 实测")`。凭证来自 `.env` 自动加载（Plan 03 的 D-06），不需要手工 export——这正是 ENV-01 在测试侧成立的证明，写进 docstring。
- 主用例 `test_real_llm_diagnose_produces_cited_report(built, tmp_path, monkeypatch, capsys)`：
  1. `monkeypatch.setenv("VELA_LLM_PROVIDER", "volcengine")` —— conftest 已把它无条件锁为 `mock`（D-11），需要真实 LLM 的用例自行改回，这是 D-11 明确规定的逃生方式。
  2. 调 `main(["agent", "diagnose", "--db", str(built["db"]), "--workspace", str(tmp_path / "realllm_ws"), "--session-id", "REALLLM-ENV02"])`。
  3. 断言返回码不是异常终止：rc 应为 `0`（answered）或 `3`（未作答但链路走完）。D-19 明确只要求「链路不因环境问题中途报错」，因此**不把 rc==0 作为硬断言**；若 rc 为其他值或抛异常即判失败。
  4. 从 `capsys` 与 workspace 产物中取报告文本（按 `cmd_agent` 的实际落盘路径定位，不要猜路径），断言 `len(report_text.strip()) > 0`。
  5. `from vela.agent.citations import CITE_RX`，断言 `CITE_RX.search(report_text)` 非 None（至少一个 `[[EV:row_hash]]`）。
  6. **禁止**断言任何具体根因标签（例如 `uds_nrc_programming_failure`）或结论文本——ROADMAP 成功判据 2 明确只要求链路通。
- 可选补一个 `test_real_llm_doctor_connectivity_all_green`：在真实凭证下跑 `main(["doctor", "--json"])`，`json.loads` 后断言 `local_ok` 为真、`provider == "volcengine"`、`probed` 为真。它把 ENV-03 的真实验收也固化下来。
- ⚠️ **安全**：用例中禁止 `print` 或断言任何环境变量的值；`capsys` 捕获的输出不得整体写入任何持久化文件；不得把凭证写进 `--session-id` 或工作区路径名。
  </action>
  <acceptance_criteria>
    - `tests/test_realllm.py` 存在且行数 ≥ 40
    - `grep -c 'pytest.mark.realllm' tests/test_realllm.py` ≥ 1
    - `grep -c 'CITE_RX' tests/test_realllm.py` ≥ 1（复用权威正则而非自写）
    - `grep -c 'uds_nrc_programming_failure' tests/test_realllm.py` 为 0（不断言结论）
    - `grep -cE 'sk-[A-Za-z0-9]{8}|ep-2[0-9]{7}' tests/test_realllm.py` 为 0
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q` 的输出含 `deselected`，且 passed 数 ≥ `01-BASELINE.md` 的 `baseline_pytest_passed`（默认排除生效）
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest -m realllm --collect-only -q` 能收集到 ≥ 1 条用例（显式覆盖生效）
    - 在无 `VELA_ARK_API_KEY` 的环境下 `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest -m realllm -q` 退出码 0 且结果为 skipped（不是 failed，且零网络调用）
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'pytest.mark.realllm' tests/test_realllm.py) -ge 1 && test $(grep -c 'CITE_RX' tests/test_realllm.py) -ge 1 && test $(grep -c 'uds_nrc_programming_failure' tests/test_realllm.py) -eq 0 && test $(grep -cE 'sk-[A-Za-z0-9]{8}|ep-2[0-9]{7}' tests/test_realllm.py) -eq 0 && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q 2>&1 | grep -q deselected && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest -m realllm --collect-only -q 2>&1 | grep -qE 'test_real_llm' && env -u VELA_ARK_API_KEY VELA_ARK_API_KEY= PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest -m realllm -q</automated>
  </verify>
  <done>`realllm` 用例就位：默认被排除、显式可跑、无凭证时 skip、断言范围严格限定为「链路通 + 报告非空 + 含引用」。</done>
</task>

<task type="auto">
  <name>Task 2: 对齐 docs/LLM_PRODUCTION.md 的三处过时口径并补两节新能力</name>
  <files>docs/LLM_PRODUCTION.md</files>
  <read_first>
    - docs/LLM_PRODUCTION.md 全文（重点：第 9-11 行「177 个用例全部基于 mock」；第 26-44 行 §2 配置环境变量含第 42 行 `source .env` 的过时指引；第 62-69 行 §4 故障降级链含第 68-69 行 `retry_backoff_s` 的过时描述；第 71-76 行 §5 运行）
    - .planning/phases/01-llm/01-03-SUMMARY.md（`.env` 自动加载的实际行为与 `dotenv_report()` 结构）
    - .planning/phases/01-llm/01-04-SUMMARY.md（SDK 化后 `timeout_s` / `max_retries` 的映射关系，以及被删除的三个死键）
    - .planning/phases/01-llm/01-06-SUMMARY.md（doctor 的 `--json` 顶层键清单与退出码语义）
    - config/llm.yaml（Plan 04 清理后的实际内容，文档描述必须与之一致）
    - .env.example（Plan 05 清理后的实际内容）
  </read_first>
  <action>
把 `docs/LLM_PRODUCTION.md` 与本阶段的行为变更逐处对齐（AGENTS.md 完成判据：行为变化须同步相关文档）：

1. **§2 配置环境变量（第 41-43 行）**：删除 `source .env       # 注意：是 source，不是仅 cat...` 这条指引及其说明。改为：`.env` 由 `src/vela/config.py` 在模块导入时自动加载一次（`override=False`，已存在的进程环境变量恒定优先），任意 CLI 子命令与 `pytest` 均自动生效，**无需 `source` 也无需手工 `export`**；临时覆盖走 shell 前置赋值 `VELA_LLM_PROVIDER=volcengine vela ...`。保留其后的 `vela doctor` 步骤。
2. **§4 故障降级链（第 68-69 行）**：`providers.volcengine.max_retries` / `retry_backoff_s` 控制重试次数与退避间隔 → 改为：`timeout_s` 与 `max_retries` 分别映射到 openai SDK 客户端的 `timeout` 与 `max_retries`；退避策略由 SDK 内置且不可配，`retry_backoff_s` 已随 SDK 化从 `config/llm.yaml` 删除；`chat_path` / `embed_path` 同样删除，请求路径由 SDK 固定，因此 `base_url` **必须含版本前缀**（方舟为 `/api/v3`）。
3. **§为什么默认是 mock（第 9-11 行）**：「177 个用例全部基于 mock」一句补充说明，新增的 `realllm` 标记用例默认被 `addopts` 排除，`make test` 仍然零 API 调用、零配额消耗。
4. **新增一节「环境自检（`vela doctor`）」**，置于 §2 之后：说明四项连通性自检（端点可达 / 鉴权有效 / 模型可用 / 四个逻辑模型映射完整）、探测时机按 provider 自动判定（mock 跳过、volcengine 自动联网）、`--offline` / `--online` 双向强制覆盖、`--json` 结构化输出及其顶层键、以及**退出码语义**：本地硬错误（配置缺失 / 必需依赖缺失 / `.env` 形态错误）返 `1`；连通性四项任一失败输出显著标 ❌ 但返 `0`，以免一次限流或临时断网中断 `run_all.sh` 的整条链路。同时说明 API key 在两种输出形态下均只显示前 4 后 4，`base_url` 与 `ep-xxxx` 接入点 ID 明文展示以便对照排查。
5. **新增一节「真实环境验收（`pytest -m realllm`）」**：给出命令 `VELA_LLM_PROVIDER=volcengine .venv/bin/python3 -m pytest -m realllm -q`（需先 `PYTHONPATH=src VELA_CONFIG_DIR=config`），说明它会产生**真实付费调用**、缺凭证时自动 skip、断言范围只覆盖「链路跑完 + 报告非空 + 含至少一个 `[[EV:row_hash]]` 引用」而不断言诊断结论正确。
6. ⚠️ 文档中新增/修改的所有示例值必须为占位形态（`<你的 API Key>`、`ep-20260101000000-xxxxx`），禁止写入任何真实凭证或真实接入点 ID。
  </action>
  <acceptance_criteria>
    - `grep -c 'source .env' docs/LLM_PRODUCTION.md` 为 0
    - `grep -c 'retry_backoff_s' docs/LLM_PRODUCTION.md` ≤ 1 且若出现必为「已删除」语境（人工核对该行）
    - `grep -c 'override=False' docs/LLM_PRODUCTION.md` ≥ 1
    - `grep -c 'vela doctor --json' docs/LLM_PRODUCTION.md` ≥ 1
    - `grep -c 'realllm' docs/LLM_PRODUCTION.md` ≥ 2
    - `grep -c '\-\-offline' docs/LLM_PRODUCTION.md` ≥ 1 且 `grep -c '\-\-online' docs/LLM_PRODUCTION.md` ≥ 1
    - `grep -c '/api/v3' docs/LLM_PRODUCTION.md` ≥ 2（base_url 版本前缀要求已记录）
    - `grep -cE 'sk-[A-Za-z0-9]{8}' docs/LLM_PRODUCTION.md` 为 0
    - 文档中提到的每个 `config/llm.yaml` 配置键都实际存在：`for k in timeout_s max_retries base_url_env api_key_env model_env fallback_chain; do grep -q "$k" config/llm.yaml || exit 1; done`
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'source .env' docs/LLM_PRODUCTION.md) -eq 0 && test $(grep -c 'override=False' docs/LLM_PRODUCTION.md) -ge 1 && test $(grep -c 'vela doctor --json' docs/LLM_PRODUCTION.md) -ge 1 && test $(grep -c 'realllm' docs/LLM_PRODUCTION.md) -ge 2 && test $(grep -c '\-\-offline' docs/LLM_PRODUCTION.md) -ge 1 && test $(grep -c '\-\-online' docs/LLM_PRODUCTION.md) -ge 1 && test $(grep -c '/api/v3' docs/LLM_PRODUCTION.md) -ge 2 && test $(grep -cE 'sk-[A-Za-z0-9]{8}' docs/LLM_PRODUCTION.md) -eq 0 && for k in timeout_s max_retries base_url_env api_key_env model_env fallback_chain; do grep -q "$k" config/llm.yaml || exit 1; done && echo OK</automated>
  </verify>
  <done>生产接入文档不再指导 `source .env`、不再描述已删除的配置键，并新增 doctor 自检与 realllm 验收两节。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `realllm` 用例 → 真实付费端点 | 这是仓库中唯一会真实外发数据与产生费用的测试路径 |
| 诊断报告（含真实日志内容）→ 测试断言与 capsys 输出 | 报告文本可能含 VIN / GPS 等敏感字段的原始形态 |
| 文档示例 → 版本库 | 接入文档最容易被顺手填入真实 key 或接入点 ID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `realllm` 用例的出站数据 | mitigate | 用例走 `main(["agent","diagnose",...])` 完整链路，出站必经 `LLMGateway.chat()` 的 `gateway/redact.py` 脱敏（Plan 04 已断言 `base.py` 零改动，不存在绕过路径）；用例不新建任何直连 SDK 的旁路 |
| T-07-02 | Information Disclosure | `capsys` 捕获的报告文本 | mitigate | 用例只做正则命中与非空断言，禁止把捕获输出写入任何持久化文件；禁止断言或打印环境变量值 |
| T-07-03 | Tampering | 付费用例被误纳入默认收集 | mitigate | 双重缓解：`pyproject.toml` 的 `-m 'not realllm'`（Plan 01）+ 无凭证自动 skip；acceptance 断言 `pytest tests/ -q` 输出含 `deselected`，且无凭证时 `-m realllm` 结果为 skipped 而非发起调用 |
| T-07-04 | Information Disclosure | `docs/LLM_PRODUCTION.md` 示例值 | mitigate | 新增示例一律占位形态；acceptance 断言文档中无 `sk-` 形态串 |
| T-07-05 | Repudiation | 文档描述与实际配置不一致 | mitigate | acceptance 用循环断言文档提及的每个 `config/llm.yaml` 键都真实存在，防止文档写出已删除的键 |
| T-07-06 | Denial of Service | realllm 用例被 CI 反复触发 | accept | 默认排除即为缓解；本项目无 CI 配置（`make test` 是唯一入口），触发需显式 `-m realllm` |
</threat_model>

<verification>
1. `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q` 退出码 0 且输出含 `deselected`
2. `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest -m realllm --collect-only -q` 收集到 ≥ 1 条
3. 无凭证时 `-m realllm` 结果为 skipped，退出码 0，零网络调用
4. `docs/LLM_PRODUCTION.md` 中 `source .env` 计数为 0，`realllm` 计数 ≥ 2
5. 文档提及的 `config/llm.yaml` 配置键全部实际存在
</verification>

<success_criteria>
- `realllm` 标记用例存在，断言范围严格限定为 ROADMAP 成功判据 2 的三条（链路跑完 / 报告非空 / 含至少一个 `[[EV:row_hash]]`）
- 默认排除与显式覆盖两条路径均被验证
- 无凭证时 skip 而非 fail
- 生产接入文档与 `.env` 自动加载、SDK 化、doctor 新能力三项行为变更完全一致
</success_criteria>

<output>
Create `.planning/phases/01-llm/01-07-SUMMARY.md` when done（须记录：`realllm` 用例的完整运行命令，供 Plan 08 的实测 checkpoint 直接复制）
</output>
