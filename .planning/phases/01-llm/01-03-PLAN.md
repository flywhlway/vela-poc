---
phase: 01-llm
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/vela/config.py
  - tests/conftest.py
  - tests/test_dotenv_loading.py
autonomous: true
requirements: [ENV-01]

must_haves:
  truths:
    - "不手工 export，仅凭项目根 .env 文件内容，任意 CLI 子命令或 pytest 均能读到火山引擎凭证与 base_url"
    - "已存在的进程环境变量优先级高于 .env，且有专门单测钉住该规则"
    - "导入 vela.config 时不产生任何 stdout/stderr 输出"
    - "pytest 会话中 VELA_LLM_PROVIDER 恒为 mock，.env 里写 volcengine 也不会让既有用例打真实付费 API"
    - "vela 被安装到 site-packages（无项目根锚点）时，.env 加载静默跳过而不是报错"
  artifacts:
    - path: "src/vela/config.py"
      provides: ".env 模块导入期一次性静默加载 + dotenv_report() 供 doctor 消费"
      contains: "load_dotenv"
      exports: ["dotenv_report"]
    - path: "tests/conftest.py"
      provides: "测试作用域锁定：provider/config_dir/profile 无条件赋值"
      contains: "os.environ[\"VELA_LLM_PROVIDER\"] = \"mock\""
    - path: "tests/test_dotenv_loading.py"
      provides: "优先级 / 锚点回溯 / 静默性 / 遮蔽报告 / provider 锁定 五类回归护栏"
      min_lines: 70
  key_links:
    - from: "src/vela/config.py 模块底部"
      to: "os.environ"
      via: "导入期调用 _load_dotenv_once()"
      pattern: "_load_dotenv_once\\(\\)"
    - from: "src/vela/config.py::dotenv_report"
      to: "src/vela/cli.py::cmd_doctor（Plan 06 消费）"
      via: "返回 path/loaded/keys/shadowed 的浅拷贝"
      pattern: "def dotenv_report"
    - from: "tests/conftest.py"
      to: "gateway/base.py::build_gateway 的 provider 判定"
      via: "导入 vela.* 之前无条件写死 VELA_LLM_PROVIDER=mock"
      pattern: "VELA_LLM_PROVIDER"
---

<objective>
按 D-05~D-11 实现 ENV-01：在 `src/vela/config.py` 模块导入时用 `python-dotenv` 一次性、静默、`override=False` 地加载项目根 `.env`，把加载事实收进可供 `vela doctor` 消费的 `dotenv_report()`；同时把 `tests/conftest.py` 的 `setdefault` 改为无条件赋值，防止 `.env` 中的 `volcengine` 让既有 177 个用例打真实付费 API。

Purpose: `.env` 加载是整个里程碑「真实 LLM 实测」的前提。挂在 `config.py` 模块导入期是唯一能同时覆盖 CLI / pytest / `vela serve` / 任意 `import vela.*` 脚本且调用方零改动的注入点。而一旦 `.env` 在 pytest 收集期就灌进 `os.environ`，`setdefault` 就不再具有保护力——D-11 是本阶段的安全关键项。

Output: 五层优先级链的实际实现 + 测试作用域锁定 + 五类回归护栏用例。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-llm/01-CONTEXT.md
@.planning/phases/01-llm/01-PATTERNS.md
@.planning/phases/01-llm/01-01-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 config.py 挂载 .env 静默加载与 dotenv_report()</name>
  <files>src/vela/config.py</files>
  <read_first>
    - src/vela/config.py 全文 144 行（重点：第 1-6 行四层链 docstring；第 7-15 行 import 分段风格；第 17-21 行 `_DEFAULT_CONFIG_DIR` / `config_dir()` 的项目根推导；第 28-34 行 `load_yaml` 的 `lru_cache` 幂等惯例；第 125-143 行 `config_hash()`）
    - .planning/phases/01-llm/01-PATTERNS.md §1（config.py 自身的模式摘录与 `parents[2]` 在 site-packages 下失效的风险说明）与 §Shared Patterns A（cfg 可注入 = 单测前提）
    - .planning/phases/01-llm/01-CONTEXT.md D-05~D-10 与 `<open_questions>` 第 1 条
    - .planning/phases/01-llm/01-02-SUMMARY.md（Plan 02 已写入 STACK.md 的五层链逐字文本，docstring 须与之一致）
    - src/vela/gateway/redact.py 第 21-43 行（`Redactor.__init__` 的「cfg 可注入 + 默认取 YAML」形状，本任务的 `_apply_dotenv(root)` 参数化沿用同一可注入思路）
  </read_first>
  <behavior>
    - `_find_project_root(start)`：从 `start` 起逐级向上（含 `start` 自身）查找含 `pyproject.toml` 或 `.git` 的目录，命中即返回该 `Path`；走到文件系统根仍未命中返回 `None`
    - `_apply_dotenv(root)`：`root` 为 `None` 或 `root/.env` 不存在时返回 `loaded=False, path=None` 且不触碰 `os.environ`
    - `_apply_dotenv(root)`：`.env` 中已存在于 `os.environ` 的键不被覆盖，其值出现在返回结构的 `shadowed` 列表中
    - `_apply_dotenv(root)`：`.env` 中进程里不存在的键被写入 `os.environ`，键名出现在返回结构的 `keys` 中
    - `_apply_dotenv(root)`：全过程不写 stdout / stderr
    - `dotenv_report()`：返回浅拷贝，调用方修改返回值不影响模块内部状态
  </behavior>
  <action>
在 `src/vela/config.py` 中实现 D-05~D-10，遵循同文件既有的「模块级常量 + 纯函数」风格：

1. **docstring（D-10）**：第 4 行优先级链由四层改为五层 `显式函数参数 > 进程环境变量 > .env > config/*.yaml > 代码内默认值`，逐字对齐 Plan 02 已写入 `.planning/codebase/STACK.md` 的表述；再补一句说明 `.env` 在本模块导入时静默加载一次、`override=False`。
2. **import**：在第三方段（`import yaml` 所在段）加 `from dotenv import dotenv_values, load_dotenv`，按字母序置于 `import yaml` 之前。禁止写 try-import 降级分支（D-03/D-05）。
3. **项目根锚点定位（D-07，裁定 Open Question 第 1 条）**：新增 `_find_project_root(start: Path) -> Path | None`，从 `start` 起逐级向上（含 `start` 自身）查找同时具备 `pyproject.toml` **或** `.git` 的目录，命中即返回；到达文件系统根返回 `None`。循环必须有界（用 `[start, *start.parents]` 遍历，不用 `while True`）。
   - 采用「锚点回溯」而非「`find_dotenv(usecwd=True)`」的理由写进函数 docstring：`usecwd=True` 会让 `.env` 定位随 cwd 漂移，直接违反 D-07「不受 cwd 影响」；锚点回溯以 `__file__` 为起点，可编辑安装下第一跳即命中项目根（`<root>/src/vela/config.py` → `<root>` 有 `pyproject.toml`），常规安装到 site-packages 时一路无锚点，返回 `None` → 静默跳过。
   - 模块级常量 `_PROJECT_ROOT = _find_project_root(Path(__file__).resolve().parent)`。
   - ⚠️ **不要改动 `_DEFAULT_CONFIG_DIR`（第 17 行）与 `config_dir()`（第 20-21 行）**：二者行为在可编辑安装下与锚点回溯等价，改写只会引入无谓的回归风险。`_PROJECT_ROOT` 仅用于 `.env` 定位。
4. **加载实现（D-06/D-08/D-09）**：
   - `_apply_dotenv(root: Path | None) -> dict`：`root` 可注入以便单测。`root` 为 `None` 或 `root/".env"` 不存在 → 返回 `{"path": None, "loaded": False, "keys": [], "shadowed": []}` 且不触碰 `os.environ`。否则先用 `dotenv_values(path)` 取出文件声明的全部键，在**加载之前**算出 `shadowed = [k for k in file_keys if k in os.environ]`（这就是 D-09 要收进 doctor 的「哪些被已存在的进程变量遮蔽」），再调 `load_dotenv(dotenv_path=path, override=False)`，返回含 `path`（字符串）、`loaded`、`keys`（排序后的文件键名列表）、`shadowed`（排序后）的 dict。
   - 模块级 `_DOTENV_STATE: dict` 与 `_DOTENV_LOADED: bool`；`_load_dotenv_once()` 做幂等守卫（已加载则直接返回），内部调 `_apply_dotenv(_PROJECT_ROOT)` 并把结果存进 `_DOTENV_STATE`。
   - 在模块**函数定义之后、文件末尾之前**调用一次 `_load_dotenv_once()`，且必须早于任何会读 `os.environ` 的模块级求值。
   - ⚠️ D-09 完全静默：本模块新增代码中禁止出现任何 `print(`；本项目铁律同时禁止 `logging`。加载失败（如 `.env` 语法异常）也不打印——`dotenv_values` 对畸形行是跳过而非抛异常，若确有异常则让它自然冒泡（不吞异常，符合 AGENTS.md 错误处理约定）。
5. **`dotenv_report() -> dict`（D-09）**：公开函数，返回 `dict(_DOTENV_STATE)` 浅拷贝，供 Plan 06 的 `cmd_doctor` 消费。**只返回键名，绝不返回任何值**——这是凭证不泄漏到 doctor 输出的结构性保证。
6. **禁止改动区**：`config_hash()`（第 125-143 行）显式枚举 `pipeline.yaml` / `parsers.yaml` / `ota_phases.yaml` 三个 YAML，零改动即满足 D-16「`env_checks.yaml` 不进 config_hash」。本任务不得向该 payload 增删任何键。
  </action>
  <acceptance_criteria>
    - `grep -c 'from dotenv import' src/vela/config.py` 为 1
    - `grep -c 'def _find_project_root' src/vela/config.py` 为 1，`grep -c 'def _apply_dotenv' src/vela/config.py` 为 1，`grep -c 'def dotenv_report' src/vela/config.py` 为 1
    - `grep -c '^_load_dotenv_once()' src/vela/config.py` 为 1（模块导入期调用一次）
    - `grep -c 'override=False' src/vela/config.py` ≥ 1
    - `grep -vE '^\s*#' src/vela/config.py | grep -c 'print('` 为 0（D-09 静默）
    - `grep -c '显式函数参数 > 进程环境变量 > .env > config/\*.yaml > 代码内默认值' src/vela/config.py` 为 1
    - `git diff src/vela/config.py | grep -E '^[+-].*(pipeline\.yaml|parsers\.yaml|ota_phases\.yaml|canon_rules_version|fingerprint_algos)'` 无输出（`config_hash()` 未被改动）
    - `git diff src/vela/config.py | grep -E '^[+-].*_DEFAULT_CONFIG_DIR'` 无输出（`_DEFAULT_CONFIG_DIR` 未被改动）
    - `PYTHONPATH=src .venv/bin/python3 -c "import vela.config"` 的 stdout 与 stderr 均为空
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'from dotenv import' src/vela/config.py) -eq 1 && test $(grep -c 'def _find_project_root' src/vela/config.py) -eq 1 && test $(grep -c 'def _apply_dotenv' src/vela/config.py) -eq 1 && test $(grep -c 'def dotenv_report' src/vela/config.py) -eq 1 && test $(grep -c 'override=False' src/vela/config.py) -ge 1 && test $(grep -vE '^\s*#' src/vela/config.py | grep -c 'print(') -eq 0 && test -z "$(git diff src/vela/config.py | grep -E '^[+-].*(pipeline\.yaml|parsers\.yaml|ota_phases\.yaml|_DEFAULT_CONFIG_DIR)')" && test -z "$(PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -c 'import vela.config' 2>&1)"</automated>
  </verify>
  <done>`config.py` 在导入期静默加载项目根 `.env`（`override=False`），暴露 `dotenv_report()`；`config_hash()` 与 `_DEFAULT_CONFIG_DIR` 零改动。</done>
</task>

<task type="auto">
  <name>Task 2: conftest.py 逐条处置四个 setdefault，锁死测试作用域</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py 第 1-16 行（四个 `setdefault` 的现状；注意第 10-11 行 `sys.path.insert` 在前，本文件顶层**没有** `from vela...` import，因此这四行在任何 `import vela.*` 之前执行）
    - .planning/phases/01-llm/01-CONTEXT.md D-11 全文（含「同理检查 `:14-15`」的要求）
    - .planning/phases/01-llm/01-PATTERNS.md §8（逐条列出四个变量各自的漂移后果：`VELA_CONFIG_DIR` 配置基准漂移、`VELA_PROFILE` 预算断言基准漂移、`PYTHONHASHSEED` 运行期赋值无效）
    - .env.example 第 22-27 行（确认 `VELA_PROFILE` 与 `PYTHONHASHSEED` 确实出现在样例中，用户 `.env` 大概率含它们）
    - tests/test_obs_and_config.py 第 80-95 行（依赖 `VELA_PROFILE=poc` 的预算断言，验证改动理由）
    - src/vela/gateway/base.py 第 155-174 行（`build_gateway` 的 provider 判定优先级：`provider_name or os.environ["VELA_LLM_PROVIDER"] or cfg["active"]`——说明为何锁死环境变量即可锁死全部用例的 provider）
  </read_first>
  <action>
按 D-11 逐条处置 `tests/conftest.py` 第 12-15 行的四个 `os.environ.setdefault`，并在文件顶部补一段注释说明因果链：conftest 的这几行在 pytest 加载 conftest 时执行，早于任何测试模块 `import vela.*`；随后首次 `import vela.config` 触发 `_load_dotenv_once()`，因为 `override=False`，`.env` 无法覆盖这里已写入的值——**先无条件写死、再让 `.env` 以 override=False 加载**，正是既满足 ENV-01「测试也能读到凭证」又杜绝付费调用的组合。

1. 第 13 行 `VELA_LLM_PROVIDER`：`setdefault` → 无条件赋值 `os.environ["VELA_LLM_PROVIDER"] = "mock"`（D-11，安全关键）。行内注释写明：若保持 `setdefault`，用户 `.env` 中的 `volcengine` 会让全部既有用例打真实付费 API 且 determinism 类断言大面积失败。需要真实 LLM 的用例自行 `monkeypatch.setenv` 回 `volcengine`（Plan 07 的 `realllm` 用例即如此）。
2. 第 12 行 `VELA_CONFIG_DIR`：改为无条件赋值 `str(ROOT / "config")`。理由注释：`.env` 若把它指向别处，全部用例的配置基准（含 `config_hash`）漂移；测试必须钉死在仓库内 `config/`。
3. 第 14 行 `VELA_PROFILE`：改为无条件赋值 `"poc"`。理由注释：`.env.example` 含 `VELA_PROFILE=poc`，用户若改为 `production`，`tests/test_obs_and_config.py` 的预算断言基准即变化。
4. 第 15 行 `PYTHONHASHSEED`：**保持 `setdefault` 不变**，但补一行注释说明为何是唯一例外——该变量必须在解释器启动前生效，运行期赋值对哈希随机化无任何作用（真正生效点是 `run_all.sh` 的 `export PYTHONHASHSEED=0`）；改成无条件赋值会制造「运行期设它有用」的错觉。

⚠️ 不得改动 `SMALL` 常量与本文件中的任何 fixture 定义。
  </action>
  <acceptance_criteria>
    - `grep -c 'os.environ\["VELA_LLM_PROVIDER"\] = "mock"' tests/conftest.py` 为 1
    - `grep -c 'setdefault("VELA_LLM_PROVIDER"' tests/conftest.py` 为 0
    - `grep -c 'os.environ\["VELA_CONFIG_DIR"\]' tests/conftest.py` 为 1，`grep -c 'os.environ\["VELA_PROFILE"\]' tests/conftest.py` 为 1
    - `grep -c 'setdefault("PYTHONHASHSEED"' tests/conftest.py` 为 1（唯一保留的 setdefault）
    - `grep -c 'SMALL = dict(density=2, chunks=140, blocks=60)' tests/conftest.py` 为 1（常量未被误改）
    - 在临时 `.env` 含 `VELA_LLM_PROVIDER=volcengine` 的条件下运行 `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_gateway.py -q`，全部通过且不产生任何网络调用
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'os.environ\["VELA_LLM_PROVIDER"\] = "mock"' tests/conftest.py) -eq 1 && test $(grep -c 'setdefault("VELA_LLM_PROVIDER"' tests/conftest.py) -eq 0 && test $(grep -c 'os.environ\["VELA_CONFIG_DIR"\]' tests/conftest.py) -eq 1 && test $(grep -c 'os.environ\["VELA_PROFILE"\]' tests/conftest.py) -eq 1 && test $(grep -c 'setdefault("PYTHONHASHSEED"' tests/conftest.py) -eq 1 && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_gateway.py tests/test_obs_and_config.py -q</automated>
  </verify>
  <done>三个会造成基准漂移的变量已改为无条件赋值，`PYTHONHASHSEED` 保留 `setdefault` 并有注释说明；测试作用域被锁死在 mock。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 新建 tests/test_dotenv_loading.py 五类回归护栏</name>
  <files>tests/test_dotenv_loading.py</files>
  <read_first>
    - src/vela/config.py（Task 1 完成后的实现，确认 `_find_project_root` / `_apply_dotenv` / `dotenv_report` 的确切签名与返回结构）
    - tests/test_gateway.py 第 143-153 行（`monkeypatch.setenv` 驱动的环境变量优先级断言形状，直接照搬）
    - tests/test_cli_and_server.py 第 1 行与第 30-35 行（测试文件首行中文 docstring 惯例；「为什么单元测试抓不到、只有这条路径能抓到」的回归用例注释惯例）
    - tests/conftest.py（Task 2 完成后的状态，确认 provider 锁定断言的对象）
    - .planning/ROADMAP.md 第 29 行（成功判据 1 明确要求「有专门单测覆盖该优先级规则」）
  </read_first>
  <behavior>
    - Test 1 `test_existing_process_env_wins_over_dotenv`：进程已有 `VELA_TEST_TOKEN=from-process`，tmp `.env` 写同名键不同值 → `_apply_dotenv(tmp)` 后 `os.environ["VELA_TEST_TOKEN"] == "from-process"`
    - Test 2 `test_dotenv_fills_absent_variable`：`.env` 中的新键在进程里不存在 → 加载后 `os.environ` 中出现且值来自 `.env`
    - Test 3 `test_shadowed_keys_are_reported`：Test 1 场景下返回结构的 `shadowed` 含 `VELA_TEST_TOKEN`，`keys` 含文件中声明的全部键
    - Test 4 `test_find_project_root_hits_pyproject_anchor`：tmp 目录树 `<r>/pyproject.toml` + `<r>/src/vela/` → `_find_project_root(<r>/src/vela)` 返回 `<r>`
    - Test 5 `test_find_project_root_returns_none_without_anchor`：tmp 目录树无 `pyproject.toml` 与 `.git` → 返回 `None`（site-packages 情形）
    - Test 6 `test_apply_dotenv_is_silent`：`capsys` 断言 `_apply_dotenv(tmp)` 的 stdout 与 stderr 均为空字符串
    - Test 7 `test_apply_dotenv_noop_when_root_is_none`：`_apply_dotenv(None)` 返回 `loaded is False` 且 `path is None`
    - Test 8 `test_conftest_locks_provider_to_mock`：`os.environ["VELA_LLM_PROVIDER"] == "mock"`
    - Test 9 `test_dotenv_report_does_not_leak_values`：`dotenv_report()` 返回结构中不含任何值型字段，仅有 `path` / `loaded` / `keys` / `shadowed`
  </behavior>
  <action>
新建 `tests/test_dotenv_loading.py`，首行中文 docstring 说明用途（`.env` 五层优先级链、项目根锚点定位、加载静默性与测试作用域锁定的回归护栏）。

实现要点：
- 全部用例通过 `_apply_dotenv(tmp_path)` 注入根目录，**不依赖也不修改仓库真实 `.env`**。这就是 Task 1 把 `root` 做成参数的目的（同 `Redactor(cfg)` 的可注入思路）。
- 环境变量一律用 `monkeypatch.setenv` / `monkeypatch.delenv(raising=False)` 设置与清理，禁止直接改 `os.environ` 导致跨用例污染。
- 测试用变量名统一使用 `VELA_TEST_` 前缀（如 `VELA_TEST_TOKEN`、`VELA_TEST_ONLY`），避免与真实配置键碰撞。
- ⚠️ **安全**：tmp `.env` 夹具中的值一律用 `DUMMY-` 前缀的假串（如 `DUMMY-from-dotenv`），禁止写入任何形似真实 API key 的字符串、禁止从进程读取真实 `VELA_ARK_API_KEY` 写入夹具。
- Test 8 的 docstring 必须写明「为什么只有这条断言能抓到」：`.env` 在 pytest 收集期就会灌进 `os.environ`，若 conftest 用 `setdefault`，177 个既有用例会全部改打真实付费 API 且 determinism 用例大面积失败；这是任何单个业务单元测试都抓不到的结构性漏洞（照 `tests/test_cli_and_server.py:30-35` 的注释惯例书写）。
- Test 9 断言 `set(dotenv_report()) <= {"path", "loaded", "keys", "shadowed"}`，把「report 只出键名不出值」钉成契约。
  </action>
  <acceptance_criteria>
    - `tests/test_dotenv_loading.py` 存在且行数 ≥ 70
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_dotenv_loading.py -q` 退出码 0，收集到的用例数 ≥ 9
    - `grep -c 'def test_' tests/test_dotenv_loading.py` ≥ 9
    - `grep -cE 'sk-[A-Za-z0-9]|VELA_ARK_API_KEY' tests/test_dotenv_loading.py` 为 0（夹具无真实凭证形态）
    - `grep -c '_apply_dotenv' tests/test_dotenv_loading.py` ≥ 4（确实走可注入路径而非依赖真实 `.env`）
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q` 退出码 0，passed 数 ≥ `01-BASELINE.md` 的 `baseline_pytest_passed`
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'def test_' tests/test_dotenv_loading.py) -ge 9 && test $(grep -cE 'sk-[A-Za-z0-9]|VELA_ARK_API_KEY' tests/test_dotenv_loading.py) -eq 0 && test $(grep -c '_apply_dotenv' tests/test_dotenv_loading.py) -ge 4 && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_dotenv_loading.py -q && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q</automated>
  </verify>
  <done>九条护栏用例全部通过；优先级规则（ROADMAP 成功判据 1）、锚点回溯、静默性、report 不泄值、provider 锁定各有专门断言；全量测试不低于基线。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` 文件 → `os.environ` | 真实凭证从磁盘进入进程内存，此后任何 print / 异常 / 审计路径都可能带出 |
| `os.environ` → pytest 会话 | `.env` 中的 `VELA_LLM_PROVIDER=volcengine` 会把 177 个离线用例转为付费真实调用 |
| 项目根锚点 → `.env` 定位 | 若定位随 cwd 漂移，可能加载到非预期目录下的 `.env`（外部可控文件） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `config.py` 模块导入期输出 | mitigate | D-09 完全静默：新增代码零 `print(`；acceptance 用 `grep -vE '^\s*#' src/vela/config.py \| grep -c 'print('` 断言为 0，并断言 `import vela.config` 的 stdout+stderr 为空 |
| T-03-02 | Information Disclosure | `dotenv_report()` 返回结构 | mitigate | 契约限定为 `{path, loaded, keys, shadowed}` 四键，只出键名不出值；Test 9 用 `set(...) <= {...}` 钉住，Plan 06 的 doctor 因而无法从此处泄值 |
| T-03-03 | Tampering | `tests/conftest.py` provider 锁定 | mitigate | D-11 无条件赋值 + Test 8 回归断言；这是「测试误打付费 API」的第二重缓解（第一重是 Plan 01 的 `-m 'not realllm'`），两重互不依赖 |
| T-03-04 | Spoofing | `.env` 定位 | mitigate | 采用 `__file__` 锚点回溯而非 `find_dotenv(usecwd=True)`，定位不受 cwd 影响（D-07）；无锚点时返回 `None` 静默跳过，不会误加载任意目录下的 `.env` |
| T-03-05 | Information Disclosure | 测试夹具 `.env` | mitigate | 夹具值一律 `DUMMY-` 前缀假串；acceptance 用 `grep -cE 'sk-[A-Za-z0-9]\|VELA_ARK_API_KEY'` 断言为 0；仓库真实 `.env` 已被 `.gitignore` 覆盖且用例不读取它 |
| T-03-06 | Denial of Service | `_find_project_root` 向上回溯 | mitigate | 用 `[start, *start.parents]` 有界遍历，不使用 `while True`，不可能在符号链接环上死循环 |
</threat_model>

<verification>
1. `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -c 'import vela.config'` 无任何输出
2. `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_dotenv_loading.py -q` 退出码 0
3. `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q` 退出码 0，passed 不低于基线
4. `git diff src/vela/config.py` 中不含 `config_hash` payload 与 `_DEFAULT_CONFIG_DIR` 的增删行
5. 临时把 `.env` 的 `VELA_LLM_PROVIDER` 置为 `volcengine` 后，`pytest tests/test_gateway.py` 仍全过且零网络调用
</verification>

<success_criteria>
- 仅凭项目根 `.env`，`PYTHONPATH=src .venv/bin/python3 -c "import os,vela.config;print('VELA_ARK_BASE_URL' in os.environ)"` 输出 `True`（无需手工 export）
- 进程环境变量优先级高于 `.env`，有专门单测钉住（ROADMAP 成功判据 1）
- 导入 `vela.config` 零输出
- pytest 会话中 `VELA_LLM_PROVIDER` 恒为 `mock`
- 无项目根锚点时 `.env` 加载静默跳过而非抛错
</success_criteria>

<output>
Create `.planning/phases/01-llm/01-03-SUMMARY.md` when done（须记录：`dotenv_report()` 的确切返回结构与键名，供 Plan 06 的 `cmd_doctor` 直接消费）
</output>
