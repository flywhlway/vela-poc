---
phase: 01-llm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/codebase/STACK.md
  - README.md
  - Makefile
autonomous: true
requirements: [ENV-01]

must_haves:
  truths:
    - "任何智能体读 AGENTS.md 后，得到的纪律是「能用成熟三方库就不手写」，不再是「依赖最小化 / 先评估 stdlib 可行性」"
    - "四份文档中不再存在「不新增运行期依赖」「先评估能否用 stdlib 实现」这类会否决 D-01 的表述"
    - "配置加载优先级链在文档中是五层（含 .env 层），与 Plan 03 将实现的行为一致"
    - "除依赖最小化外的全部架构铁律在 AGENTS.md 中原样保留（D-02）"
  artifacts:
    - path: "AGENTS.md"
      provides: "新依赖纪律 + realllm 标记登记"
      contains: "realllm"
    - path: ".planning/PROJECT.md"
      provides: "Constraints 中的 Tech stack 口径更新"
      contains: "python-dotenv"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ENV-01 去掉 stdlib 约束后的需求原文"
      contains: "ENV-01"
    - path: ".planning/codebase/STACK.md"
      provides: "五层优先级链 + 依赖清单含新依赖"
      contains: "python-dotenv"
  key_links:
    - from: "AGENTS.md 代码约定"
      to: "本阶段与后续 Phase 2~6 的依赖选型"
      via: "D-01 项目级永久变更"
      pattern: "三方库"
    - from: ".planning/codebase/STACK.md 优先级链"
      to: "src/vela/config.py docstring（Plan 03 改写）"
      via: "两处必须表述同一条五层链"
      pattern: "\\.env"
---

<objective>
落地 D-01 的项目级永久变更与 D-10 的优先级链改写：把散落在四份权威文档 + README + Makefile help 中的「依赖最小化 / 先评估 stdlib 可行性 / 不新增运行期依赖」口径全部替换为新纪律「能用成熟三方开源库解决的一律不手写」，并把配置加载优先级链从四层改写为含 `.env` 的五层。

Purpose: D-01 是项目级永久变更，Phase 2~6 同样适用。只要旧口径还留在 AGENTS.md / PROJECT.md / REQUIREMENTS.md 里，后续任何智能体读到的就是「新增依赖前先评估 stdlib」，D-01 就会被反复推翻。文档改写不是顺手为之，是本阶段的交付物。

Output: 六个文件的口径对齐；D-02 明确保留的其余铁律一字不动。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-llm/01-CONTEXT.md
@.planning/phases/01-llm/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 改写依赖纪律口径（AGENTS.md / PROJECT.md / REQUIREMENTS.md）</name>
  <files>AGENTS.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - AGENTS.md 全文（重点第 34 行 pytest 标记清单、第 44-50 行「代码约定」、第 36-42 行「架构铁律」、第 52-56 行「安全边界」）
    - .planning/PROJECT.md 第 86-96 行 §Constraints（第 88 行是待改写项）
    - .planning/REQUIREMENTS.md 第 17-24 行 §ENV（第 21 行 ENV-01 是待改写项）
    - .planning/phases/01-llm/01-CONTEXT.md 第 20-30 行（D-01 与 D-02 原文，含「必须同步改写三处」清单）
    - .planning/phases/01-llm/01-PATTERNS.md §12（四处待改写原文已逐字定位）
  </read_first>
  <action>
逐条替换以下三处原文（D-01）：

1. `AGENTS.md` 代码约定中的 `- 依赖最小化：运行期仅 duckdb / pyarrow / PyYAML / pytz，新增依赖前先评估能否用 stdlib 实现。`
   → 改为一条表达新纪律的条目：核心思想是「能用成熟三方开源库解决的，一律不手写实现；新增依赖只需满足纯本地可安装、不引入必须联网才能跑通主链路的服务」，并列出当前运行期依赖为 duckdb / pyarrow / PyYAML / pytz / python-dotenv / openai。条目中必须出现字符串 `三方库`，且不得再出现 `新增依赖前先评估能否用 stdlib 实现`。
   同时在 `AGENTS.md` 第 34 行的 pytest 标记清单中补上 `realllm`，说明其语义为「需要真实 LLM 端点的付费用例，默认被 addopts 排除，显式 `-m realllm` 才跑」。
   ⚠️ D-02：`AGENTS.md` 的「架构铁律」五条、「日志纪律」「错误处理」「解析失败留痕」「安全边界」「完成判据」各条一字不动，只改依赖最小化那一条 + 标记清单那一行。

2. `.planning/PROJECT.md` §Constraints 中的 `- **Tech stack**: Python ≥ 3.11，src/ 布局，运行期依赖仅 duckdb / pyarrow / PyYAML / pytz — 依赖最小化是既定纪律，新增依赖前先评估 stdlib 可行性`
   → 依赖清单补 `python-dotenv` 与 `openai`，破折号后的理由改为新纪律表述，并标注这是 2026-07-31 Phase 1 讨论的 D-01 项目级永久变更。不得再出现 `依赖最小化是既定纪律` 与 `先评估 stdlib 可行性`。
   ⚠️ §Constraints 其余各条（本地优先 / 单线程 / 查询唯一收口 / 配置驱动 / 图节点即方法 / 日志纪律 / Security / 成本）一字不动。

3. `.planning/REQUIREMENTS.md` 第 21 行 ENV-01 中的括号内容 `（stdlib 实现，不新增运行期依赖；已存在的进程环境变量优先级更高）`
   → 改为 `（用 python-dotenv 实现，见 D-01/D-03/D-05；已存在的进程环境变量优先级更高）`。ENV-01 的其余措辞与需求编号不变。
   同时在 §ENV 的引言块（第 19 行那段「本类别不在四份文档覆盖范围内…」）末尾补一句，说明「原文中的 stdlib 约束已被 Phase 1 的 D-01 推翻」，让阅读者不至于以为需求被偷改。

改写时保留各文件既有的 Markdown 层级、列表符号与中文标点风格。
  </action>
  <acceptance_criteria>
    - `grep -c '新增依赖前先评估能否用 stdlib 实现' AGENTS.md` 为 0
    - `grep -c '三方库' AGENTS.md` ≥ 1
    - `grep -c 'realllm' AGENTS.md` ≥ 1
    - `grep -c '依赖最小化是既定纪律' .planning/PROJECT.md` 为 0，且 `grep -c 'python-dotenv' .planning/PROJECT.md` ≥ 1
    - `grep -c 'stdlib 实现，不新增运行期依赖' .planning/REQUIREMENTS.md` 为 0
    - `grep -c 'python-dotenv' .planning/REQUIREMENTS.md` ≥ 1
    - `grep -c '查询唯一收口' AGENTS.md` ≥ 1 且 `grep -c '不使用 `logging` 模块\|不使用 logging 模块' AGENTS.md` ≥ 1（D-02：其余铁律未被误删）
    - `grep -c 'gateway/redact.py' AGENTS.md` ≥ 1（安全边界未被误删）
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c '新增依赖前先评估能否用 stdlib 实现' AGENTS.md) -eq 0 && test $(grep -c '三方库' AGENTS.md) -ge 1 && test $(grep -c 'realllm' AGENTS.md) -ge 1 && test $(grep -c '查询唯一收口' AGENTS.md) -ge 1 && test $(grep -c 'gateway/redact.py' AGENTS.md) -ge 1 && test $(grep -c '依赖最小化是既定纪律' .planning/PROJECT.md) -eq 0 && test $(grep -c 'python-dotenv' .planning/PROJECT.md) -ge 1 && test $(grep -c 'stdlib 实现，不新增运行期依赖' .planning/REQUIREMENTS.md) -eq 0 && test $(grep -c 'python-dotenv' .planning/REQUIREMENTS.md) -ge 1</automated>
  </verify>
  <done>三份权威文档的依赖纪律口径已替换为 D-01 新纪律；D-02 明确保留的其余铁律逐条仍在。</done>
</task>

<task type="auto">
  <name>Task 2: 改写配置加载优先级链与依赖清单（STACK.md / README.md / Makefile）</name>
  <files>.planning/codebase/STACK.md, README.md, Makefile</files>
  <read_first>
    - .planning/codebase/STACK.md 第 40-56 行（依赖清单：核心 4 个 + 可选）与第 58-67 行 §Configuration（第 67 行是四层优先级链，待改写）
    - README.md 第 114-130 行（目录结构中 `requirements.txt` / `requirements-optional.txt` 的依赖描述）
    - Makefile 第 13-27 行 help 文本（第 15-16 行描述依赖）
    - .planning/phases/01-llm/01-CONTEXT.md D-10（新的五层链原文）与 D-09（加载静默、信息收到 doctor）
    - src/vela/config.py 第 1-6 行（当前四层链 docstring —— 归 Plan 03 改写，本任务只需保证文档口径与之一致）
  </read_first>
  <action>
1. `.planning/codebase/STACK.md`：
   - 第 67 行的优先级链 `显式函数参数 > 环境变量 > config/*.yaml > 代码内默认值` 改为 D-10 的五层链：`显式函数参数 > 进程环境变量 > .env > config/*.yaml > 代码内默认值`。同一行末尾关于「业务代码不直接读 os.environ，全部收敛在 config.py/gateway/*」的表述保留。
   - 在 §Configuration 的 Environment 列表（第 60-66 行）中新增一条，说明 `.env` 由 `src/vela/config.py` 在**模块导入时**自动加载一次（`override=False`，已存在的进程环境变量恒定优先；加载过程完全静默，命中路径与被遮蔽的键名在 `vela doctor` 输出中呈现）——对应 D-06 / D-08 / D-09。
   - 核心运行时依赖段（第 43-46 行附近）追加 `python-dotenv>=1.0`（`.env` 自动加载，见 `src/vela/config.py`）与 `openai>=1.40`（OpenAI 兼容 provider 的 SDK 基座，见 `src/vela/gateway/openai_compat.py`）；把「核心运行时依赖仅 4 个」一类的计数表述同步改为 6 个。
2. `README.md` 第 120 行 `requirements.txt  必需依赖：duckdb / pyarrow / PyYAML / pytz` → 依赖清单补 `python-dotenv / openai`。若 README 中还有其他同口径的依赖计数或「零第三方依赖」类表述，一并对齐（用 `grep -n 'duckdb / pyarrow\|duckdb/pyarrow\|零第三方' README.md` 定位）。
3. `Makefile` 第 15 行 help 文本 `安装运行期依赖（duckdb/pyarrow/PyYAML/pytz）` → 补 `python-dotenv/openai`。第 16 行 `install-dev` 的描述保持原意。
   ⚠️ 只改 help 的 `@echo` 文本，不得改动 `install` / `install-dev` / `test` / `test-fast` / `eval` 等任何目标的命令体。

⚠️ 本任务不改 `src/vela/config.py`（归 Plan 03）与 `docs/LLM_PRODUCTION.md`（归 Plan 07），避免跨计划文件冲突。
  </action>
  <acceptance_criteria>
    - `grep -c '显式函数参数 > 进程环境变量 > .env > config/\*.yaml > 代码内默认值' .planning/codebase/STACK.md` ≥ 1
    - `grep -c 'python-dotenv' .planning/codebase/STACK.md` ≥ 1 且 `grep -c 'openai>=' .planning/codebase/STACK.md` ≥ 1
    - `grep -c 'override=False' .planning/codebase/STACK.md` ≥ 1（D-08 已被文档记录）
    - `grep -c 'python-dotenv' README.md` ≥ 1
    - `grep -c 'python-dotenv' Makefile` ≥ 1
    - `git diff --stat Makefile` 显示改动仅在 help 段：`git diff -U0 Makefile | grep -c '^[+-][^+-]' ` 的增删行全部落在 `@echo` 行（人工核对 diff 时确认 `install:` / `test:` 目标体未变）
    - `PYTHON=.venv/bin/python3 make help` 退出码 0（Makefile 未被写坏）
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c 'python-dotenv' .planning/codebase/STACK.md) -ge 1 && test $(grep -c 'override=False' .planning/codebase/STACK.md) -ge 1 && grep -q '显式函数参数 > 进程环境变量 > .env > config/\*.yaml > 代码内默认值' .planning/codebase/STACK.md && test $(grep -c 'python-dotenv' README.md) -ge 1 && test $(grep -c 'python-dotenv' Makefile) -ge 1 && PYTHON=.venv/bin/python3 make help >/dev/null && git diff -U0 Makefile | grep '^[+-][^+-]' | grep -v '@echo' | wc -l | grep -qx '0'</automated>
  </verify>
  <done>STACK.md 的优先级链为五层且记录了 override=False；三份文件的依赖清单含 python-dotenv 与 openai；Makefile 仅 help 文本变动。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档（AGENTS.md / PROJECT.md）→ 后续所有智能体行为 | AGENTS.md 是所有 AI coding agent 的强制前置读物；误删安全条款等于对未来每一次改动放开限制 |
| 文档示例 → 版本库 | 文档里若为「示范」写入真实凭证形态字符串，会被提交进 git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | AGENTS.md §安全边界 / §架构铁律 | mitigate | D-02 要求只改「依赖最小化」一条；acceptance 用 `grep -c '查询唯一收口'`、`grep -c 'gateway/redact.py'` 正向断言安全条款与铁律仍在 |
| T-02-02 | Elevation of Privilege | 新依赖纪律表述 | mitigate | 新纪律文案必须显式保留「纯本地可安装、不引入必须联网才能跑通主链路的服务」这一限定，避免被读成「任意依赖均可引入」（D-02 本地优先） |
| T-02-03 | Information Disclosure | 文档中的环境变量示例 | mitigate | STACK.md / README.md 中新增的 `.env` 说明只写变量名与语义，禁止写任何形似真实 key 的值 |
| T-02-04 | Tampering | Makefile 目标命令体 | mitigate | acceptance 断言 `git diff -U0 Makefile` 的增删行全部落在 `@echo` 行，构建/测试目标不可被顺带改写 |
</threat_model>

<verification>
1. `grep -c '新增依赖前先评估能否用 stdlib 实现' AGENTS.md` 为 0，且新纪律条目存在
2. `.planning/PROJECT.md` 与 `.planning/REQUIREMENTS.md` 中的旧口径原文已消失，新依赖名出现
3. `.planning/codebase/STACK.md` 的优先级链为五层且含 `.env`
4. D-02 保留项抽查：`查询唯一收口` / `gateway/redact.py` / `logging` 三条在 AGENTS.md 中仍在
5. `PYTHON=.venv/bin/python3 make help` 退出码 0
</verification>

<success_criteria>
- 四份权威文档（AGENTS.md / PROJECT.md / REQUIREMENTS.md / STACK.md）不再含任何会否决 D-01 的表述
- 五层优先级链在 STACK.md 中就位，与 Plan 03 将写入 `config.py` docstring 的表述逐字一致
- README.md 与 Makefile help 的依赖清单已包含两个新依赖
- 除依赖纪律与标记清单外，AGENTS.md 其余内容零改动
</success_criteria>

<output>
Create `.planning/phases/01-llm/01-02-SUMMARY.md` when done（须记录：改写后的五层优先级链逐字文本，供 Plan 03 复制到 `config.py` docstring）
</output>
