---
phase: 02-metrics-baseline
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-03]
files_modified:
  - pyproject.toml
  - requirements-optional.txt
  - src/vela/eval/stats.py
  - src/vela/eval/runner.py
  - src/vela/eval/report.py
  - src/vela/cli.py
  - Makefile
  - tests/test_eval.py
autonomous: true
requirements: [METR-04, METR-07]

must_haves:
  truths:
    - "vela eval run --repeat N（N≥2）输出各指标均值±标准差与 Student t 95% CI；无 --repeat 时行为与今日单次一致"
    - "报表同时含逐次 run 明细与聚合行"
    - "vela eval run --reuse-workspace 在 gold/analysis.duckdb 存在且 qa_report.checks_passed=True 时跳过重建，否则重建"
    - "CLI 支持 --no-cache 关闭 LLM 磁盘缓存（接到 Plan 03 开关）"
  artifacts:
    - path: "src/vela/eval/stats.py"
      provides: "mean/std/t-CI（scipy）"
      exports: ["mean_std_ci", "aggregate_metrics"]
    - path: "src/vela/cli.py"
      provides: "--repeat / --reuse-workspace / --no-cache"
      contains: "reuse-workspace"
  key_links:
    - from: "cmd_eval --repeat"
      to: "vela.eval.stats.mean_std_ci"
      via: "N 次 EvalRunner.run 后聚合"
      pattern: "mean_std_ci|ci95"
    - from: "EvalRunner._one --reuse-workspace"
      to: "qa/qa_report.json checks_passed"
      via: "_workspace_reusable"
      pattern: "checks_passed"
---

<objective>
交付重复评测统计（METR-04/D-09..D-11）与 workspace 复用（METR-07/D-14），并把 `--no-cache` 接到网关缓存开关。scipy 以可选 `[eval]` 进入依赖且并入 `dev`（Package Legitimacy Audit 已 [OK]）。

Purpose: 基线需要置信区间；迭代评测需要跳过重复建库与可关缓存。
Output: `eval/stats.py`、CLI 旗标、reuse 判定、scipy 依赖、单测。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-metrics-baseline/02-CONTEXT.md
@.planning/phases/02-metrics-baseline/02-RESEARCH.md
@.planning/phases/02-metrics-baseline/02-01-SUMMARY.md
@.planning/phases/02-metrics-baseline/02-03-SUMMARY.md
@src/vela/cli.py
@src/vela/eval/runner.py
@src/vela/eval/report.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 加入 scipy 可选依赖并实现 eval.stats t-CI</name>
  <files>pyproject.toml, requirements-optional.txt, src/vela/eval/stats.py, tests/test_eval.py</files>
  <read_first>
    - pyproject.toml（optional-dependencies：dev/all）
    - requirements-optional.txt
    - .planning/phases/02-metrics-baseline/02-RESEARCH.md Package Legitimacy Audit（scipy/numpy [OK]）与 mean_std_ci 示例
    - Makefile install-dev（确认装 `[dev]`）
  </read_first>
  <action>
按 D-10 / Phase1 D-01：新增 `src/vela/eval/stats.py`，用 `scipy.stats.t.interval` + `sem` 实现 `mean_std_ci(vals, confidence=0.95) -> {mean,std,ci95,n}`（RESEARCH 示例）。无 scipy 时 import 失败给出「pip install -e '.[eval]'」清晰错误——主链路（无 --repeat）禁止顶层 import scipy。

依赖声明（合法性已 [OK]，无需人工包闸门）：
- `pyproject.toml`：`optional-dependencies.eval = ["scipy>=1.11"]`；把 `scipy>=1.11` 并入 `dev` 与 `all`（保证 `make install-dev` 后 stats 单测可跑）
- `requirements-optional.txt`：追加 scipy 行与用途注释

单测：已知小样本数组，断言 mean/std/ci95 合理（可用固定向量对 scipy 直接期望）。缺 scipy 时 skip 或 install-dev 后必跑。
  </action>
  <verify>
    <automated>PYTHON=.venv/bin/python3 make install-dev && PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_eval.py -k 'stats or mean_std or ci95' -q</automated>
  </verify>
  <done>scipy 在 dev/eval extra；mean_std_ci 可测且非手写 t 表。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI --repeat / --reuse-workspace / --no-cache 接线</name>
  <files>src/vela/cli.py, src/vela/eval/runner.py, src/vela/eval/report.py, Makefile, tests/test_eval.py</files>
  <read_first>
    - src/vela/cli.py（cmd_eval 与 argparse eval run）
    - src/vela/eval/runner.py（_one 总是 build）
    - src/vela/evidence/qa.py（qa_report.json checks_passed）
    - .planning/phases/02-metrics-baseline/02-CONTEXT.md（D-09/D-11/D-14）
    - .planning/phases/02-metrics-baseline/02-RESEARCH.md reuse 判定与 Pitfall 5/6
  </read_first>
  <behavior>
    - 无 --repeat：单次 run，报表与今日一致（可多新指标键但不改变默认结构破坏）
    - --repeat N（N<2 报错 rc=2）：跑 N 次，写出 runs[] 明细 + aggregate 行（均值±std 与 ci95）
    - --reuse-workspace：_workspace_reusable = duckdb 存在且 qa checks_passed True；否则 build
    - --no-cache：关闭 gateway 缓存（环境或 EvalRunner/AgentGraph 传参）
    - cmd_eval 硬退出条件仍为原四项（D-25）；repeat 模式对聚合 top1 等用均值判定或对每次判定——选定「用最后一次/聚合均值」并在 SUMMARY 写死：推荐用聚合均值对照原阈值
  </behavior>
  <action>
1. `EvalRunner.__init__` 增加 `reuse_workspace: bool = False`、`cache_enabled: bool | None = None`；`_one` 按 RESEARCH `_workspace_reusable` 实现（D-14）。半成品禁止 silent reuse。
2. `cmd_eval`：解析 `--repeat`、`--reuse-workspace`、`--no-cache`；repeat 时循环调用 runner，用 `eval.stats` 聚合数值指标；`render_markdown` / JSON 增加 `runs` + `aggregate`（D-11）。
3. 将 `--no-cache` 映射到 Plan 03 的缓存开关（环境变量 `VELA_LLM_CACHE=0` 或 gateway 构造参数）。
4. Makefile 可加 `eval-repeat` 示例目标（mock、N=2）——非付费。
5. 单测：tmp_path 下假 workspace（有/无 qa）验证 reuse 分支；stats 聚合键存在。完整 10 场景 eval 不强制在本任务跑完（慢），但 reuse/repeat 逻辑单元测必须覆盖。

⚠️ 禁止改 graph 推理；禁止把过程/消融目标加入硬退出码。
  </action>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_eval.py -k 'repeat or reuse or stats or no_cache' -q</automated>
  </verify>
  <done>METR-04/METR-07 CLI 可测；--no-cache 接线；退出码契约保持。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 参数 → EvalRunner | N≥2 校验；reuse 条件严格 |
| scipy 安装 → 统计路径 | 可选依赖，缺包明确失败 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | --reuse-workspace 半成品库 | mitigate | 要求 analysis.duckdb + checks_passed |
| T-02-09 | Denial of Service | --repeat 过大 | accept | POC CLI；可不设上限或温和上限（如 N≤20）并文档化 |
| T-02-SC | Tampering | scipy pip | mitigate | RESEARCH 已 [OK]；写入 optional-dependencies |
</threat_model>

<verification>
- stats/repeat/reuse 单测绿
- `make install-dev` 后可 import scipy
</verification>

<success_criteria>
- METR-04、METR-07 行为可观测
- 默认无 --repeat 路径不强制 import scipy
</success_criteria>

<output>
Create `.planning/phases/02-metrics-baseline/02-04-SUMMARY.md` when done
</output>
