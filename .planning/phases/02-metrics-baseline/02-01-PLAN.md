---
phase: 02-metrics-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vela/agent/citations.py
  - src/vela/eval/runner.py
  - src/vela/eval/report.py
  - src/vela/agent/graph.py
  - src/vela/obs/metrics.py
  - src/vela/cli.py
  - tests/test_agent.py
  - tests/test_eval.py
autonomous: true
requirements: [METR-01, METR-02]

must_haves:
  truths:
    - "零引用报告 CitationReport.ok 为 False，且 has_citations 为 False"
    - "total==0 时 dangling_rate 为 None 而非 0.0，有专门单测钉死"
    - "citation_coverage 可对 report_md 计算并进入评测 metrics/_TARGETS（G1≥0.9 仅报表目标）"
    - "EvalRunner/metrics 对 dangling_rate=None 不崩溃；cmd_eval 退出码契约不被新指标破坏"
  artifacts:
    - path: "src/vela/agent/citations.py"
      provides: "dangling_rate/ok/has_citations + citation_coverage 事实句切分"
      exports: ["CitationReport", "citation_coverage", "split_factual_sentences"]
    - path: "tests/test_agent.py"
      provides: "total==0 与 coverage 边界单测"
      contains: "test_zero_citation"
  key_links:
    - from: "CitationReport.ok"
      to: "eval metrics / CLI 展示"
      via: "to_dict() 含 has_citations；runner 消费 None-safe"
      pattern: "has_citations"
    - from: "citation_coverage"
      to: "EvalResult.metrics / _TARGETS"
      via: "report_md 聚合"
      pattern: "citation_coverage"
---

<objective>
按 D-01/D-02/D-03 修正引用质量闸门语义并新增 `citation_coverage`，同时把评测聚合层改成 None-safe，使零引用报告在度量侧强制失败。本计划不改 `node_report` 重试/status 机（D-04 / ADR-2）。

Purpose: 消除 F-01「零引用悖论」——否则后续真实基线与过程指标无意义。
Output: 修正后的 `CitationReport`、coverage 函数、None-safe runner/report、钉死单测。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-metrics-baseline/02-CONTEXT.md
@.planning/phases/02-metrics-baseline/02-RESEARCH.md
@AGENTS.md
@src/vela/agent/citations.py
@src/vela/eval/runner.py
@src/vela/eval/report.py
@src/vela/agent/graph.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 钉死零引用闸门与 citation_coverage 单测（RED→GREEN）</name>
  <files>tests/test_agent.py, src/vela/agent/citations.py</files>
  <read_first>
    - .planning/phases/02-metrics-baseline/02-CONTEXT.md（D-01/D-02/D-03/D-04）
    - src/vela/agent/citations.py（当前 dangling_rate/ok/to_dict）
    - tests/test_agent.py（既有引用用例风格；禁止 unittest.mock）
    - .planning/phases/02-metrics-baseline/02-RESEARCH.md Discretion Recommendations 事实句切分
  </read_first>
  <behavior>
    - total==0：dangling_rate is None；has_citations is False；ok is False
    - 有合法引用且无悬空：ok is True；has_citations is True；dangling_rate==0.0
    - 有引用且有悬空：ok is False
    - citation_coverage：空文本→1.0（无事实句）；纯标题/空行不计；含 [[EV:…]] 的事实句计入分子；分隔符类为中英文句号/叹问号/换行
    - to_dict() 含 has_citations，且 dangling_rate 可为 null（JSON None）
  </behavior>
  <action>
按 D-01/D-02 改 `CitationReport`：`dangling_rate` 在 `total==0` 返回 `None`；新增 `has_citations`（`total > 0`）；`ok = has_citations and not dangling`。同步 `to_dict()`。

按 D-03 与 RESEARCH 裁量：新增模块级 `split_factual_sentences(text) -> list[str]` 与 `citation_coverage(text) -> float`：
- 分隔：`re.split(r"[。！？.!?\n]+", text)` 后 strip
- 丢弃：空串；匹配 `^\s*#{1,6}\s` 的 Markdown 标题行；匹配 `^\s*[-*_]{3,}\s*$` 的分隔线
- 覆盖：句子内存在 `CITE_RX` 即计；coverage = covered/total_sentences；若无事实句返回 `1.0`（分母为 0 时视为无事实可评，避免假性 0）

⚠️ D-04/D-24：禁止改 `node_report` 重试、status、提示词语义。禁止在 `agent/nodes/` 建文件。

先写失败单测再实现，直至 pytest 绿。
  </action>
  <acceptance_criteria>
    - `CitationReport(total=0).dangling_rate is None` 且 `has_citations is False` 且 `ok is False`
    - 有合法引用无悬空时 `ok is True` 且 `dangling_rate == 0.0`
    - `to_dict()` 含键 `has_citations`；`total==0` 时 `dangling_rate` 为 JSON null
    - `citation_coverage("") == 1.0`；含 `[[EV:…]]` 的事实句提高 coverage；纯 `#` 标题不计事实句
    - `pytest tests/test_agent.py -k 'citation or zero_citation or coverage' -q` 退出码 0
    - `rg -n "insufficient_citation|retry" src/vela/agent/citations.py` 无本任务新增的 reporter 重试逻辑
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py -k 'citation or zero_citation or coverage' -q</automated>
  </verify>
  <done>零引用 ok=False / dangling_rate=None / has_citations / coverage 边界单测全部通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EvalRunner/report None-safe 聚合与 coverage 入报表</name>
  <files>src/vela/eval/runner.py, src/vela/eval/report.py, src/vela/agent/graph.py, src/vela/obs/metrics.py, src/vela/cli.py, tests/test_eval.py</files>
  <read_first>
    - src/vela/eval/runner.py（CaseResult.dangling_rate；metrics 均值；_one 中 float(...)）
    - src/vela/eval/report.py（_TARGETS）
    - src/vela/agent/graph.py（gauge report.dangling_rate 与 event rate= 传参处——仅允许 None-safe）
    - src/vela/obs/metrics.py（Metrics.gauge/snapshot 对 None 的 round 行为——须跳过或容忍 None）
    - src/vela/cli.py（cmd_eval 退出码四条件；diagnose 打印 dangling_rate）
    - .planning/phases/02-metrics-baseline/02-RESEARCH.md Pitfall 1 / Open Questions (RESOLVED) #1
  </read_first>
  <behavior>
    - CaseResult 可存 dangling_rate=None；metrics.dangling_citation_rate 只对有引用用例求平均悬空率
    - metrics 新增 citation_coverage、zero_citation_cases、citation_gate_pass_rate（has_citations and ok 的占比或等价）
    - _TARGETS 增加 citation_coverage: (0.9, ">=")——仅报表展示，不加入 cmd_eval 硬退出条件（D-25）
    - cmd_eval 原四条件保持；mock 黄金评测退出码契约不因新指标变红
  </behavior>
  <action>
1. `CaseResult.dangling_rate` 类型改为 `float | None`；`_one` 从 `citation_check` 读取时保留 `None`（禁止 `float(None)`）。另存 `has_citations`/`citation_ok`/`citation_coverage`（对 `st.report_md` 或等价字段调用 `citation_coverage`）。
2. `metrics()`：`dangling_citation_rate` = 仅 `dangling_rate is not None` 的用例均值；若全无引用则用 `None` 或明确哨兵并在报表标注；新增 `zero_citation_cases`、`citation_gate_pass_rate`、`citation_coverage`（用例均值）。
3. `report.py`：`_TARGETS` 增加 `citation_coverage`；其它指标表展示新键；逐用例表悬空列可打印 `None`。
4. None-safe 全覆盖：`graph.py` 仅对 `metrics.gauge("report.dangling_rate", …)` / 事件字段跳过 None；**或** `obs/metrics.py::gauge` 忽略 `None` 值——禁止改节点控制流/重试/status（D-24）。完成后 `rg -n "dangling_rate" src/` 确认无 `float(None)` / 未守卫 `round(None)`。
5. `cli.py` diagnose 打印与 `cmd_eval`：退出码**不**因 `has_citations` 新增硬门（D-25 / RESEARCH Open Q1 RESOLVED）；人读输出可展示零引用失败。
6. 单测覆盖 None-safe 聚合与 coverage 键存在。

解释器约定：`PYTHON=.venv/bin/python3`。
  </action>
  <acceptance_criteria>
    - `CaseResult.dangling_rate` 类型为 `float | None`；`_one` 遇零引用不抛 TypeError
    - `metrics()` 含 `citation_coverage`、`zero_citation_cases`（或等价）、`citation_gate_pass_rate`（或等价）
    - `_TARGETS` 含 `citation_coverage` 且目标为 `(0.9, ">=")`
    - `cmd_eval` 硬退出仍仅原四条件：`rg -n "has_citations|zero_citation" src/vela/cli.py` 不出现在退出码布尔表达式中
    - `PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py tests/test_eval.py -q` 退出码 0
    - `git diff src/vela/agent/graph.py` 无 node_report 重试 / status 机 / 提示词改动（仅允许 None-safe 计量）
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_agent.py tests/test_eval.py -q --tb=line</automated>
  </verify>
  <done>评测/CLI 消费修正后闸门；零引用度量失败；退出码契约保持；无 graph 推理改动。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模型报告文本 → CitationReport | 不可信 LLM 输出经程序化校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | CitationReport.ok | mitigate | 零引用强制 ok=False；单测钉死 total==0 |
| T-02-02 | Repudiation | eval dangling 聚合 | mitigate | None 与零引用分列统计，禁止把 None 当 0.0 粉饰 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 本计划无新包安装 |
</threat_model>

<verification>
- `make test-fast` 与相关 pytest 绿
- `rg -n "node_report|insufficient_citation" src/vela/agent/graph.py` 无本计划引入的重试逻辑
- 回归门稍后由全量 `make test` 在 wave 合并时确认
</verification>

<success_criteria>
- METR-01/METR-02 可观测行为成立
- D-04：未改 reporter 推理重试
- D-25：未破坏 cmd_eval 既有退出码四条件
</success_criteria>

<output>
Create `.planning/phases/02-metrics-baseline/02-01-SUMMARY.md` when done
</output>
