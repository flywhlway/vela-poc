---
phase: 02-metrics-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vela/config.py
  - docs/CONFIG_HASH_HISTORY.md
  - tests/test_obs_and_config.py
autonomous: true
requirements: [METR-03]

must_haves:
  truths:
    - "修改 skills / budget.yaml / llm.yaml / gateway/prompts.py 任一项后 config_hash 必变"
    - "修改 env_checks.yaml 不改变 config_hash（D-06）"
    - "docs/CONFIG_HASH_HISTORY.md 记录断代旧→新 hash、纳入清单、日期与原因"
  artifacts:
    - path: "src/vela/config.py"
      provides: "扩展后的 config_hash payload"
      contains: "budget"
    - path: "docs/CONFIG_HASH_HISTORY.md"
      provides: "NR-6 指纹断代映射表"
      contains: "old_hash"
  key_links:
    - from: "config_hash()"
      to: "load_skills / budget.yaml / llm.yaml / prompts.py"
      via: "canonical_json payload 键"
      pattern: "prompts_sha256|skills"
    - from: "docs/CONFIG_HASH_HISTORY.md"
      to: "首次扩展断代"
      via: "表行 old→new"
      pattern: "sha256:"
---

<objective>
按 D-05/D-06/D-07/D-08 扩展 `config_hash()` 覆盖技能库、预算、LLM 配置与提示词源文件，交付断代映射表，并用四类扰动 + env_checks 负例单测钉死。

Purpose: 修复 F-10——证据包 salt 必须能区分「改技能/预算/提示词」的版本。
Output: 新 payload 口径、`docs/CONFIG_HASH_HISTORY.md` 首行、扰动单测。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-metrics-baseline/02-CONTEXT.md
@.planning/phases/02-metrics-baseline/02-RESEARCH.md
@src/vela/config.py
@tests/test_obs_and_config.py
@src/vela/gateway/prompts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 config_hash 并钉死四类扰动单测</name>
  <files>src/vela/config.py, tests/test_obs_and_config.py</files>
  <read_first>
    - src/vela/config.py（config_hash / load_skills / load_yaml / canonical_json）
    - tests/test_obs_and_config.py（既有 hash 确定性用例）
    - .planning/phases/02-metrics-baseline/02-CONTEXT.md（D-05/D-06/D-08）
    - .planning/phases/01-llm/01-CONTEXT.md（D-16 env_checks 不进 hash）
  </read_first>
  <behavior>
    - 保留既有 pipeline/parsers/phases/canon_rules_version/algos
    - 新增 budget、llm、skills（经 load_skills()）、prompts_sha256（prompts.py 原始字节 sha256）
    - 不读取 env_checks.yaml；改 env_checks 内容 hash 不变
    - 分别扰动 skills YAML / budget.yaml / llm.yaml / prompts.py → hash 各变
  </behavior>
  <action>
按 RESEARCH「扩展 config_hash」示例扩展 payload（D-05）。skills 用 `load_skills()` 同源数据（或按 sorted 文件原始文本——须与加载同源且稳定）。`prompts_sha256` 读 `src/vela/gateway/prompts.py` 文件字节，禁止 import 后哈希（避免 docstring 外副作用）。明确不纳入 `env_checks.yaml`（D-06）。

单测用 tmp_path / 注入 `VELA_CONFIG_DIR` 或项目既有测试夹具模式：四类正向扰动 + env_checks 负例。注意 `load_yaml` 的 `lru_cache`——测试须清缓存或换 config_dir 使变更可见。

⚠️ 不改 prompts.py 语义内容（可在测试中临时改字节再还原）；不改 graph 控制流。
  </action>
  <acceptance_criteria>
    - `config_hash()` 返回值仍以 `sha256:` 开头
    - payload 含键覆盖 budget / llm / skills（或等价）与 prompts 内容哈希；不含 env_checks
    - 分别扰动 skills YAML、budget.yaml、llm.yaml、prompts.py 后 hash 各与基线不同（四条断言单测）
    - 仅扰动 env_checks.yaml 后 hash 与基线相同
    - `pytest tests/test_obs_and_config.py -k config_hash -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_obs_and_config.py -k config_hash -q</automated>
  </verify>
  <done>四类输入扰动使 hash 变化；env_checks 扰动不变化；格式仍为 sha256: 前缀。</done>
</task>

<task type="auto">
  <name>Task 2: 交付 CONFIG_HASH_HISTORY 断代映射表</name>
  <files>docs/CONFIG_HASH_HISTORY.md</files>
  <read_first>
    - .planning/phases/02-metrics-baseline/02-CONTEXT.md（D-07）
    - docs/ 目录既有文档风格（短、可操作）
  </read_first>
  <action>
新建 `docs/CONFIG_HASH_HISTORY.md`（D-07）。表格列：`date | old_hash | new_hash | added_inputs | reason`（裁量列名可微调但须含旧/新/纳入清单/原因）。

执行一次：用改动前解释器记录「扩展前」hash（若代码已改，从 git show HEAD:src/vela/config.py 临时对比或在 Task1 提交前采样——SUMMARY 写清采样命令）。首行记录：
- date: 2026-07-31 或实际断代日
- added_inputs: skills via load_skills / budget.yaml / llm.yaml / gateway/prompts.py
- reason: Phase 2 METR-03 / NR-6 首次扩展指纹覆盖

正文简短说明：历史证据包仍用旧 salt，不批量重算；之后有意改口径须追加行。禁止灌水。
  </action>
  <acceptance_criteria>
    - `docs/CONFIG_HASH_HISTORY.md` 存在且含 `old_hash`/`new_hash`（或表头等价列）与 `added_inputs`/`reason`
    - 至少一行含 `sha256:` 旧值与新值（新旧不同）
    - 正文声明历史包不批量重算、后续口径变更须追加行
    - 文件无 API key / `.env` 凭证模式匹配
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/CONFIG_HASH_HISTORY.md && grep -E 'old_hash|added_inputs|sha256:' docs/CONFIG_HASH_HISTORY.md | head -5</automated>
  </verify>
  <done>断代表落盘且含旧→新与纳入清单；NR-6 可审计。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 配置/提示词 → config_hash → 证据包 Merkle salt | 指纹遗漏导致版本不可区分 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Repudiation | config_hash 覆盖缺口 | mitigate | D-05 扩 skills/budget/llm/prompts + 扰动单测 |
| T-02-04 | Tampering | env_checks 误入指纹 | mitigate | D-06 排除 + 负例单测 |
| T-02-SC | Tampering | pip installs | accept | 本计划无新包 |
</threat_model>

<verification>
- pytest config_hash 相关全绿
- HISTORY 文件非空且含断代行
</verification>

<success_criteria>
- METR-03 验收条件满足
- 与 Phase 1 D-16 一致：env_checks 不进 hash
</success_criteria>

<output>
Create `.planning/phases/02-metrics-baseline/02-02-SUMMARY.md` when done
</output>
