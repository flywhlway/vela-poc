---
phase: 02-metrics-baseline
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-03, 02-04]
files_modified:
  - src/vela/eval/process.py
  - src/vela/eval/runner.py
  - src/vela/eval/report.py
  - src/vela/cli.py
  - tests/test_eval.py
autonomous: true
requirements: [METR-05, METR-08]

must_haves:
  truths:
    - "评测报表含 7 项过程指标键与每轮决策轨迹表（可测即可，不要求达标）"
    - "vela eval run --ablation 运行时 mask 剔除 expected_skills，不改 builtin.yaml"
    - "四项消融/泛化指标进入 _TARGETS；代理口径在报表脚注标明 Phase 5 后替换"
    - "未改 AgentGraph 节点控制流或 CONF-03"
  artifacts:
    - path: "src/vela/eval/process.py"
      provides: "7 项过程指标与轨迹聚合"
      exports: ["aggregate_process_metrics", "decision_trace"]
    - path: "src/vela/eval/report.py"
      provides: "_TARGETS 含消融四指标 + 过程展示"
      contains: "misdiagnosis_rate_under_ablation"
  key_links:
    - from: "EvalRunner --ablation"
      to: "SkillRegistry(skills=masked)"
      via: "mask expected_skills at runtime"
      pattern: "ablation|expected_skills"
    - from: "DiagnosisResult.events"
      to: "process metrics"
      via: "aggregate_process_metrics"
      pattern: "premature_stop_rate"
---

<objective>
实现过程指标聚合（METR-05/D-15）与消融评测集（METR-08/D-16/D-17）：全部在 eval 侧完成，禁止为指标去改 graph 推理或提前实现六级置信度。

Purpose: Phase 3~6 需要可观测过程/泛化尺子；本阶段只建立可测性。
Output: `eval/process.py`、`--ablation`、报表 _TARGETS 扩展、代理脚注、单测。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-metrics-baseline/02-CONTEXT.md
@.planning/phases/02-metrics-baseline/02-RESEARCH.md
@.planning/phases/02-metrics-baseline/02-04-SUMMARY.md
@src/vela/eval/runner.py
@src/vela/eval/report.py
@src/vela/agent/skills.py
@src/vela/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 过程指标与决策轨迹聚合入报表</name>
  <files>src/vela/eval/process.py, src/vela/eval/runner.py, src/vela/eval/report.py, tests/test_eval.py</files>
  <read_first>
    - .planning/phases/02-metrics-baseline/02-RESEARCH.md「过程指标数据源缺口」表与 Pitfall 7
    - src/vela/agent/state.py（rounds / used_skills / events 可达字段）
    - src/vela/eval/runner.py（_one 已有 gateway_stats）
    - src/vela/gateway/audit.py（Plan 03 后的 finish_reason）
  </read_first>
  <behavior>
    - metrics 含：premature_stop_rate / llm_parse_failure_rate / llm_truncation_rate / verdict_supported_ratio / skill_switch_per_session / unexplained_error_rate / citation_coverage
    - 报表含「每轮决策轨迹」表（case_id, round_no, selected_skill, stop, 关键 verdicts 摘要等——从现有 state/events 能拿到的字段）
    - 代理指标脚注：「代理口径，Phase 5 后替换」或「聚合自 events，Phase 3 前允许偏高」
  </behavior>
  <action>
新建 `eval/process.py`（D-15）。按 RESEARCH 表从 `DiagnosisResult.events` / `SessionState` / audit 聚合，**不改 graph 控制流**。

代理定义钉死（D-17 / RESEARCH）：
- premature_stop_rate：首轮 stop=True 会话占比
- llm_parse_failure_rate：plan.done 且 skill is None 且 stop=False 且无 actions（代理）
- llm_truncation_rate：finish_reason==length 占比（依赖 Plan 03 audit）
- verdict_supported_ratio：verify.done 的 supported/claims
- skill_switch_per_session：相邻 round selected_skill 变化次数均值
- unexplained_error_rate：消融外可用代理——ERROR 行未进 evidence_pool 的占比（经 LogQueryAPI，禁止直连 DuckDB）；若数据不足可返回 None 并脚注
- citation_coverage：复用 Plan 01 函数

报表增加过程指标节与轨迹表。过程目标**不**加入 cmd_eval 硬退出（D-25）。单测用构造 events/state 的最小夹具，不强制全链路 10 场景。
  </action>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_eval.py -k process -q</automated>
  </verify>
  <done>7 项过程指标键与轨迹表可测；无 graph 推理改动。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: --ablation 运行时技能 mask 与四指标入 _TARGETS</name>
  <files>src/vela/eval/runner.py, src/vela/eval/report.py, src/vela/cli.py, tests/test_eval.py</files>
  <read_first>
    - src/vela/agent/skills.py（SkillRegistry(skills=…)）
    - src/vela/agent/graph.py（__init__ skills 注入——只读用法，不改控制流）
    - src/vela/eval/golden.py（expected_skills）
    - .planning/phases/02-metrics-baseline/02-CONTEXT.md（D-16/D-17）
    - .planning/phases/02-metrics-baseline/02-RESEARCH.md 消融代理指标示例
  </read_first>
  <behavior>
    - CLI：`vela eval run --ablation` 旗标（非新子命令）
    - 对含 expected_skills 的故障场景：masked = skills 去掉 expected；健康场景跳过 mask 或计入对照（SUMMARY 写清分母）
    - 指标：misdiagnosis_rate_under_ablation / novel_detection_recall / unexplained_error_rate / confidence_calibration_error 进入 metrics 与 _TARGETS
    - 不要求达标；不改 config/skills/*.yaml；不实现 CONF-03
  </behavior>
  <action>
按 RESEARCH Pattern 3 在 `_one` 注入 masked `SkillRegistry`（D-16）。代理口径（D-17）：
- misdiagnosis：ablation 下 answered 且 predicted 不在无故障集合
- novel_detection_recall：ablation 下正确拒绝（unanswerable/human_gate 或 _no_fault）
- confidence_calibration_error：二元 ECE 代理（answered→conf=1 else 0）
- unexplained_error_rate：与过程指标同名时消融集上重算或复用，报表注明

`_TARGETS` 纳入四键（目标值可先用宽松占位，如展示用）；**cmd_eval 硬退出不引用这四键**（D-25）。报表脚注「代理口径，Phase 5 后替换」。

单测：构造假 golden + 可注入 skills 列表，断言 mask 去掉 expected id；metrics 含四键。
  </action>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_eval.py -k 'ablation or process' -q</automated>
  </verify>
  <done>消融一键可跑（单测级）；四指标在 _TARGETS；无 CONF-03 / 无改技能 YAML。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden expected_skills → runtime skill mask | 仅评测注入，不持久化残缺库 |
| events → 过程指标 | 代理口径须标注，防误读为能力 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Repudiation | 代理指标被当最终能力 | mitigate | 报表强制脚注 Phase 5 替换 |
| T-02-11 | Tampering | 消融改 builtin.yaml | mitigate | 仅运行时 mask；单测断言源文件未改 |
| T-02-SC | Tampering | pip | accept | 无新包 |
</threat_model>

<verification>
- process/ablation 单测绿
- `rg -n "CONF-03|novel:" src/vela/agent/` 无本计划新增实现
- graph.py 无控制流 diff（允许无 diff）
</verification>

<success_criteria>
- METR-05 / METR-08 可测性成立
- ADR-2 / D-24 未破
</success_criteria>

<output>
Create `.planning/phases/02-metrics-baseline/02-05-SUMMARY.md` when done
</output>
