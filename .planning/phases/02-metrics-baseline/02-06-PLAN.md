---
phase: 02-metrics-baseline
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified:
  - scripts/bench.py
  - Makefile
  - .planning/phases/02-metrics-baseline/baseline/README.md
  - .planning/phases/02-metrics-baseline/baseline/report.md
  - .planning/phases/02-metrics-baseline/baseline/result.json
  - .planning/phases/02-metrics-baseline/02-GATE-RESULTS.md
  - tests/test_realllm_baseline.py
autonomous: false
requirements: [METR-09, PERF-01]
user_setup:
  - service: volcengine_ark
    why: "METR-09/PERF-01 真实方差基线与 bench 需火山引擎凭据"
    env_vars:
      - name: VELA_LLM_PROVIDER
        source: "设为 volcengine"
      - name: VELA_ARK_API_KEY
        source: ".env（Phase 1 已配置；勿提交）"
      - name: VELA_ARK_BASE_URL
        source: ".env"
      - name: VELA_ARK_MODEL_CHAT
        source: ".env 或 llm.yaml 映射"

must_haves:
  truths:
    - "baseline/ 下存在 Markdown 人读报告 + JSON 机读明细，含 config_hash、doctor 指纹字段、N≥3、provider=volcengine、no-cache、逐次指标与聚合 CI"
    - "报告明确标注：仿真回归门、非能力宣称；G4 未测；44.4% 退役"
    - "bench 可对真实 provider 产出 token 成本与端到端 P95（可与基线同次采集）"
    - "make test 不触发付费；回归门 make test failed=0 且仿真已通过用例回归数=0"
  artifacts:
    - path: ".planning/phases/02-metrics-baseline/baseline/report.md"
      provides: "人读方差/成本/延迟基线"
      contains: "ci95|置信|非能力"
    - path: ".planning/phases/02-metrics-baseline/baseline/result.json"
      provides: "机读明细"
      contains: "config_hash"
    - path: "scripts/bench.py"
      provides: "真实 provider 成本与 P95"
      contains: "no-cache|estimated_cost|p95"
  key_links:
    - from: "make baseline / realllm"
      to: "baseline/result.json"
      via: "--no-cache --repeat N --provider volcengine"
      pattern: "no-cache"
    - from: "vela doctor --json"
      to: "baseline 元数据"
      via: "嵌入 fingerprint 键"
      pattern: "config_hash|provider"
---

<objective>
在闸门/指标口径已修正后，按 D-18..D-23 / D-25 收尾跑真实火山引擎无缓存基线（METR-09）并扩展 bench（PERF-01），产物落盘 `baseline/`，宣告 44.4% 退役（NR-1）。不改推理逻辑。

Purpose: Phase 3~6 一切准确率对比必须以本目录新基线为准。
Output: baseline 报告+JSON、bench 扩展、Makefile 目标、回归门记录、人工核准。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@$HOME/.cursor/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/02-metrics-baseline/02-CONTEXT.md
@.planning/phases/02-metrics-baseline/02-RESEARCH.md
@.planning/phases/01-llm/01-BASELINE.md
@.planning/phases/01-llm/01-08-SUMMARY.md
@scripts/bench.py
@Makefile
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 bench 并添加 make baseline 脚手架</name>
  <files>scripts/bench.py, Makefile, .planning/phases/02-metrics-baseline/baseline/README.md, tests/test_realllm_baseline.py</files>
  <read_first>
    - scripts/bench.py（现有建库吞吐 + mock 延迟）
    - src/vela/gateway/budget.py（Plan 03 snapshot 成本字段）
    - src/vela/cli.py（Plan 04/05 旗标：--repeat/--no-cache/--provider）
    - .planning/phases/02-metrics-baseline/02-CONTEXT.md（D-18/D-19/D-20/D-21/D-23）
    - tests/test_realllm.py（Phase 1 realllm 模式：模块级 mark + autouse 凭证守卫）
  </read_first>
  <action>
1. 扩展 `scripts/bench.py`（D-21）：`--provider`（已有可强化）、`--no-cache`、输出 JSON/摘要含 `diagnose_p95_s`、`estimated_cost_usd` 或 token 成本、`session_used`；真实 provider 路径可读 TokenLedger.snapshot。可与 eval 共用 `eval.stats` 做重复统计，但不强制跑完整 EvalRunner。
2. Makefile：新增显式付费目标 `baseline`（例如 `VELA_LLM_PROVIDER=volcengine` + `vela eval run --no-cache --repeat 3 ... --out .planning/phases/02-metrics-baseline/baseline`）与可选 `bench-volc`；**不得**把付费并入 `make test`/`make eval` 默认路径（D-20）。
3. `baseline/README.md`：说明必须 `--no-cache` + volcengine、产物文件名、禁止引用 44.4%、仿真非能力宣称。
4. 可选 `tests/test_realllm_baseline.py`：模块级 `pytestmark = realllm`，检查 baseline 产物 schema 或跳过若文件未生成——默认 addopts 排除，不进 CI 付费。

⚠️ 禁止提交 `.env` / API key；报告脱敏。
  </action>
  <acceptance_criteria>
    - `scripts/bench.py` 支持真实 provider 路径产出含 p95 与成本/token 字段的摘要或 JSON
    - Makefile 含显式 `baseline`（或等价）目标且**未**并入默认 `test`/`eval`
    - `baseline/README.md` 存在，写明 `--no-cache` + volcengine、禁止引用 44.4%、非能力宣称
    - 若新增 `test_realllm_baseline.py`：模块级 `realllm` 标记；默认 `pytest` collect 不含付费路径
    - `grep -n 'baseline\|no-cache' Makefile` 至少一命中
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/ -q -k 'not realllm' --collect-only 2>/dev/null | tail -5; grep -n 'baseline\|no-cache' Makefile; test -f .planning/phases/02-metrics-baseline/baseline/README.md</automated>
  </verify>
  <done>bench/Makefile/README 就绪；付费不进默认 test。</done>
</task>

<task type="auto">
  <name>Task 2: 跑回归门并准备基线采集命令</name>
  <files>.planning/phases/02-metrics-baseline/02-GATE-RESULTS.md</files>
  <read_first>
    - .planning/phases/01-llm/01-BASELINE.md（baseline_eval_correct_case_ids）
    - .planning/ROADMAP.md Phase 2 回归门
    - AGENTS.md 完成判据
  </read_first>
  <action>
在**全部 Plan 01–05 已合并**的工作树上执行（D-25）：
1. `PYTHON=.venv/bin/python3 make test` → failed=0；记录 passed 数
2. `PYTHON=.venv/bin/python3 make eval`（mock）→ 解析 `eval_result.json` 正确场景集合，与 `01-BASELINE.md` 的 `baseline_eval_correct_case_ids` 比对，**回归数必须为 0**（一票否决）
3. 将命令、退出码、正确 ID 集合、回归数写入 `02-GATE-RESULTS.md`（无密钥）

若回归数 > 0：停止，不得宣称 Phase 2 完成；排查是否误改推理或退出码契约。

同时写好将执行的付费基线命令块（供 Task 3 人工确认后执行），含 doctor --json 采集指纹步骤。
  </action>
  <acceptance_criteria>
    - `02-GATE-RESULTS.md` 存在且含 `failed: 0`（或等价）与 `regression_count: 0`
    - 记录正确场景 ID 集合，并与 `01-BASELINE.md` 的 `baseline_eval_correct_case_ids` 差集为空
    - 文件无 API key / `.env` 凭证模式
    - 含待执行的付费基线命令块（`--no-cache`、`--repeat`、N≥3、volcengine）
  </acceptance_criteria>
  <verify>
    <automated>grep -E 'regression_count|failed|baseline_eval' .planning/phases/02-metrics-baseline/02-GATE-RESULTS.md | head -20</automated>
  </verify>
  <done>GATE-RESULTS 记录 make test 全绿与回归数 0；付费命令待跑。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工核准真实无缓存基线落盘</name>
  <files>.planning/phases/02-metrics-baseline/baseline/report.md, .planning/phases/02-metrics-baseline/baseline/result.json</files>
  <read_first>
    - .planning/phases/02-metrics-baseline/02-GATE-RESULTS.md
    - .planning/phases/02-metrics-baseline/baseline/README.md
    - .planning/phases/02-metrics-baseline/02-CONTEXT.md（D-18/D-19/NR-1）
  </read_first>
  <action>
暂停：在用户凭据可用且 Task 2 回归门已通过后，执行付费基线（或由执行器在用户批准后跑）：
- `vela doctor --json` 采集指纹嵌入元数据（D-26，不改 doctor 契约）
- `vela eval run --no-cache --repeat N`（N≥3）`--provider` 对应 volcengine，out 到 `baseline/`
- 可选同次或紧随 `scripts/bench.py --provider volcengine --no-cache` 写入报告 PERF 章节（D-21/D-23）
- 生成 `baseline/report.md` + `baseline/result.json`：含 config_hash、doctor 字段、N、provider、cache=false、逐次指标、均值±std、ci95、成本与 P95
- 醒目标注：仿真回归门、非能力宣称、G4 未测、自此刻起禁止引用 44.4%（D-19 / NR-1）

分数低于 44.4% **不是失败**（尺子变准）。

**完成语义（必须写进 SUMMARY）：**
- **有凭证且跑完：** `baseline/report.md` + `result.json` 落盘 → METR-09/PERF-01 = **done**
- **无凭证/跳过付费：** 仅基础设施（Plan 01–05 + Task1/2）可验收；METR-09/PERF-01 标记为 **blocked**（不得写 done）；`baseline/` 可放 README「待补跑」占位，**禁止伪造数字**冒充基线完成
  </action>
  <what-built>
Plan 01–05 度量基础设施 + Task1/2 的 bench/Makefile/回归门记录。待人工确认凭据与付费跑数后落盘真实基线。
  </what-built>
  <how-to-verify>
1. 确认 `.env` 中火山引擎凭据可用：`PYTHON=.venv/bin/python3 make doctor` 连通项可接受。
2. 确认 `02-GATE-RESULTS.md` 中 `regression_count: 0` 且 pytest failed=0。
3. 批准后执行 Makefile `baseline`（或等价命令），检查：
   - `baseline/result.json` 含 `config_hash`、`n`≥3、`cache`/`no_cache` 为关、`provider` 为 volcengine
   - `baseline/report.md` 含均值±标准差、95% CI、成本/P95，并写明「非能力宣称」「44.4% 退役」
4. 确认报告无 API key 明文。
5. 回复 `approved`（基线已落盘）→ METR-09 done；若跳过付费，回复须含 `METR-09 blocked` /「待补跑」，**不得**把基础设施完成说成 METR-09 done。
  </how-to-verify>
  <resume-signal>approved（基线已落盘）或 METR-09 blocked / describe issues</resume-signal>
  <acceptance_criteria>
    - 有凭证路径：`baseline/report.md` 与 `baseline/result.json` 均存在；JSON 含 `config_hash`、`n`≥3、`provider`、no-cache；报告含 CI 与「非能力宣称」「44.4%」退役声明
    - 无凭证路径：SUMMARY/`GATE-RESULTS` 明确写 `METR-09: blocked`（及 PERF-01 blocked）；不得存在伪造的完整基线数字文件冒充完成
    - 产物无 API key 明文（`rg` 凭证模式零命中）
    - 人工 resume-signal 为 `approved` 或显式 blocked 说明
  </acceptance_criteria>
  <verify>
    <automated>echo "checkpoint:human-verify — 基线落盘由人工核准；有凭证须 baseline/report.md+result.json；无凭证须 METR-09 blocked"</automated>
  </verify>
  <done>METR-09/PERF-01 基线产物落盘（done）或明确 blocked/待补跑；NR-1 对齐。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地 CLI → 火山引擎 API | 付费出站；凭据仅 .env |
| 基线报告 → 后续阶段决策 | 禁止伪造；禁止当 G4 能力 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Information Disclosure | baseline 报告 | mitigate | 禁止写入 API key；沿用 redact |
| T-02-13 | Spoofing | 伪造基线数字 | mitigate | 人工 checkpoint；JSON 含逐次明细可审计 |
| T-02-14 | Denial of Service | 付费误入 CI | mitigate | realllm 排除；make baseline 显式 |
| T-02-SC | Tampering | pip | accept | 无新包 |
</threat_model>

<verification>
- GATE-RESULTS 回归数 0
- baseline 产物 schema 齐全（或待补跑声明）
- `make test` 不收集付费用例
</verification>

<success_criteria>
- METR-09 / PERF-01：有凭证则 baseline/ 落盘为 done；无凭证则显式 blocked（基础设施完成 ≠ METR-09 done）
- NR-1：44.4% 退役声明落盘（有基线报告时）或写入 README/GATE 待补跑说明
- D-25 回归门通过
</success_criteria>

<output>
Create `.planning/phases/02-metrics-baseline/02-06-SUMMARY.md` when done
</output>
