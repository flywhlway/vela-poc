---
phase: 02-metrics-baseline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vela/gateway/cache.py
  - src/vela/gateway/base.py
  - src/vela/gateway/audit.py
  - src/vela/gateway/budget.py
  - config/budget.yaml
  - .env.example
  - tests/test_gateway.py
autonomous: true
requirements: [METR-06, PERF-02]

must_haves:
  truths:
    - "LLM 磁盘缓存键为 (provider, physical_model, prompt_sha256, params)；默认根目录 .cache/vela/llm/"
    - "--no-cache 或 VELA_LLM_CACHE=0 时不读写缓存；同键二次 chat 命中率可测且 >90%"
    - "缓存挂在脱敏之后、provider.complete 之前；不缓存未脱敏内容"
    - "TokenLedger.snapshot 含累计 tokens 与按配置单价估算成本；超 diagnose_cost_alert 发 EventBus ALERT 且不替代 BudgetExceeded"
  artifacts:
    - path: "src/vela/gateway/cache.py"
      provides: "磁盘缓存读写与键规范化"
      exports: ["LLMDiskCache", "cache_key"]
    - path: "src/vela/gateway/budget.py"
      provides: "成本归集与告警钩子"
      contains: "estimated_cost"
    - path: "config/budget.yaml"
      provides: "cost 单价与告警阈值"
      contains: "diagnose_cost_alert"
  key_links:
    - from: "LLMGateway.chat"
      to: "LLMDiskCache"
      via: "redact 后 lookup/store"
      pattern: "cache"
    - from: "TokenLedger.charge"
      to: "EventBus ALERT"
      via: "estimated_cost > diagnose_cost_alert"
      pattern: "cost.alert|diagnose_cost"
---

<objective>
落地 LLM 四元组磁盘缓存（METR-06/D-12/D-13）与 TokenLedger 成本归集可观测（PERF-02/D-22），并记录 finish_reason 供后续过程指标聚合。不改 Provider 业务语义与预算硬切断语义。

Purpose: 重复评测可承受成本；成本/延迟基线有计量源；过程指标 llm_truncation_rate 有数据源。
Output: cache 模块、gateway 钩子、budget cost 段、gateway 单测。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-metrics-baseline/02-CONTEXT.md
@.planning/phases/02-metrics-baseline/02-RESEARCH.md
@src/vela/gateway/base.py
@src/vela/gateway/budget.py
@src/vela/gateway/audit.py
@config/budget.yaml
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 LLM 磁盘缓存并挂到 LLMGateway.chat</name>
  <files>src/vela/gateway/cache.py, src/vela/gateway/base.py, src/vela/gateway/audit.py, .env.example, tests/test_gateway.py</files>
  <read_first>
    - src/vela/gateway/base.py（chat：redact → precheck → complete → charge → audit 顺序）
    - src/vela/gateway/audit.py（prompt_sha256 已有）
    - .planning/phases/02-metrics-baseline/02-RESEARCH.md Pattern 2 / Pitfall 4 / Discretion 缓存格式
    - .gitignore（确认 .cache/ 已忽略）
  </read_first>
  <behavior>
    - 键 = sha256(canonical_json({provider, physical_model, prompt_sha256, params}))；文件 `.cache/vela/llm/<key>.json`
    - 内容含 response_text、token 计数、finish_reason、created_at
    - 环境：VELA_LLM_CACHE_DIR 覆盖根；VELA_LLM_CACHE=0 或 gateway/cli `--no-cache` 关闭（本任务在 gateway 层支持 enable 开关；CLI 旗标由 Plan 04 接线）
    - 命中仍 ledger.charge（用缓存内 token 数）并增加 cache_hits；损坏 JSON → miss
    - 同 prompt 二次调用：第二次不触达 provider.complete（用 MockProvider 计数器验证）
    - determinism/测试默认关闭缓存（conftest 或 gateway 默认 enable=False，评测显式开启）
  </behavior>
  <action>
新建 `src/vela/gateway/cache.py`（D-12）。在 `LLMGateway.chat`：**必须先 redact**，再查缓存，再 precheck/complete；写缓存仅存脱敏后流量（D-13 + 安全）。禁止按 provider 名写专属分支。

`audit.py`：记录 `finish_reason`（允许，非推理逻辑），供 METR-05。

测试默认禁用缓存，避免污染 determinism；专用用例显式开启并用 tmp_path 作 cache 根。目标命中率验证：同键两次 chat，第二次 hit。

`.env.example` 增加 `VELA_LLM_CACHE` / `VELA_LLM_CACHE_DIR` 注释行（无密钥）。

⚠️ 不改 graph 控制流；不引入 Redis/网络缓存。
  </action>
  <acceptance_criteria>
    - 存在 `src/vela/gateway/cache.py` 且导出缓存键/读写 API
    - `LLMGateway.chat` 在 redact 之后查/写缓存（`rg -n "redact|cache" src/vela/gateway/base.py` 顺序可审计）
    - `VELA_LLM_CACHE=0` 或 enable=False 时不读写磁盘；同键二次 chat 第二次不调用 provider.complete（单测）
    - 审计记录含 `finish_reason` 字段
    - `.gitignore` 覆盖 `.cache/`；`.env.example` 含 `VELA_LLM_CACHE` 说明行
    - `pytest tests/test_gateway.py -k cache -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_gateway.py -k cache -q</automated>
  </verify>
  <done>缓存键四元组、脱敏后挂载、可关闭、命中可测、finish_reason 入审计。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TokenLedger 成本归集与超限 ALERT</name>
  <files>src/vela/gateway/budget.py, config/budget.yaml, tests/test_gateway.py</files>
  <read_first>
    - src/vela/gateway/budget.py（TokenLedger.charge/snapshot/BudgetExceeded）
    - src/vela/config.py（load_yaml / load_budget——本任务禁止改 config.py，避免与 Plan 02 并行冲突；成本段由 TokenLedger 直接 load_yaml("budget.yaml")["cost"]）
    - config/budget.yaml
    - src/vela/obs/events.py（EventBus 发射模式）
    - .planning/phases/02-metrics-baseline/02-CONTEXT.md（D-22）
  </read_first>
  <behavior>
    - budget.yaml 顶层 `cost:` 段：input_per_1k / output_per_1k / diagnose_cost_alert（USD/1K 占位可 0.0，alert 给合理默认如 1.0）
    - snapshot 增加 estimated_cost_usd、prompt_tokens、completion_tokens（或等价累计）、cache_hits（若 ledger 可见）
    - 会话估算成本超过 diagnose_cost_alert → EventBus ALERT（如 event type cost.alert）；不抛 BudgetExceeded
    - token 硬切断语义不变：超 round/session token 仍 BudgetExceeded
  </behavior>
  <action>
扩展 `budget.yaml` 的 `cost:`（D-22）；`load_budget` 或独立 `load_cost_rates()` 读取单价——配置驱动，禁止硬编码单价进业务。扩展 `TokenLedger`：累计 prompt/completion、估算成本、`snapshot()` 可被 eval/bench 读取；超限告警通过可选 `on_alert` 回调或直接 `EventBus`（与项目事件风格一致）。

单测：charge 后 snapshot 含成本字段；设极低 alert 阈值触发 ALERT；token 超限仍抛 BudgetExceeded。

若 `BudgetProfile` frozen dataclass 不便塞成本字段，允许 TokenLedger 直接读 `load_yaml("budget.yaml")["cost"]`，但须可测、可配置。
  </action>
  <acceptance_criteria>
    - `config/budget.yaml` 含顶层 `cost:`（含单价与 `diagnose_cost_alert`）
    - `TokenLedger.snapshot()`（或等价）含估算成本与累计 token 字段
    - 成本超 alert 触发 EventBus ALERT（或 on_alert），**不**抛 `BudgetExceeded`
    - token 超 round/session 仍抛 `BudgetExceeded`（既有单测不红）
    - `pytest tests/test_gateway.py -k 'cost or ledger or budget' -q` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src VELA_CONFIG_DIR=config .venv/bin/python3 -m pytest tests/test_gateway.py -k 'cache or cost or ledger or budget' -q</automated>
  </verify>
  <done>成本归集可观测；ALERT 不替代硬切断；配置有 cost 段。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 出站 prompt → 磁盘缓存 | 仅脱敏后内容可落盘 |
| TokenLedger → 告警/切断 | 告警软、切断硬 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | LLMDiskCache | mitigate | 缓存点在 Redactor.redact 之后；.cache/ gitignore |
| T-02-06 | Tampering | 缓存 blob | mitigate | 键绑定 prompt_sha256；坏 JSON→miss |
| T-02-07 | Elevation of Privilege | 缓存跳过 charge | mitigate | 命中仍 charge + cache_hits 分计 |
| T-02-SC | Tampering | pip installs | accept | 本计划无新包 |
</threat_model>

<verification>
- gateway cache/cost 单测绿
- `make test-fast` 不触发付费
</verification>

<success_criteria>
- METR-06 / PERF-02 基础设施可测
- 预算硬切断语义未削弱
</success_criteria>

<output>
Create `.planning/phases/02-metrics-baseline/02-03-SUMMARY.md` when done
</output>
