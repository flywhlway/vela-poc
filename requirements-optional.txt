# ---- 可选依赖：缺失时自动降级到标准库实现，功能不受影响 ----
xxhash>=3.4      # 缺失 -> 回退 hashlib.blake2b(8B)，norm_hash/row_hash 语义不变
blake3>=0.4      # 缺失 -> 回退 hashlib.blake2b(16B)，raw_hash 语义不变
fastapi>=0.110   # 缺失 -> `vela serve` 回退到标准库 http.server
uvicorn>=0.29
pytest>=8.0      # 运行测试套件所需
scipy>=1.11      # 评测 --repeat：Student t 95% CI（vela.eval.stats）；主链路不强制
